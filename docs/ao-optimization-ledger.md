# AO Optimization Ledger

This ledger inventories every optimization or diagnostic requested for the UVSR
AO performance project, every relevant Activision GTAO and Intel XeGTAO
technique, and additional candidates found in primary-source research. It is
anchored to canonical commit `5f43205ecfe00e31fd64af34cad0f031472a224c` and
the target profile of 1920x1080 output, half-linear-resolution AO, eight total
radial samples, radius 3.0, thickness 0.5, quadratic spacing, AO only, and no
adaptive, visibility-temporal, or user-enabled spatial filtering. The
Activision PS4 approximation and XeGTAO remain in this document only as
researched and measured rejections; neither is present in the build.

## Ledger Status

The inventory contains 414 unique IDs. Historical row text records what was
implemented and measured during the investigation; the removal table below is
the current authoritative override for retired families. No inspected source
created a licensing block.

Results that cite source inspection are established on `5f43205`. The current
controlled results were collected on the target Intel Arc integrated GPU
(`DEV_7D55`, driver `32.0.101.8724`) at 1920x1080 in Release, with 120 warm-up
frames and 600 measured frames per entry. The compatibility, historical per-
function math, paired-buffer, historical new-candidate, and fixed-sample
matrices all completed every
requested measured frame with zero incomplete frames. Older NVIDIA measurements
remain historical cross-adapter evidence and are labeled as such. No timing
cited from another product or GPU is a UVSR result.

## Removed Feature Disposition

These decisions override older inventory rows that say an experiment was
implemented or retained. The measurements remain below so the rejection is
auditable rather than disappearing with the code.

| Removed Family | Measured Or Generated-Code Evidence | Expected Improvement After Evidence | Why It Was Removed | Current Build Surface |
| --- | --- | ---: | --- | --- |
| Per-function Standard/Fast Exact/Fast Approximation math controls | The 24-profile controlled Intel matrix kept nearly every comparison within approximately +/-1%; the strongest directional result was only 0.93% and did not establish a repeatable production win | Approximately 0%; any plausible sub-1% gain is below the current decision threshold | The dropdowns and runtime selectors added substantial UI, benchmark, constant-buffer, and shader complexity without a conclusive benefit | Removed from settings, UI, CLI matrices, benchmark schema, constants, shaders, and tests; retained shaders use their static validated math |
| Packed-edge 4x4, separate and fused | Generated DXIL retained 209 static texture loads and emitted no gather instruction; runtime measurements put both paths near 4.70 ms against a roughly 2.71 ms compact Reference in that reconstruction run | Negative: approximately 1.99 ms slower, or about a 73% regression in that run | Sixteen source evaluations per full-resolution pixel overwhelmed the intended wider-support benefit | Removed from profiles, planner enums, UI, benchmark sequence, shader macros, packaged permutations, and tests; packed-edge 2x2 remains |
| XeGTAO LUT/mixed, inline-Hilbert/mixed, and LUT/FP32 profiles | Every controlled Intel XeGTAO variant was slower than canonical Reference. Internal Xe-only tuning changed ordering but never made the family a faster UVSR replacement | Negative versus canonical Reference on this driver; 0% expected production improvement as a UVSR performance profile | It is a different full-resolution 18-read estimator and failed the requested faster-replacement goal on the target Intel driver | Removed from profiles, verification presets, UI, CLI matrices, host resources/passes, shaders, shader package, and tests; historical source and timing evidence remains below |
| Adaptive sparse sampling | Historical compatibility and paired-format runs did not establish a stable complete-effect win; feedback-related deltas changed direction or had worse p95 behavior | No reliable positive estimate remains after the measured inconsistency | Its neighborhood analysis, feedback history, motion dependency, stochastic budget behavior, formats, and permutations made custom combinations harder to interpret than fixed-count comparisons | Removed from settings, buffer controls, constant buffers, resources, bindings, shader permutations, benchmark serialization, planner keys, and tests; all remaining sampling uses an explicit fixed count |
| Automated benchmark test matrices and Reference comparison | The completed Intel runs already supplied the fixed-count, noise, reconstruction, math, buffer, compatibility, and candidate evidence used by this ledger | 0%; orchestration never changed renderer GPU work | Multi-run actions made the Statistics UI and benchmark state harder to read; the internal Reference profile remains available to command-line benchmark validation | Removed from the UI and `--benchmark-sequence` command line; only isolated current-profile measurement remains |
| Activision PS4 scheduler plus scalar/packed-gather GTAO approximation | Both profiles were slower than canonical Reference on the controlled Intel driver; packed versus scalar ordering changed across repeats, and the path required source-specific depth, spatial, temporal, and upsample work | Negative versus Reference: scalar was 23.24% slower and packed was 18.63% slower in the current controlled check | It was an approximate different-estimator research pipeline, not a faster UVSR replacement, and its two filter variants added substantial duplicate code and UI | Removed from planner/profile enums, verification presets, UI, host resources, shaders, packaged permutations, and tests |
| Visibility temporal accumulation settings and diagnostic profile | The current renderer has TAA, while visibility history was disabled in the primary workload and its paired format timings were inconsistent | 0% expected primary-workload improvement; temporal buffer pairs did not establish a stable win | Two temporal systems exposed overlapping human controls and unnecessary buffer-format choices | Removed from the AO settings surface and profile catalog; the temporal-copy/source shaders and tests are removed, all selectable profiles keep visibility accumulation off, and renderer TAA owns temporal stability |
| Diagnostic floors, fused debug output, and analytic-horizon attribution controls | These modes intentionally produced invalid, debug, or different-estimator output; none was a shippable performance setting | 0% product improvement by definition | They answered investigation questions but cluttered the profile catalog and could be mistaken for production candidates | Removed from profile and verification enums, planner passes and bindings, UI, host selection, packaged shader variants, and tests; historical measurements remain below |
| 16x8/8x16 groups, 32/64/128 radius clamps, and conservative filter algebra | Groups were effectively neutral, every clamp regressed against Fixed 8 at radius 3, and conservative algebra was 0.27% slower in the controlled check after an earlier 0.223 ms smoke regression | Approximately 0% for the tested workload; measured directions were neutral or negative | None established a repeatable win, while each expanded profile, permutation, validation, and UI surface | Removed from configuration/profile enums, UI, planner contracts, host shader selection, shader package, and tests |
| Duplicate-rejection and full-mask-exit feature-off controls | Matched repeats stayed inside system drift and did not justify user-facing toggles | No proven user-selectable improvement | The underlying shortcuts are safe internal behavior; only their investigation controls failed to earn UI or shader variants | Off profiles, uniforms, shader variants, and tests removed; duplicate-pixel rejection and full-mask early exit remain always enabled |
| Mixed-Precision AO 8T and Aggressive Experimental AO 8T verification presets | Neither preset had a compiled implementation and both reported unavailable | 0% | Unavailable menu entries were not runnable experiments and made the profile list misleading | Removed from verification enums, profile menu, validation, and tests |
| Planner-only R8 AO, embedded count/edge R16, R8_UNORM edges, and fixed-18 source-port states | Every state was unavailable or belonged only to the removed XeGTAO/packed experiment families; no retained profile selected one | 0% | Keeping impossible enum/resource branches made removed experiments appear architecturally available | Removed from planner enums, resource masks, descriptions, validation branches, and tests |

## Last Cleanup Removal Inventory

This inventory is deliberately limited to the immediately preceding cleanup
request: make **Buffers** a sibling drawer, decouple buffer presets, remove
diagnostic presets, and remove failed performance experiments. It excludes
everything removed in earlier requests, including XeGTAO, the PS4 scheduler,
scalar/packed-gather GTAO, packed-edge 4x4, adaptive sampling, visibility
temporal accumulation, benchmark matrices, and per-function math controls.

| Removed In That Cleanup | Exact Features | Developer-Panel Restoration Note |
| --- | --- | --- |
| Trace diagnostics | Constant scheduler; constant trace; depth-read-only trace; bitmask-ALU-only trace | Useful only as timing floors; could return behind an explicitly invalid-output developer panel |
| Reconstruction and composition diagnostics | Nearest resolve; bilinear resolve; composition-only; composition bypass; fused full-resolution AO debug output | Useful for attribution, not product output; safe only when clearly marked diagnostic |
| Algorithm-attribution control | UVSR analytic-horizon / Activision Closest Match profile | Different estimator rather than a UVSR optimization; a developer panel would need a persistent different-output warning |
| Failed thread-group experiments | Fixed 16x8 and fixed 8x16 first-trace groups | Both were effectively neutral; reasonable hidden controls if future Intel drivers warrant a retest |
| Failed radius-clamp experiments | Projected-radius clamps at 32, 64, and 128 pixels | All regressed against the fixed-eight baseline at radius 3; hidden retesting is possible for substantially larger radii |
| Failed filter-algebra experiment | Conservative FP32 filter algebra with invariant hoists and offline Gaussian weights | Slightly slower in the controlled run; could return only as one bundled developer experiment, not per-function controls |
| Failed feature-off controls | Duplicate-pixel rejection Off; full-mask early exit Off | Only the Off experiments were deleted; both safe shortcuts remain hard-enabled in every retained shader |
| Unavailable verification presets | Mixed-Precision AO 8T; Aggressive Experimental AO 8T | These had no compiled implementation, so restoring menu entries alone would still produce unusable controls |
| Dead planner-only formats and states | Fixed-18 sample specialization; R8 raw AO; embedded R16 count-plus-edge storage; R8_UNORM edge storage and their resource-mask branches | Do not restore as UI until a real retained pass allocates, binds, and validates each state |

## GI Compatibility

The fixed-eight trace used by Low and Medium has always had compiled AO, GI,
and AO+GI permutations. Its former GI failure came from treating the fixed
trace as the AO-only fused final-application experiment. Product quality
presets use compatible separate reconstruction/composition whenever GI is
active.

The separate packed-edge writer and filter also had GI-capable shader algebra,
but the host planner restricted the profile to AO and did not allocate or bind
the edge plane for GI workloads. The four separate packed-edge 2x2 profiles now
accept AO, GI, or AO+GI, allocate the R8_UINT plane for any active consumer, and
compile/bind explicit AO/GI filter permutations. The advanced depth-normal
reconstruction profile can use that path under GI. The fused
resolve/application profiles remain `(Mutex GI)` because they directly apply
AO to the lighting target and cannot preserve the separate GI reconstruction
and composition order without becoming a different multi-output shader.

Buffer configuration appears as a sibling drawer directly below the visibility
drawer, with a format-only preset dropdown followed by vertically labeled
**Trace AO**, **Current Bounce GI**, **Accumulated GI**, **Output AO**,
**Output GI**, and **Long-Range Depth** controls. The presets are
**Performance Precision**, **Default Precision**, **Compact AO**, and
**Compact GI**. Low and Medium begin with Performance Precision; High and Ultra
begin with Default Precision. Manual buffer edits preserve the rest of the
visibility configuration and change the product profile label to
**Custom / Advanced**.

## Shared Exact Sampling

The current build replaces the free-form sample slider and separate later-
bounce specialization selector with one **Exact Sample Count** dropdown. Its
only selectable totals are 8, 12, 16, 20, 24, 48, and 64. Every value has
compiled AO-only, GI-only, AO+GI, bounce-metadata, later-bounce reinjection,
packed-noise composition, and packed-edge composition permutations for every
retained estimator and scheduler specialization.

AO and every active GI bounce use exactly the selected total. This current
behavior supersedes historical rows that describe a halving schedule, an
eight-sample later-bounce floor, or separate fixed-later/fixed-all profiles.
The generic runtime-count shader remains only for internal Reference benchmarks
and fallback composition; it is not a product preset or selectable count.
Exhaustive CPU equivalence coverage now checks all seven exact totals across all
three estimators and AO, GI, and AO+GI consumers.

## Contribution-Terminated GI

**Limit Bounces** is enabled by default and preserves the explicit one-through-
eight path. Disabling it records a 16-bounce fault-guard command stream, starts
at the selected contribution cutoff, and raises that bar by 4x per bounce.
Each bounce performs a wave-coalesced contribution test and sets one GPU flag
only when its stored frontier can clear the next bar. A one-thread control pass
converts the flag into either the normal full-screen indirect dispatch or a
zero-group dispatch. There is no CPU readback or GPU/CPU synchronization.

The default limited path uses the `ENABLE_BOUNCE_CONTINUATION=0` trace
specialization. At the default one bounce it records neither a later-bounce
trace nor a continuation-control dispatch; the unlimited-bounce feature
therefore contributes no per-frame GPU work until **Limit Bounces** is disabled.
AO Power similarly defaults to an identity specialization with its `pow`
instruction compiled out. A distinct powered compositor is selected only after
the AO Power slider moves away from 1.0.

This mode is not expected to beat a deliberately limited four-bounce render;
it computes additional visible transport when that transport remains above the
bar. Its performance comparison is against a naive fixed 16-bounce
implementation. If convergence occurs after bounce four, it removes 12 of the
15 possible later full-screen traces, or 80% of that naive later-bounce dispatch
work, while retaining 15 one-thread control dispatches. Convergence after
bounce eight removes 8 of 15 later traces, or 53%. Exact total-frame savings
remain scene, exposure, intensity, resolution, and cutoff dependent and require
an isolated measurement; the default limited path adds no dispatch.

The final controlled Intel Arc run used 120 warm-up and 240 measured frames at
the locked 1920x1080 Sponza camera. Both generic and fixed-eight
contribution-terminated paths completed 240/240 frames with zero incomplete
frames and visually clean captures. Both converged by bounce four. Generic
Reference measured 6.7620 ms complete median, 7.0455 ms complete p95, and
3.5361 ms for **Later Bounces**. Exact fixed-eight first/later tracing measured
6.0759 ms, 6.4526 ms, and 2.8820 ms respectively: 10.15% lower complete
median, 8.41% lower complete p95, and 18.50% lower later-bounce median than the
generic extended-bounce profile. This is a profile-to-profile result, not a
claim that extended transport is faster than a deliberately capped four-bounce
render.

Authored emissive sources remain enabled at the calibrated gain 4.0, but their
checkbox and gain slider have been removed. This preserves current lighting
behavior while eliminating two controls that were not part of the retained
performance experiments.

Blocked dispositions are not measured rejections. In particular, F-030 and
O-039 remain unmet deliverable/evidence gaps. Auto Fixed (D-005), trace LDS
(D-020), reconstruction LDS (D-024/R-011), four-output coarsening
(D-026/R-013), and staged AO-only ILP (L-015/O-022) remain isolatable runtime
families with no complete selectable implementation. Their current architecture
dispositions mean only that the build lacks the experiment; that absence does
not establish that a benchmark-only PSO would be unreasonable. D-019, L-016,
and O-037 have concrete prerequisites recorded in their rows and still need
implementation or stronger generated-code/tooling evidence before they can be
called evaluated experiments. These entries are intentionally visible as
incomplete requirements rather than being reported as slower, rejected, or
impossible.

Feasible curated source experiments also remain screen-space-size horizon-
thickness EMA (A-011), depth-derived receiver normals (A-014), velocity-
agreement adaptive clamp width (Q-027), XeGTAO Low (X-002), XeGTAO Medium
(X-003), native R8 AO storage/decode (X-018), standalone depth-derived-normal
generation (X-020), and in-main depth-derived normals (X-021). Their row-level
correctness, architecture, quality, and measurement gates explain why they are
deferred; none of those dispositions means the experiment is impossible.

## How to Read This Ledger

Start with **Controlled Intel Finalist Ranking** for measured priorities and
**Removed Feature Disposition** plus **Plain-English Production Disposition**
for the keep/drop decision. In the
large inventory, search by optimization name or ID. **Final Disposition** says
whether the feature is retained, optional, diagnostic-only, already equivalent,
not applicable, or blocked; **Reason** explains why. A positive saving means
`baseline - candidate`, so larger is faster. Compare only rows that name the
same baseline. The removed Activision PS4 GTAO approximation and removed UVSR
horizon control are different estimators or workloads and are not
interchangeable with the canonical UVSR visibility-bitmask result. XeGTAO, PS4,
horizon, and diagnostic comparisons are historical rejection or attribution
evidence.

## Classification and Evidence Conventions

- Exact means the same input texels and LODs, sample set and ownership order,
  estimator, weights, output semantics, and required intermediate quantization
  are preserved. Generated-code changes still require image and fixture checks.
- Numerical means the algorithm is retained but arithmetic, transcendental
  evaluation, precision, format, or rounding changes.
- Algorithmic means sampling, depth representation, reconstruction, history,
  filtering, visibility, or lighting behavior changes.
- Diagnostic means a deliberately non-product workload used to isolate a timing
  floor. Diagnostic controls never establish product-image performance.
- Zero inactive cost means CPU selection of the reference pipeline with no hot
  shader branch, extra binding, texture, pass, history, or optional dispatch.
- Expected-impact entries are engineering estimates, not measurements. They use
  the supplied approximately 1.2 ms trace, 1.1 ms filtering, and 0.9 ms
  previously unattributed observation; exact logical byte or invocation changes
  where derivable; and low/medium/high ranking elsewhere. Estimates overlap
  heavily and must not be
  added together. Only a recorded adapter, build, profile key, warm-up, sample
  count, median, and p95 can replace an estimate with a result.

## Remaining Feature Scorecard

This scorecard covers every remaining user-selectable optimization family.
Fixed-count variants and the four packed-edge reconstruction variants are
grouped so the ranking describes real decisions rather than repeating near-
identical rows. Removed diagnostics and failed experiments do not appear in
these four current tables; their measured scores remain inferable from the
historical controlled check and the removal table above.
Ordinary image controls such as AO strength, GI intensity, sample count, and
spatial radius are not optimizations and are outside this
scorecard.

Ranks retain their original evidence-order numbers when a removed family is
deleted; the 0-100 scores, not contiguous numbering, are the prioritization
signal.

The three component scores have different meanings:

- **Universal Performance** rewards repeatable speed across hardware,
  consumers, and ordinary settings without changing the estimator or image.
- **Situational Performance** rewards the best credible speed when the
  workload, consumer, and quality constraints match.
- **UI Nonredundancy** gives 100 to a unique, understandable decision that
  belongs in the ordinary UI and 0 to a duplicate or benchmark-only control
  that should be hidden.
- **Average** is the unweighted arithmetic mean of those three scores, rounded
  to the nearest integer. It is a prioritization aid, not an additive speed
  estimate or an image-quality approval.

### Current Controlled Intel Check

The current check used PBR Sponza Decorated at 1920x1080 on Intel Arc graphics,
with 120 warm-up and 480 measured frames per profile. Twenty-two main profiles
and three matched repeats completed every requested frame with zero incomplete
frames. The executable reports build identity `96939e1`; it also contains the
current uncommitted UI and packaging fixes. Raw JSON, CSV, and final-frame
captures are in
`visibility-benchmarks/scorecard-controlled-20260719` and
`visibility-benchmarks/scorecard-controlled-20260719-repeat`.
The radius-clamp, group-shape, conservative-math, horizon, feature-off,
Activision Schedule, and PS4 rows below are historical rejection or attribution
evidence; their user-selectable profiles and implementation paths have since
been removed. Duplicate rejection and full-mask exit remain internal defaults,
but their feature-off controls are gone.

| Candidate And Correct Baseline | Current Median Result | Interpretation |
| --- | ---: | --- |
| Exact Fixed 8 + Fused versus Reference | 1.9453 versus 2.5543 ms; 23.85% faster | Strong exact AO-only winner; agrees with the earlier 20.6-22.4% controlled Intel range |
| Exact Fused versus Reference | 2.0179 versus 2.5543 ms; 21.00% faster | Strongest broadly reusable component when the AO-only fused consumer contract applies |
| Fused Depth-Normal Reconstruction And Apply versus Reference | 1.9970 versus 2.5543 ms; 21.82% faster | Strong algorithmic result; manual edge and motion acceptance remains mandatory |
| Intel Edge-Guided Reconstruction Methods versus Reference | 2.2467-2.3133 ms; 9.44-12.04% faster | All four retained variants helped here, with depth-guided fastest and depth-plus-normal safer at discontinuities |
| Activision Schedule versus Reference | 2.4385 versus 2.5543 ms; 4.54% faster | Historical result only; the coupled scheduler was removed with the PS4 approximation |
| Offline Packed Spacetime Noise versus Reference | 2.4654 versus 2.5543 ms; 3.48% faster | Small exact delivery win on this run; earlier retries were less stable |
| Exact Fixed 8 versus Reference | 2.4903 versus 2.5543 ms; 2.51% faster | Supporting specialization, consistent with the earlier 1.9-4.7% Intel range |
| Radius Clamps versus Fixed 8 | 0.24-0.83% slower | No win at radius 3; compare with Fixed 8 because each clamp profile already includes that specialization |
| 16x8 And 8x16 Groups versus Fixed 8 | 0.02% slower and 0.09% faster | Effectively neutral; absolute Reference deltas would incorrectly credit the bundled Fixed-8 specialization |
| Conservative Numerical versus Reference | 0.27% slower | No performance reason to promote it |
| UVSR Horizon GTAO versus Reference | 16.16% faster | Different estimator; useful algorithm ceiling, not an exact UVSR replacement |
| PS4 Packed Gather versus PS4 Scalar | 3.74% faster | Historical comparison only; both variants were removed after remaining slower than Reference |
| PS4 Scalar versus Reference | 23.24% slower | Historical source-inspired comparison; the profile is no longer selectable |
| Duplicate Rejection Off And Full-Mask Exit Off | Repeats were 0.80% and 0.52% faster than Fixed 8 | Both differences are inside current system drift; no universal benefit is claimed for either enabled shortcut |

### Intel Spatial Reconstruction Ranking

This ranking uses complete-effect median time, not just the reconstruction
dispatch, from the same controlled Intel Arc run: PBR Sponza Decorated,
1920x1080 output, half-resolution AO, eight fixed samples, 120 warm-up frames,
and 480 measured frames with zero incomplete frames per option. The first four
rows are the like-for-like methods in the **Reconstruction Method** dropdown.
The fused row is shown separately because it also removes the standalone final
application stage and remains `(Mutex GI)`.

Here **Legacy Reference** means the canonical generic UVSR bitmask path at that
same workload: AO only, half-resolution tracing, eight samples, radius 3,
thickness 0.5, quadratic spacing, Toroidal Blue Noise, R16F raw and final AO,
legacy guide-aware upsampling, and separate composition. Its optional spatial
denoiser was disabled, but half-resolution output still required the normal
guide-aware reconstruction pass. The comparator measured 2.5543 ms median and
2.7731 ms p95; it is not **Unreconstructed Full Resolution Input**.

| Rank | User-Facing Option | Complete Median | Versus 2.5543 ms Legacy Reference | Plain-English Reading |
| ---: | --- | ---: | ---: | --- |
| 1 | Depth-Guided Reconstruction (Intel) | 2.2467 ms | 12.04% faster | Fastest separate method; uses depth alone, so it needs the strongest visual check at normal discontinuities |
| 2 | Depth-Normal Reconstruction (Intel) | 2.2976 ms | 10.05% faster | Slightly slower than depth-only but protects both geometric depth edges and surface-normal changes |
| 3 | Slope-Aware Reconstruction (Intel) | 2.2993 ms | 9.99% faster | Essentially tied with depth-plus-normal; adjusts depth continuity on slopes and is more scene-dependent |
| 4 | Leakage-Limited Reconstruction (Intel) | 2.3133 ms | 9.44% faster | Slowest retained Intel method and deliberately permits a tiny cross-edge contribution to avoid fully disconnected corners |
| Situational Overall | Fused Depth-Normal Reconstruction And Apply (Intel) (Mutex GI) | 1.9970 ms | 21.82% faster | Fastest total AO pipeline here because it combines reconstruction and application; not a like-for-like spatial-only choice |

The first feature-off run showed much larger slowdowns, but unrelated
composition time moved by the same order and a matched Fixed-8 repeat shifted
from 2.49 to 2.82 ms. Those two outliers are excluded rather than used to
inflate the shortcut scores. The PS4 scalar launch also exposed an interrupted
four-byte shader blob with zero permutations. Regenerating the target produced
a 23,392-byte blob containing both `ACTIVISION_PACKED_GATHER=0` and `=1`
permutations before the successful scalar and packed reruns.

### Universal Performance Ranking

The former Legacy Reference, Exact Fast, and Fast Edges product-preset labels
have been removed. The rows below use the corresponding retained internal or
advanced implementation names; their measurements still apply to those
underlying techniques.

| Rank | Remaining Feature | Score | Short Reason |
| ---: | --- | ---: | --- |
| 1 | Exact fixed-8 profile | 82 | Repeated large AO savings and exact fixed-eight output; GI keeps only the smaller fixed-trace part |
| 2 | Exact Fused Resolve And Apply | 70 | Large exact traffic and dispatch saving on Intel and NVIDIA, but only for reduced-resolution AO without the conflicting final stages |
| 3 | Shared Exact 8/12/16/20/24/48/64 traces | 60 | One exact specialization now covers AO and every GI bounce; earlier fixed-trace gains were modest and adapter-dependent |
| 3 | Final GI 16-bit precision | 55 | Strong 8.39% Intel GI result, but it is numerical and irrelevant when GI is off |
| 5 | Offline Packed Spacetime Noise | 52 | Exact delivery with a 3.48% current win, but earlier runs were less stable and it requires the matching offline-computed sequence |
| 6 | Raw AO 16-bit precision | 45 | Broadly applicable bandwidth reduction, but precision changes and no isolated controlled win is recorded |
| 6 | Final AO 16-bit precision | 45 | Broad AO storage saving with a measured 1.39% gain; still a numerical format choice |
| 8 | Depth-Normal Reconstruction profile | 35 | Large current speedup and GI support, but packed reconstruction changes edge behavior |
| 9 | Intel edge-guided reconstruction methods | 30 | Current 9.44-12.04% gains are real, but every method is algorithmic and scene-sensitive |
| 9 | Fused Depth-Normal Reconstruction And Apply | 30 | Very fast in AO-only mode, but combines algorithmic edges with the fused consumer restriction |
| 9 | Depth Hierarchy 16-bit precision | 30 | Measured 3.88% gain when hierarchy storage matters; radius-3 direct-depth work does not use it |
| 12 | Raw GI Frontier 16-bit precision | 28 | Measured 1.56% GI gain, numerical output, and no AO-only benefit |
| 13 | Cumulative GI 16-bit precision | 25 | Measured 1.77% gain applies only to active multi-bounce GI |
| 20 | Noise Pattern choices | 5 | Primarily quality and convergence controls; timing differences were small and pattern-dependent |
| 25 | Internal Reference benchmark profile | 0 | It is the correctness and timing baseline, not a speed optimization |
| 25 | Generic Fallback / Custom | 0 | It preserves arbitrary combinations but adds no performance technique by itself |

### Situational Performance Ranking

| Rank | Remaining Feature | Score | Short Reason |
| ---: | --- | ---: | --- |
| 1 | Exact fixed-8 profile | 100 | Best exact measured recipe at 23.85% faster for reduced-resolution AO |
| 1 | Exact Fused Resolve And Apply | 100 | Repeated 17.7-21.0% Intel gains when its AO-only contract fits |
| 3 | Fused Depth-Normal Reconstruction And Apply | 98 | Saved 21.82% when approximate edge reconstruction and AO-only fusion are acceptable |
| 4 | Depth-Normal Reconstruction profile | 96 | Near-top speed with one advanced choice and GI-compatible fallback |
| 5 | Final GI 16-bit precision | 92 | Best measured format result at 8.39% for an active GI workload |
| 6 | Intel edge-guided reconstruction methods | 88 | Saved 9.44-12.04% in the current view without requiring fused application |
| 8 | Depth Hierarchy 16-bit precision | 78 | Strong 3.88% format gain when long-radius hierarchy reads are active |
| 9 | Shared Exact 8/12/16/20/24/48/64 traces | 78 | The selected exact count specializes both the first trace and meaningful later-GI work without a second control |
| 11 | Offline Packed Spacetime Noise | 70 | Current 3.48% win is useful with the matching offline-computed sequence |
| 13 | Cumulative GI 16-bit precision | 66 | Measured 1.77% gain for active multi-bounce accumulation |
| 15 | Raw GI Frontier 16-bit precision | 62 | Measured 1.56% bandwidth win for active GI tracing |
| 16 | Raw AO 16-bit precision | 60 | Can reduce broad trace-resolution traffic when its precision is sufficient |
| 17 | Final AO 16-bit precision | 58 | Measured 1.39% gain is modest but applies to a common full-resolution payload |
| 25 | Noise Pattern choices | 25 | They can reduce variance and therefore required samples, but do not directly remove work at a fixed count |
| 27 | Generic Fallback / Custom | 5 | Its only performance value is enabling a hand-tuned combination of other features |
| 29 | Internal Reference benchmark profile | 0 | Baseline selection cannot produce a speedup over itself |

### UI Nonredundancy Ranking

| Rank | Remaining Feature | Score | Short Reason |
| ---: | --- | ---: | --- |
| 1 | Exact Fused Resolve And Apply | 95 | It exposes the largest independent exact optimization and its `(Mutex GI)` boundary |
| 2 | Shared Exact 8/12/16/20/24/48/64 traces | 94 | One dropdown controls the real AO and GI workload and replaces the redundant later-bounce selector |
| 3 | Generic Fallback / Custom | 90 | It honestly identifies combinations that no longer match a named preset |
| 4 | Final GI 16-bit precision | 88 | Unique high-impact GI format control with strong evidence |
| 4 | Noise Pattern choices | 88 | Important quality/convergence decision that is not represented by other controls |
| 6 | Depth Hierarchy 16-bit precision | 80 | Unique long-trace storage decision with measured evidence |
| 7 | Final AO 16-bit precision | 78 | Clear full-resolution output decision with no equivalent control |
| 7 | Offline Packed Spacetime Noise | 78 | It now occupies one distinct choice in the Noise Pattern dropdown instead of duplicating the sample-pattern decision |
| 9 | Exact fixed-8 profile | 72 | It remains one advanced choice, while Low now owns the approachable eight-sample starting point |
| 9 | Depth-Normal Reconstruction profile | 72 | It keeps the approximate edge recipe available without duplicating a product preset |
| 9 | Intel edge-guided reconstruction methods | 72 | One dropdown is justified, with the four methods differentiated by the edge evidence they use |
| 9 | Raw GI Frontier 16-bit precision | 72 | Unique current-bounce GI payload |
| 9 | Cumulative GI 16-bit precision | 72 | Unique multi-bounce accumulation payload |
| 14 | Raw AO 16-bit precision | 70 | Unique trace-resolution AO payload |
| 15 | Fused Depth-Normal Reconstruction And Apply | 55 | Valuable underlying mode, but the depth-normal reconstruction profile already exposes its main recipe |
| 16 | Internal Reference benchmark profile | 0 | It remains essential for measurement but no longer occupies product UI |

### Average Ranking

| Rank | Remaining Feature | Score | Short Reason |
| ---: | --- | ---: | --- |
| 1 | Exact Fused Resolve And Apply | 88 | Exceptional exact AO speed with one clearly stated compatibility limit |
| 2 | Exact fixed-8 profile | 85 | Strong exact speed and situational upside; Low now provides the approachable preset role |
| 3 | Final GI 16-bit precision | 78 | Strong measured GI win and a unique, understandable buffer choice |
| 4 | Shared Exact 8/12/16/20/24/48/64 traces | 77 | Modest exact speed, broad AO/GI composability, and one nonduplicated workload control |
| 5 | Depth-Normal Reconstruction profile | 68 | Large speed and a focused advanced control outweigh algorithmic edge risk, but do not erase it |
| 6 | Offline Packed Spacetime Noise | 67 | Small exact speedup with a clearer, nonduplicated role in the unified Noise Pattern dropdown |
| 7 | Intel edge-guided reconstruction methods | 63 | Strong situational speed; keep the differentiated methods grouped behind one reconstruction dropdown |
| 7 | Depth Hierarchy 16-bit precision | 63 | Good long-radius format win and a unique storage decision |
| 9 | Fused Depth-Normal Reconstruction And Apply | 61 | High AO-only upside but overlaps depth-normal reconstruction and changes the final application |
| 10 | Final AO 16-bit precision | 60 | Modest measured gain with a clear, common buffer scope |
| 11 | Raw AO 16-bit precision | 58 | Plausible broad traffic win, but isolated controlled evidence is missing |
| 12 | Raw GI Frontier 16-bit precision | 54 | Small measured GI win and a unique payload |
| 12 | Cumulative GI 16-bit precision | 54 | Small measured multi-bounce win and a unique payload |
| 14 | Noise Pattern choices | 39 | Important quality UI with little direct fixed-work performance value |
| 15 | Generic Fallback / Custom | 32 | Essential honest state label, not a speed feature |
| 16 | Internal Reference benchmark profile | 0 | Essential benchmark baseline, deliberately absent from product preset UI |

The plain-English production recommendation from this combined score is to use
High as the reference-quality factory preset, Low and Medium as
reduced-resolution performance tiers, and Ultra as the deliberate
high-cost/two-bounce tier. Retain **Exact Fused Resolve And Apply**,
depth-normal reconstruction, the shared exact trace count, packed-edge modes,
and offline-computed packed noise as advanced composable controls. Keep the
strongest 16-bit buffer defaults. Radius clamps, thread groups, conservative
math, feature-off controls, diagnostic floors, the horizon control, and the PS4
pipeline remain removed.

## Expected Performance Impact

This is the ranked forecast for every distinct implemented runtime candidate
family. It uses the user's 2.5-2.7 ms complete AO baseline and the 2.6 ms
midpoint for percentages. Positive values are possible savings; negative values
are possible regressions. These are engineering forecasts, not measured target-
GPU results. Ranges overlap and are non-additive because fixed specialization,
bindings, packed noise, reconstruction, and fusion often remove the same work.
Rank reflects plausible impact and maximum credible upside, not a claim that
each range has a positive midpoint.
Retention still requires manual image acceptance and motion/disocclusion review.
The target Intel adapter now has isolated Release timing with 120 warm-up and
600 complete measured frames per entry; native GRF, spill, SIMD-width, and
occupancy counters remain unavailable.

### Controlled Intel Finalist Ranking

The controlled Intel runs used PBR Sponza Decorated at 1920x1080 on Intel Arc
integrated graphics, with 120 warm-up and 600 measured frames per profile. All
entries listed here completed 600/600 measured frames with zero incomplete
frames. This ranking compares each optimization with its named baseline; rows
with different estimators are diagnostic comparisons, not interchangeable
product candidates.

| Measured Rank | Optimization And Baseline | Median Saving | Expected Production Interpretation |
| ---: | --- | ---: | --- |
| 1 | Exact Fixed 8 plus fused resolve/apply versus Reference | 0.624-0.678 ms; 20.6-22.4% across two controlled runs | Strongest same-estimator candidate; keep for manual image validation and a real performance profile |
| 2 | Exact fused resolve/apply versus Reference | 0.535-0.570 ms; 17.7-18.8% across two controlled runs | Strong pass/traffic removal; keep for a real performance profile |
| 3 | UVSR horizon GTAO control 8 versus Reference | 0.451 ms; 15.4% complete, with 0.455 ms or 77.6% lower trace time | Diagnostic algorithm comparison only; it proves bitmask-estimator overhead but changes the estimator |
| 4 | Final GI `RGBA16_FLOAT` versus `RGBA32_FLOAT` | 0.326 ms; 8.39% | Keep 16-bit final GI storage; strongest paired format result |
| 5 | Exact Fixed 8 versus Reference | 0.056-0.141 ms; 1.9-4.7% across controlled runs | Keep as an optional supporting specialization; the combination with fusion is much more conclusive |
| 6 | Depth hierarchy `R16_FLOAT` versus `R32_FLOAT` | 0.125 ms; 3.88% | Keep 16-bit hierarchy storage |

The historical paired buffer run also measured raw GI and cumulative GI
`RGBA16_FLOAT` at
1.56% and 1.77% faster than their 32-bit controls, and final AO `R16_FLOAT` at
1.39% faster than `R32_FLOAT`. Adaptive-feedback, temporal-GI, and temporal-depth
format deltas changed direction or had worse p95 behavior. Those temporal buffer
controls are no longer exposed, and their roughly two-percent differences are
treated as noise rather than recommendations.

### Forecast Finalist Ranking

This table ranks the largest expected deltas among the implemented finalists
and new direct comparisons. Each row names its own baseline; overlapping rows
are non-additive.

| Rank | Implemented Finalist Or Comparison | Forecast Complete-Effect Saving | Percent Of 2.6 ms | Confidence And Scope |
| ---: | --- | ---: | ---: | --- |
| 1 | Exact Fixed 8 plus fused resolve/apply versus canonical Reference | 0.20-0.60 ms | 8-23% | Strongest same-estimator forecast; controlled Intel measurements saved 20.6-22.4% |
| 2 | Exact fused 2x2 resolve/application versus separate resolve/composition | 0.15-0.45 ms | 6-17% | Removes a full-resolution R16F round trip and dispatch; controlled Intel measurements saved 17.7-18.8% |
| 3 | Fused packed-edge 2x2 versus separate compact resolve/composition | 0.12-0.40 ms | 5-15% | Similar traffic leverage with algorithmic reconstruction and an image gate |
| 4 | Exact fixed-8 trace versus generic eight-sample trace | 0.04-0.20 ms | 2-8% | Controlled Intel results saved 1.9-4.7%; keep mainly as a component of the stronger fused profile |

### Detailed Bitmask Candidate Ranking

| Rank | Implemented Candidate Family | Forecast Complete-Effect Saving | Percent Of 2.6 ms | Confidence And Scope |
| ---: | --- | ---: | ---: | --- |
| 1 | Exact fused 2x2 resolve/application | 0.15-0.45 ms | 6-17% | Removes a 7.9 MiB/frame logical R16F round trip and one dispatch; DXIL comparison drops the separate pair's 50 static loads/four stores to 20 loads/one store |
| 2 | Fused packed-edge 2x2 | 0.12-0.40 ms | 5-15% | Adds cheap algorithmic connectivity to fusion but pays one R8 edge write/read |
| 3 | Packed depth-edge 2x2 | 0.08-0.30 ms | 3-12% | Replaces repeated continuous guide arithmetic with four 2-bit connections |
| 4 | Packed depth-plus-normal 2x2 | 0.06-0.27 ms | 2-10% | Better normal discontinuities with extra trace arithmetic |
| 5 | Packed slope-adjusted 2x2 | 0.05-0.25 ms | 2-10% | Potentially fewer false slope edges; algorithmic and scene-sensitive |
| 6 | Packed controlled-leakage 2x2 | 0.04-0.24 ms | 2-9% | Cheap resolve plus deliberate corner leakage; visual gate is essential |
| 7 | Exact fixed-8 trace | 0.04-0.20 ms | 2-8% forecast | Controlled local trace medians were both 0.101376 ms and complete median regressed 0.926%; retain only as a benchmark/component of the fused finalist |
| 8 | Exact fixed-12 trace | 0.03-0.16 ms | 1-6% | Same-count comparison against generic 12 only |
| 9 | Exact fixed-16 trace | 0.02-0.13 ms | 1-5% | Same-count comparison against generic 16 only |
| 10 | Packed current FAST delivery | 0.01-0.14 ms | <1-5% | DXIL texture-load call sites fall from 14 to 11, or 21.4%, versus fixed-eight while retaining the same FAST values |
| 11 | Exact fixed-20 trace | 0.01-0.11 ms | <1-4% | Same-count comparison; control saving is diluted by twenty reads |
| 12 | Minimal AO/GI bindings and dummy-binding removal | 0.00-0.10 ms | 0-4% | Driver-sensitive; descriptor surface changes but physical depth reads do not |
| 13 | Hierarchy-aware fixed permutation | 0.00 ms at radius 3; 0.01-0.10 ms when hierarchy is already profitable | 0% at target radius | Requested radius 3 selects direct depth; this candidate matters only for eligible radius-8-plus AO |
| 14 | Fixed later-bounce 8 and fixed all-bounce 8 | 0.00 ms for AO only; 0.02-0.25 ms for active GI | 0% for target AO | Preserves GI owner order but has no AO-only consumer |
| 15 | Lazy PSO caching and conditional candidate resources | 0.00-0.03 ms steady state | 0-1% | Primarily startup/memory/off-state protection rather than shader speed |

Supporting rows such as fixed loop termination, repeated squaring, edge-byte
packing, symmetric enforcement, intermediate omission, and barrier removal are
not ranked again because their forecast is already contained in the owning
runtime family. The per-row table entries below retain their individual source,
classification, evidence, quality, and disposition with equal diligence.

### Deferred High-Upside Forecast

| Rank | Deferred Candidate | Forecast Complete-Effect Saving | Percent Of 2.6 ms | Limitation |
| ---: | --- | ---: | ---: | --- |
| 1 | AO-aware deferred lighting plus AO-only `BaseLighting` removal | 0.25-0.80 ms | 10-31% | Exact ordering/resource integration is unimplemented |
| 2 | Half-resolution spatial resolve then cheap full-resolution apply | 0.15-0.55 ms | 6-21% | Receiver identity and quality-gated implementation are pending |
| 3 | Combined spatial plus temporal resolve | 0.00 ms primary; 0.20-0.65 ms with temporal enabled | 0% primary; 8-25% temporal workload | Bounds/history equivalence and PSO are pending |
| 4 | Gather/LDS/thread-coarsened reconstruction | -0.12 to 0.35 ms | -5 to 13% | Register, ordering, cache, and target timing evidence are pending |
| 5 | Conservative mixed-precision trace/filter blocks | -0.12 to 0.30 ms | -5 to 12% | Physical width, conversions, correctness, and image proof are pending |

These values are low-confidence, non-additive design forecasts. Removed XeGTAO
and Activision PS4 evidence is retained in **Removed Feature Disposition** and
the historical timing sections, not ranked as a current finalist.

## Primary Source Index

- Activision 2016 technical report `ATVI-TR-16-01`: [Practical Realtime
  Strategies for Accurate Indirect Occlusion](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf)
- Activision SIGGRAPH 2016 deck: [Practical Real-Time Strategies for Accurate
  Indirect Occlusion](https://blog.selfshadow.com/publications/s2016-shading-course/activision/s2016_pbs_activision_occlusion.pdf)
- Activision expanded 2019 technical memo `ATVI-TR-19-01`: [Practical
  Real-Time Strategies for Accurate Indirect Occlusion](https://www.activision.com/cdn/research/Practical_Real_Time_Strategies_for_Accurate_Indirect_Occlusion_NEW%20VERSION_COLOR.pdf)
- Intel XeGTAO 1.30 at pinned commit
  `a5b1686c7ea37788eeb3576b5be47f7c03db532c`: [README](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/README.md),
  [core shader](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli),
  [constants](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.h),
  and [entry points](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/vaGTAO.hlsl)
- Visibility bitmask: [paper](https://arxiv.org/pdf/2301.11376) and
  [DOI](https://doi.org/10.1007/s00371-022-02703-y)
- SSRT3: [repository](https://github.com/cdrinmatane/SSRT3) and
  [compute shader](https://github.com/cdrinmatane/SSRT3/blob/main/HDRP/Shaders/Resources/SSRTCS.compute)
- Intel ASSAO: [paper](https://www.intel.cn/content/dam/develop/external/us/en/documents/adaptive-screen-space-ambient-occlusion-702913.pdf)
  and [shader](https://github.com/GameTechDev/ASSAO/blob/master/Projects/ASSAO/ASSAO/ASSAO.hlsl)
- AMD CACAO: [manual](https://gpuopen.com/manuals/fidelityfx_sdk/techniques/combined-adaptive-compute-ambient-occlusion/)
  and [shader](https://github.com/GPUOpen-Effects/FidelityFX-CACAO/blob/master/ffx-cacao/src/ffx_cacao.hlsl)
- NVIDIA SAO: [paper](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf)
- Filter-adapted sampling: [importance-sampled FAST](https://jcgt.org/published/0014/01/08/paper.pdf),
  [original FAST](https://arxiv.org/pdf/2310.15364), and
  [EA generator](https://github.com/electronicarts/fastnoise)
- NVIDIA spatiotemporal blue noise: [guidance](https://developer.nvidia.com/blog/rendering-in-real-time-with-spatiotemporal-blue-noise-textures-part-2/)
  and [generator](https://github.com/NVIDIA-RTX/STBN)
- Vendor and API guidance: [Intel Arc/Xe-HPG](https://www.intel.com/content/www/us/en/developer/articles/guide/arc-a-series-gaming-api-developer-optimization.html),
  [AMD RDNA](https://gpuopen.com/learn/rdna-performance-guide/),
  [AMD FP16](https://gpuopen.com/learn/first-steps-implementing-fp16/),
  [AMD SPD](https://gpuopen.com/manuals/fidelityfx_sdk/techniques/single-pass-downsampler/),
  and [Shader Model 6 wave operations](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/hlsl-shader-model-6-0-features-for-direct3d-12)
- Additional primary sources: [NVIDIA deinterleaved texturing](https://d29g4g2dyqv443.cloudfront.net/sites/default/files/akamai/gameworks/samples/DeinterleavedTexturing.pdf)
  and [Line-Sweep AO](https://diglib.eg.org/server/api/core/bitstreams/786e1ab5-c669-48ac-9e99-56a1564edcad/content)

## Measurement and Same-Engine Controls

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M-001 | Per-stage median and p95 records for depth preparation, first trace, each later bounce, spatial denoise, temporal, fused denoise/upsample, required upsample, fused application, and composition | Supplied prompt, Phase 1 | Measurement | Exact instrumentation | Frame-correlated asynchronous queries expose a complete-pipeline envelope, nonoverlapping named stages, and nested bounce 2/3/4 breakdowns | Retain frame identity; compute producer, named-stage-total, unattributed-timer-difference, and envelope distributions from complete frames only | CPU sample buffer and optional export data | No extra dispatch or resource; timestamp queries run only with the visibility effect | Negligible estimated GPU impact - measurement/reporting only; it enables trustworthy ranking rather than AO speed. | 120-frame warm-up; at least 240 complete frames; compare named-stage total and unattributed timer difference with the complete envelope | The controlled local sequence completed 600/600 frames for each of nine profiles with zero incomplete frames and exported every applicable named-stage median/p95; controlled Intel target timing is not yet measured. | No image effect | Implemented and retained | `VisibilityBenchmarkStage` includes depth, first trace, aggregate later trace, bounce 2/3/4, spatial denoise, temporal, fused spatial denoise/upsample, required upsample, fused resolve/apply, composition, and envelope. No stage is named `Other` and unrelated stages are not combined. Breakdown-only bounces are excluded from the summed mask; frame-correlated statistics prevent summing percentiles from different frames. |
| M-002 | Constant-output trace floor | Supplied prompt, Phase 1 | Trace | Diagnostic | No equivalent | Same dispatch and output UAV; write constant AO without depth reads | One lazily created PSO; no texture | None; reference PSO selected | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95 and UAV traffic versus reference | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Intentionally invalid image | Implemented as benchmark-only control | Isolates dispatch and output-write floor without claiming product speed. Implementation evidence: `TRACE_DIAGNOSTIC=1`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-003 | Depth-read-only trace floor | Supplied prompt, Phase 1 | Trace | Diagnostic | No equivalent | Preserve expected sample coordinates and depth reads; omit interval and bitmask math | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95, sampler and cache counters | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Intentionally invalid image | Implemented as benchmark-only control | Separates texture/cache cost from visibility ALU. Implementation evidence: `TRACE_DIAGNOSTIC=2`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-004 | Bitmask-math-only trace floor | Supplied prompt, Phase 1 | Trace | Diagnostic | CPU unit fixtures only | Deterministic local intervals with no texture reads | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95, instruction and register comparison | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Intentionally invalid image | Implemented as benchmark-only control | Estimates interval, range-mask, popcount, and union ALU cost. Implementation evidence: `TRACE_DIAGNOSTIC=3`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-005 | Temporal copy floor | Supplied prompt, Phase 1 | Temporal | Diagnostic | No equivalent | Copy current AO to history/output without reprojection, bounds, or validation | One lazy PSO and normal temporal outputs | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95 and logical read count | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Intentionally omits temporal stability | Implemented as benchmark-only control | Separates history-write cost from temporal algorithm cost. Implementation evidence: `TEMPORAL_COPY_DIAGNOSTIC`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-006 | Bilinear upsample floor | Supplied prompt, Phase 1 | Reconstruction | Diagnostic | Compact joint bilateral always runs at reduced resolution | Bilinear-only reconstruction pipeline | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95 and full-resolution traffic | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Expected edge leakage; diagnostic only | Implemented as benchmark-only control | Establishes unavoidable full-resolution read/write floor. Implementation evidence: `RECONSTRUCTION_DIAGNOSTIC=1`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-007 | Nearest upsample floor | Supplied prompt, Reconstruction list | Reconstruction | Diagnostic | No equivalent | Nearest reduced AO lookup and application | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95 and image capture | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Blocky diagnostic image | Implemented as benchmark-only control | Establishes minimum source-read path and diagnoses filter cost. Implementation evidence: `RECONSTRUCTION_DIAGNOSTIC=2`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-008 | No full-resolution AO intermediate | Supplied prompt, Phase 1 and fusion section | Reconstruction/application | Exact when half rounding is preserved | Current reduced AO is resolved to full-resolution `R16_FLOAT` before composition | Fused current resolve and application; explicitly half-round resolved AO for strict comparison | One fused PSO; removes rather than adds final AO texture | None | High estimate - removes one 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; it targets filtering and application work that was previously not separated and overlaps other fusion rows. | Complete-effect median/p95, logical traffic, exact image comparison | On the controlled local adapter, fusion saved 6.250% median and 17.510% p95; Fixed 8 plus fusion saved 7.870% and 19.066%. Both completed 600/600 frames with zero incomplete frames. | Final captures were clean; exact same-phase GPU equivalence remains pending | Implemented as optional experiment | Directly attacks measured filtering and composition buckets while preserving math. Implementation evidence: `src/screen_space_visibility_fused_apply_cs.hlsl` and `ExactFusedResolveApply`. It is CPU-selected and optional; Reference binds, allocates, and dispatches none of its candidate-only work. Keep both fusion profiles for product validation; target Xe-LPG timing remains unclaimed. |
| M-009 | Composition bypass and isolated composition | Supplied prompt, Phase 1 | Application | Diagnostic | Composition now has its own named HUD and benchmark stage | Bypass and isolated full-screen composition controls | Up to two lazy PSOs | None | Diagnostic only - expected 0 ms product improvement; value is isolating trace/filter/composition cost or a matched horizon reference. | Median/p95 and output comparison | Benchmark control and explicit composition attribution are implemented; controlled Intel target timing is not yet measured. | Bypass intentionally invalid; isolated path preserves output | Implemented as benchmark-only control | Separates AO production from lighting application. Implementation evidence: `DiagnosticCompositionOnly`/`DiagnosticCompositionBypass` in `src/visibility_performance_plan.cpp`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources, and the control makes no product-quality claim. |
| M-010 | Same-engine XeGTAO High source-port control | Supplied prompt; [pinned XeGTAO README](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/README.md) | Full AO control | Algorithmic benchmark control | Three full-resolution High profiles now select the port directly | LUT/mixed precision closest port, inline-Hilbert/mixed precision, and LUT/FP32 controls under the same camera, timer, application, and export system | Five-mip R16F depth, one R16F working AO, one R8 edge plane, and an optional 8 KiB R16_UINT Hilbert LUT | None; Reference creates no XeGTAO PSO, binding, resource, or pass | Forecast -0.40 to 0.80 ms versus the 2.6 ms UVSR baseline, with low comparability confidence; this is a different estimator | Report depth prefilter, main trace, spatial denoise, composition, complete median/p95, exact profile key, and image review | Direct pinned High path, resource contract, 4096-entry Hilbert fixtures, and 40 strict DXC permutations are implemented. All three profiles completed 600/600 controlled local frames with zero incomplete frames: LUT mixed 0.413696/0.454656 ms, inline mixed 0.419840/0.454656 ms, and LUT FP32 0.349184/0.384000 ms. The independent precision repeat confirmed FP32's local lead. | Clean final captures; different estimator and adapter prevent bit-identical output | Implemented as optional experiment | The port retains five depth mips, 3x3x2 High work, Hilbert/R2, horizon math/falloffs, packed edges, and one sharp denoise. Disclosed adapters transform world normals, preserve the 96-byte constants prefix in a 176-byte buffer, separate logical/padded extents, use R16F AO with emulated R8 rounding, fix the noise phase without matching TAA, omit bent normals/generated normals, and add safety guards. Prefer LUT/FP32 locally; retain all three for quality/reference testing and do not infer Xe-LPG behavior from NVIDIA. |
| M-011 | Activision PS4 GTAO approximation | Supplied prompt; [Activision 2016 deck, slides 54-104](https://blog.selfshadow.com/publications/s2016-shading-course/activision/s2016_pbs_activision_occlusion.pdf); [expanded 2019 memo](https://www.activision.com/cdn/research/Practical_Real_Time_Strategies_for_Accurate_Indirect_Occlusion_NEW%20VERSION_COLOR.pdf) | Full AO control | Algorithmic benchmark control | Separate scalar-filter and packed-gather profiles run a coupled approximation | Eight-tap linear horizon trace with published 4x4-by-6 schedule, closest-valid half-depth prep, 4x4 spatial pass, temporal accumulation, required upsample, and normal UVSR composition | Conditional prepared depth, spatial AO, AO/depth history, and motion input | None; Reference selects none of these resources or passes | Expected 0 ms exact-product saving; packed gather versus scalar forecast -0.08 to 0.25 ms | Report every named stage separately; compare scalar/gather output, motion, thin geometry, far fade, and complete median/p95 | Both variants completed 600/600 controlled local frames with zero incomplete frames. Scalar measured 0.306176/0.340019 ms and packed 0.302080/0.313344 ms; packed saved 1.338% median and 7.845% p95 versus scalar. | Clean final captures; source-informed approximation because shipping code/constants are unpublished | Implemented as optional experiment | The repair follows the 2016 console workload and restores the depth-trace-spatial-temporal-upsample order, eight total linear taps, six distinct bidirectional slice axes over `[0, pi)`, signed horizon sample domain, outer-quarter fade, derivative-aware 4x4 filtering, and motion/depth history rejection. The closest valid linear depth guides each 2x2 footprint, and one direction per frame converges through the six-phase history after warm-up. Both variants were slower than Reference locally, so retain them as faithful algorithm comparisons rather than local performance profiles; prefer packed when using the comparison. |
| M-012 | Historical one-at-a-time benchmark actions | Supplied prompt, Verification drawer | Measurement | Exact instrumentation | Earlier builds supplied sequential Reference/current, fixed-count, noise, reconstruction, math, new-candidate, precision, and all-implemented runners | Retain only isolated Run Current; a person can select any profile explicitly without sequence state changing settings behind the UI | No remaining sequence state; isolated result files only | None | 0 ms product improvement; orchestration never changed renderer GPU work | Historical entry order/settings restoration and completed export evidence | Fixed 5/5, noise 6/6, reconstruction 12/12, math 2/2, and all-implemented 58/58 smokes completed before removal. Later candidate and precision sequences completed 600/600 frames per entry with zero incomplete frames. | No image effect beyond each selected candidate | Removed after evidence collection | The completed evidence remains valid, but multi-run actions duplicated explicit profile selection and made readiness, cancellation, and settings restoration harder to understand. |
| M-013 | Historical benchmark result export with adapter, clocks, settings, and shader key | Supplied prompt, Verification drawer | Measurement | Exact instrumentation | Earlier builds automatically produced schema-v2 JSON, raw per-frame CSV, and the last measured frame | Remove file export and keep only the latest in-memory timing summary in Statistics | No remaining export files or capture staging | None | Negligible GPU change; the removed path ran after measurement | Confirm no serializer, capture staging, export command line, or export UI remains; preserve timer summaries | The controlled local sequences previously exported matching JSON, raw CSV, and final-frame BMP sets for every entry. That evidence remains historical and is not regenerated by the current build. | No image effect | Removed after evidence collection | The Visibility benchmark still locks the controlled camera, collects complete frame-correlated timers, reports progress, and retains latest medians and p95 values in memory. JSON/CSV/BMP serialization, measured-frame capture, artifact naming and hashing, re-export, the folder action, and `--benchmark-output` are removed. |
| M-014 | Headless single-profile benchmark command line | Current implementation; user smoke workflow | Measurement | Exact instrumentation | Renderer accepts one verification or implementation profile, benchmark request, warm-up/count, and auto-close options | Keep validation console-only with nonzero exit status so automated runs cannot block on a modal dialog | CPU arguments and in-memory timing summary only | None | Negligible estimated GPU impact - launch control only; it makes repeatable isolated smoke runs practical | Positive run plus invalid profile/count/unavailable-profile process-exit tests | Positive controlled single-profile runs completed every requested frame. Invalid counts, profile names, and unavailable profiles returned exit code 1 with stderr and no modal dialog. Historical sequence evidence remains in M-012. | No image effect beyond the selected profile | Implemented and retained | All arguments are parsed before device creation; invalid or unavailable values write to standard error and return nonzero without a message box. `--benchmark-output`, `--benchmark-sequence`, and every multi-run action are removed. |

## Fast Math and Validation

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | Shared depth-linearization denominator | Supplied prompt; [SAO p.3, Eq.2](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=3) | Trace/filter | Exact or Numerical if operation moves | Denominator is recomputed across linear-depth and position helpers | Exact-fast helper returns depth and reconstruction factors together | None | None through separate PSO | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL instruction count; reference fixtures and image diff | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact tolerance test pending | Implemented as optional experiment | Amortizes division; moving it earlier can change rounding, so bitwise claims require evidence. Implementation evidence: `FILTER_EXACT_FAST_MATH` shared reconstruction. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| F-002 | Precomputed NDC-to-view constants | Supplied prompt; [pinned XeGTAO shader `XeGTAO_ComputeViewspacePosition`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Trace/filter | Exact | XeGTAO source port uses precomputed perspective multipliers/addends; generic bitmask uses its existing reconstruction | Retain the pinned Xe path and add a bitmask specialization only after position-equivalence proof with orthographic fallback | Xe constant fields only while selected | None in Reference | Included in M-010 for Xe; low-to-medium potential remains for generic bitmask | DXIL and image/position fixtures | Implemented in XeGTAO; generic bitmask transfer unimplemented | Exact in source port; generic tolerance proof pending | Blocked by correctness | The source port demonstrates the perspective form but rejects orthographic projection to Reference. Applying it to the product trace remains a separate exactness task rather than an unavailable Xe feature. |
| F-003 | Linear-depth multiply-add view reconstruction | Supplied prompt; [SAO p.3, Eqs.3-4](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=3) | Trace/filter | Exact or Numerical | General reconstruction helper | Specialized perspective reconstruction from linear depth | None beyond F-002 constants | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Position fixture grid, DXIL, image diff | Primary sources use this bandwidth/ALU reduction | Pending | Blocked by correctness | Valid only when projection convention and half-pixel mapping match exactly. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-004 | Analytic projected pixel radius | Supplied prompt; [Activision memo p.3](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf#page=3) | Trace | Exact if endpoint result matches | Projects two world-space endpoints | Perspective analytic radius permutation with generic fallback | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Radius fixtures across depth/FOV/aspect; DXIL; image diff | Source supports inverse-depth screen radius | Pending | Blocked by correctness | Removes endpoint projection while preserving radius only after equivalence proof. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-005 | Maximum projected-radius cache clamp | Supplied prompt; [Activision memo p.3](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf#page=3) | Trace | Algorithmic | Reference projected radius is unlimited | Curated fixed-8 AO profiles clamp each projected side to 32, 64, or 128 pixels before sampling | None | None through separate CPU-selected PSOs | Forecast savings: 32 px -0.50 to 0.30 ms; 64 px -0.20 to 0.22 ms; 128 px -0.12 to 0.14 ms. Same read count, so locality and scene coverage dominate. | Radius sweep, cache/sampler counters, thin/far occluder captures | All three packaged profiles completed the comprehensive Intel smokes and directionally regressed versus Fixed 8: 32 px by 0.070-0.466 ms, 64 px by 0.085-0.119 ms, and 128 px by 0.067-0.125 ms. Controlled timing and separate quality acceptance remain pending. | May omit distant occlusion; separate visual acceptance required | Implemented as optional experiment | `AlgorithmicProjectedRadiusClamp32/64/128` select compile-time `MAX_PROJECTED_RADIUS_PIXELS` wrappers and distinct profile keys. This is explicitly algorithmic, never folded into Reference, and uses no auxiliary texture or pass while off. |
| F-006 | Native `sincos` | Supplied prompt; Intel Arc optimization guide | Trace | Exact or Numerical by generated implementation | Direction construction uses separate operations/path-dependent math | Exact-fast direction permutation using `sincos` only if DXIL/ISA improves | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical depth reads; scene dependence and overlap are high. | DXIL/ISA, direction ULP fixtures, median/p95 | Source-level benefit is unproven pending the generated-code comparison | Pending | Not applicable | The compiler may already combine the operations, while a source rewrite may alter sector-boundary rounding. Until F-030 records DXIL for both forms, compiler subsumption is not established. A separate shader permutation could preserve zero Reference cost, so this disposition is provisional evidence debt rather than a demonstrated lack of applicability. |
| F-007 | Precomputed slice-direction lookup | Supplied prompt; Activision fixed schedules | Trace | Numerical unless table reproduces values | Runtime direction evaluation | Constant table indexed by fixed scheduler phase | Small constant table | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | DXIL, ULP and mask disagreement, timing | Pending | Boundary quality pending | Blocked by correctness | Removes transcendentals but table rounding can alter interval sectors. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-008 | Direction packed with noise | Supplied prompt | Trace/noise | Numerical or Algorithmic | Direction is derived after independent scheduler reads | Packed direction/vector-noise profile | Conditional packed/vector noise texture | None; texture absent and reference PSO selected | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Logical loads, cache, mask/image comparison | Pending | Correlation/spectral test required | Blocked by correctness | Can remove math/load but must not collapse independent dimensions. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-009 | XeGTAO/Lagarde fast `acos` | Supplied prompt; [Activision deck slide 63](https://blog.selfshadow.com/publications/s2016-shading-course/activision/s2016_pbs_activision_occlusion.pdf#page=63); [pinned XeGTAO `FastACos`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Trace | Numerical | `VisibilityFastAcos` exists for the projected-angle path and the XeGTAO port retains its signed-input approximation | Retain explicit Reference/Approximate selection where applicable | None | None | Expected 0 ms incremental - already active in relevant comparison paths | DXIL, angle ULP, interval-mask disagreement, image/timing | Implemented in both relevant shader families | Quality remains algorithm-specific | Already equivalent | Signed input is essential in the Xe path; clamping to nonnegative values produces over-occlusion. Do not add a duplicate control unless profiling finds an uncovered call site. |
| F-010 | Approximate `atan2` | Supplied prompt | Trace | Numerical | Native angle path where required | Aggressive approximate-math permutation | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Angular and mask disagreement at axes/endpoints; timing | No primary AO source established a safe UVSR approximation | High boundary risk | Blocked by correctness | Isolatable, but sector discontinuities demand exhaustive endpoint tests. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-011 | XeGTAO-style fast square root | Supplied prompt; [Activision deck slide 63](https://blog.selfshadow.com/publications/s2016-shading-course/activision/s2016_pbs_activision_occlusion.pdf#page=63); [pinned XeGTAO `FastSqrt`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Trace | Numerical | XeGTAO source port uses the pinned approximation; generic bitmask fast `acos` still uses native `sqrt` | Keep the source-port math; expose a bitmask variant only after sector-boundary proof | None | None | Included in M-010 for Xe; low-to-medium potential remains for generic bitmask | DXIL, relative error, mask disagreement, median/p95 | Implemented in XeGTAO; no generic-bitmask target result | Source-defined in Xe; bitmask sector risk pending | Blocked by correctness | The complete port clears the architecture gap only for its own estimator. Approximation error can still cross a whole UVSR half-sector boundary, so generic transfer remains gated. |
| F-012 | Reuse one inverse length | Supplied prompt; Activision deck slides 61-64 | Trace | Exact | Direction/back-direction and slice helpers normalize related vectors repeatedly | Trusted normalized helper overloads and shared `rsqrt` | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | DXIL; unit-vector assertions; mask/AO/GI fixtures | Redundant normalization confirmed by source inspection | Exact tests pending | Blocked by correctness | High-value exact ALU reduction if producer invariants are proven. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-013 | Trusted unit-normal inputs | Supplied prompt; Intel Arc optimization guide | Trace/filter | Exact with validated producer contract | `SafeNormalize` repeats normalization on known unit inputs | Release-only trusted-unit permutation; debug assertions retain checks | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Producer-format audit, assertions, DXIL, image diff | Some bases are immediately normalized then normalized again | Pending | Blocked by correctness | Remove only provably redundant normalization; packed normals still need decode normalization where required. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-014 | Repeated squaring for `pow(x,16)` | Supplied prompt | Reconstruction | Exact apart from reassociation | Joint weight uses `pow(normalDot,16)` | Four multiplications in exact-fast filter/fused PSO | None | None | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL, weight ULP, image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Expected visually exact | Implemented as optional experiment | Fixed integer exponent has an exact algebraic expansion and Intel documents low `pow` issue rate. Implementation evidence: repeated-squaring normal weights. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| F-015 | Fixed quadratic radial distribution | Supplied prompt; [pinned XeGTAO README](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/README.md) | Trace | Exact | Current shader already selects `x*x` when exponent is 2.0, behind runtime comparisons | Compile exponent 2 directly in fixed profile | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL branch/instruction comparison; sample-position fixtures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Specialization removes comparisons and general `pow` fallback from target PSO. Implementation evidence: `FIXED_RADIAL_EXPONENT_TWO`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| F-016 | Combined single exponential | Supplied prompt | Reconstruction | Exact apart from reassociation | Compact filter multiplies two `exp` terms | Sum exponents and evaluate once | None | None | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL, ULP/image diff, timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | `exp(a)*exp(b)=exp(a+b)` but rounding/underflow differ slightly. Implementation evidence: the single-`exp` depth weight. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| F-017 | `exp2` weight approximation | Supplied prompt | Reconstruction | Numerical | Native `exp` weights | Approximate-math filter permutation | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Weight error, edge suite, image/timing | Pending | Pending | Blocked by correctness | Conversion and target SFU behavior must be measured; not assumed faster. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-018 | Packed-edge weighting without exponentials | Supplied prompt; XeGTAO/ASSAO/CACAO packed-edge filters | Reconstruction | Algorithmic | Continuous depth/normal exponential weights | Packed-edge filter permutations | Packed edges or packed AO/edge resource | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Timing, edge fixture suite, temporal stability, captures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Expected controlled leakage/quantization | Implemented as optional experiment | Changes reconstruction kernel and guide response. Implementation evidence: CPU-selected packed-edge profiles. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| F-019 | Precomputed Gaussian spatial weights | Supplied prompt | Reconstruction | Numerical | Gaussian filter recomputes radius-canceling weights per tap | Constant 16-tap FP32 table for fixed disk offsets in the conservative filter profile | Small shader constant table | None | Low-to-medium estimate - approximately 0.02-0.18 ms for the complete conservative filter family, heavily overlapping invariant-hoisting rows. | FP32 fixture, DXIL, image/timing | Candidate implemented; controlled Intel target timing is not yet measured. | Offline FP32 constants can differ from runtime `exp` by rounding/reassociation and are therefore not claimed bit-exact. | Implemented as optional experiment | `FILTER_EXACT_FAST_MATH` selects the 16-value table and related invariant hoists in `screen_space_visibility_filter_exact_cs.hlsl`; the profile is intentionally classified Conservative Numerical rather than mixed precision or exact. Reference selects the runtime-weight shader and has no candidate cost. |
| F-020 | Hoist projection invariants | Supplied prompt; Activision and XeGTAO | Trace | Exact | Projection/slice constants are recomputed inside loops/helpers | Exact-fast fixed trace PSOs | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | DXIL loop-body inspection and image fixtures | Several invariants identified in current shader | Exact tests pending | Already equivalent | Primary sources move radius/falloff/frame invariants outside radial loop. Code evidence: `src/screen_space_visibility_cs.hlsl`, `src/screen_space_visibility_filter_cs.hlsl`, and `src/shaders.cfg`. The current path already performs the work, so no extra option or inactive-path cost is required. |
| F-021 | Hoist filter invariants | Supplied prompt | Reconstruction | Exact | Center reconstruction and scale terms repeat per tap | Exact-fast filter/fused PSOs | None | None | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL and image diff | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Preserves tap set and weights while reducing ALU. Implementation evidence: the exact-fast filter wrapper. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| F-022 | Precompute fixed radial positions | Supplied prompt; ASSAO sample table; XeGTAO fixed presets | Trace | Exact for fixed scheduler/count | Progressive sample calculation runs dynamically | Fixed-count tables or compile-time constants preserving jittered strata | Constant table if compiler does not fold | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Compare every stratum/order/coordinate for all rotations | Pending | Exact sample identity required | Blocked by correctness | Valid only if independent within-stratum jitter and nested prefix are unchanged. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-023 | Precompute fixed mip choices | Supplied prompt; [SAO p.3](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=3); ASSAO shader | Trace | Exact when existing thresholds map identically | Runtime distance thresholds choose one of five mips | Fixed per-tap choice in fixed-radius/profile permutations | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Exhaustive tap-distance/mip fixture, DXIL, timing | Current choices are hard-coded but still selected per tap | Pending | Not applicable | Table is exact only for fixed radius/projection assumptions encoded in key. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| F-024 | Trusted-input release checks | Supplied prompt; Intel Arc optimization guide | Trace | Exact under host contract | Repeated finite/range checks remain in hot path | Trusted Release PSO; reference and debug-assert PSOs retained | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Invalid-input debug tests, DXIL, target timing | Some hierarchy and reconstructed values are already producer-clamped | No valid-input image change | Blocked by correctness | Remove only checks made redundant by resource creation and validated constants. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-025 | Selective finite checks | Supplied prompt | Trace/filter | Exact under defined finite-input contract | Broad finite checks | Keep checks only at external/untrusted boundaries | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | NaN/Inf injection tests and DXIL | Pending | No valid-input change; fault containment changes | Blocked by correctness | Balances robustness and hot-loop cost without silently trusting all inputs. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| F-026 | Eliminate unused GI math/resources from AO-only shader | Supplied prompt; visibility paper p.5 | Trace | Exact | Reference AO compiles GI math out but retains a broad dummy-compatible CPU layout | Fixed and advanced AO-only PSOs compile `ENABLE_GI=0` and build only the reflected AO/depth/normal/noise/output bindings | Removes GI/source bindings; no added resource | None | Low estimate: part of the 0.00-0.10 ms minimal-binding family and non-additive with fixed/fused rows. | SRV/UAV counts, DXIL reflection, image/timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Candidate layout construction removes the CPU-side dummy GI/source surface as well as shader math. Reference remains the canonical broad layout with no candidate branch or PSO creation. |
| F-027 | Eliminate unused AO math/resources from GI-only shader | Supplied prompt | Trace | Exact | Reference GI compiles AO math out but retains a broad common layout | Fixed GI-only and later-bounce PSOs compile `ENABLE_AO=0` and bind their reflected GI/source/frontier outputs only | Removes AO bindings | None | Expected 0 ms for primary AO-only; part of the minimal-binding potential for GI/multi-bounce runs. | SRV/UAV and DXIL reflection; GI owner fixture and timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Consumer-specific advanced layouts keep unused AO inputs/outputs out of GI candidates without forcing AO formats onto GI. Reference is unchanged. |
| F-028 | Compile-time AO-only application variants | Supplied prompt | Application | Exact | One broad composition layout/path | AO-only fused application PSO with exact equations; the separate composition path remains the broad Reference PSO | No extra resource; fused mode removes unused inputs and the AO intermediate | None | High estimate - the fused variant removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | DXIL, binding counts, exact capture | The AO-only fused candidate is implemented; a separately specialized non-fused composite is not. Controlled Intel target timing is not yet measured. | Exact semantics required | Implemented as optional experiment | The implemented fused PSO avoids runtime consumer decisions and redundant inputs. A separate AO-only composite would require another layout/PSO and remains a disclosed deferred candidate; it could preserve zero Reference cost but has no generated-code or timing evidence yet. |
| F-029 | Compile-time source activity | Supplied prompt, GI list | GI trace | Exact | Uniform source-potential checks remain | GI permutation keyed by known active source class | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | DXIL, inactive/active source fixtures | No uniformly known active source class exists in the current GI workload | Exact only when uniform inactivity is proven | Not applicable | Source potential depends on per-pixel material/radiance, emissive state, and bounce data. AO-only already compiles GI out; an active GI profile cannot remove the per-pixel gate without restricting inputs or changing behavior. A separate PSO therefore has no exact compile-time fact to specialize on under the current scene contract. |
| F-030 | Generated-code validation for every source simplification | Supplied prompt | Build/verification | Exact instrumentation | Shader compilation exists; reproducible core-profile comparison now exists | Record DXIL instruction/register data per curated key | CPU report only | None | Negligible estimated GPU impact - reporting/validation only, with no extra dispatch when inactive; it enables trustworthy ranking. | Automated disassembly diff | [`visibility-dxil-evidence.md`](visibility-dxil-evidence.md) records a representative 18-variant core set; the script reproduces CSV, disassemblies, and the report. It does not cover every selectable profile. | No image effect | Blocked by architecture | The report proves several changes survive DXC: Packed FAST reduces static texture-load call sites 14 to 11, exact compact algebra reduces static IR 6.4% and unary sites 34%, and exact fusion reduces the compared separate pair from 50 loads/four stores to 20 loads/one store. It omits the dedicated Xe and PS4 passes, fixed GI/later-bounce permutations, most packed-edge variants, group shapes, radius clamps, and diagnostics. DXIL also cannot expose physical Intel registers, spills, SIMD width, or occupancy. The requested every-simplification/physical-retention gate is therefore incomplete, not impossible or falsely claimed exhaustive. |

## Precision and Formats

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P-001 | Reference FP32 trace arithmetic | Supplied prompt | Trace | Exact reference | Default arithmetic is FP32 | Preserve unchanged Reference PSO | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Reference image and DXIL identity | Present | Canonical reference | Already equivalent | This is the required fallback and comparison basis |
| P-002 | `min16float` mixed trace arithmetic | Supplied prompt; [Intel Arc guide](https://www.intel.com/content/www/us/en/developer/articles/guide/arc-a-series-gaming-api-developer-optimization.html); [AMD FP16 guide](https://gpuopen.com/learn/first-steps-implementing-fp16/) | Trace | Numerical | UVSR bitmask remains FP32; XeGTAO now has source-matched mixed and FP32 profiles | Keep the generic bitmask mixed-precision proposal separate from the implemented XeGTAO precision matrix | None | None | Generic bitmask forecast remains -0.12 to 0.30 ms; XeGTAO mixed versus FP32 is separately forecast -0.15 to 0.35 ms | D3D feature query, DXIL/ISA, register/occupancy, mask/image diff, timing | Both XeGTAO precision profiles completed the main controlled run and independent 600-frame repeat with zero incomplete frames. FP32 was 11.2-15.6% faster by median locally; generic bitmask mixed precision and target physical-width benefit remain unproven. | Clean XeGTAO captures; pending for generic bitmask | Blocked by correctness | Minimum precision may remain FP32 and target ISA evidence is required. The implemented XeGTAO path supplies a valid same-algorithm precision comparison and favors FP32 on this NVIDIA stack, but it does not prove that UVSR's sector-quantized finite-interval bitmask is safe in half precision or predict Xe-LPG behavior. |
| P-003 | Native FP16 trace arithmetic | Supplied prompt; [D3D12 native 16-bit query](https://learn.microsoft.com/en-us/windows/win32/api/d3d12/ns-d3d12-d3d12_feature_data_d3d12_options4) | Trace | Numerical | FP32 | Experimental native-16 PSO gated by `Native16BitShaderOpsSupported` | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Feature query, DXIL/ISA, masks, registers, timing | XeGTAO reports 5-20% on tested GPUs but also regressions; not UVSR target evidence | Pending | Blocked by correctness | Optional only; conversion overhead and precision-sensitive sectors may erase the gain. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-004 | Mixed-precision view depth and position | Supplied prompt | Trace/reconstruction | Numerical | FP32 | Aggressive mixed-precision PSO | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Position relative error over depth range; mask and edge disagreement | No source supports safety for UVSR intervals | High risk at far depth | Blocked by correctness | Keep FP32 in conservative profile; isolate only as aggressive experiment. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-005 | Mixed-precision front/back interval and CDF math | Supplied prompt | Trace | Numerical | FP32 | Aggressive interval PSO | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Exhaustive sector-boundary/endpoint fixtures and image diff | No validation yet | Very high discontinuity risk | Blocked by correctness | Half errors can change whole bits, so scalar AO error is insufficient evidence. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-006 | Mixed-precision sector quantization | Supplied prompt | Trace | Numerical | FP32 angle-to-sector quantization | Aggressive quantization PSO | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Mask disagreement at every half-sector threshold | No validation yet | Very high discontinuity risk | Blocked by correctness | Preserve the paper's half-sector rounding rule and compare masks exactly. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-007 | FP16 GI radiance accumulation | Supplied prompt | GI trace | Numerical | FP32 slice and accumulated radiance | Experimental GI precision PSO | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | HDR absolute/relative error, bounce-energy sweep, temporal captures | Not tested | Accumulated and multi-bounce error risk | Blocked by correctness | Conservative profile must retain FP32; FP16 is optional only. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-008 | R32F prepared linear depth | Supplied prompt; SAO p.3 | Depth preparation | Numerical relative to direct device-depth reconstruction | Device depth is sampled directly; temporal history is R32F | Conditional R32F linear-depth base/pyramid | Conditional depth texture | None; no allocation or pass | Medium bandwidth/occupancy potential - narrower data can reduce traffic/live state, with correctness gates. | Prep plus trace median/p95, depth ULP and image diff | Pending | High-precision prepared-depth reference | Blocked by architecture | Provides a reference for separating hierarchy behavior from R16 quantization. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| P-009 | R16F prepared linear depth | Supplied prompt; [pinned XeGTAO README](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/README.md); CACAO manual | Depth preparation | Numerical | Generic radius-8-plus hierarchy and the XeGTAO five-mip hierarchy use R16F | Retain only for consumers that validate the quantization; the Activision approximation instead uses R32_UINT storage containing masked FP32 depth bits plus a two-bit guide offset | Conditional R16F texture | None | Expected 0 ms incremental inside the source port; its full prep cost is included in M-010 | Total prep plus trace, depth error, and image comparison | All three Xe source-port profiles completed 600/600 controlled frames with clean captures; depth-stage medians were 0.020480, 0.020480, and 0.019456 ms. Target timing remains pending. | Source-port depth bias and precision follow the pinned path | Implemented and retained | The XeGTAO profile always selects its R16F hierarchy and FP16 depth bias. Reference radius 3 still allocates and dispatches no hierarchy; the PS4 guide is a distinct packed-FP32 resource rather than an R16F or R32F plane. |
| P-010 | Raw R8 AO for naturally bounded estimators | Supplied prompt; XeGTAO/ASSAO/CACAO | Trace | Numerical | Raw AO is R16F | Estimator-gated R8 raw-output PSO | Conditional R8 AO texture replacing R16F | None | Medium bandwidth estimate - R8 halves plane bytes versus R16F, only if range and validation remain correct. | Range proof, quantization/image/temporal error, bandwidth/timing | Uniform Solid Angle and Cosine-Weighted Solid Angle can exceed one | Pending only for a mathematically bounded estimator | Blocked by architecture | Must be impossible to select for unbounded estimators; never clamp product data silently. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| P-011 | Raw R8 AO for unbounded estimators | Supplied prompt, estimator constraint | Trace | Numerical with destructive clamp | Raw R16F preserves values above one | No control | None | None | Medium bandwidth/occupancy potential - narrower data can reduce traffic/live state, with correctness gates. | Analytic estimator range and stress fixtures | Values above one are explicitly possible | Fails range preservation | Blocked by correctness | A checkbox cannot make a lossy bounded encoding correct; Off would be free but the enabled result violates the output contract |
| P-012 | Packed R16_UINT AO count plus four edges | Supplied prompt; visibility paper p.5; XeGTAO packed edges | Trace/reconstruction | Exact only for popcount-only estimators; Algorithmic edges | R16F AO, no edge metadata | Estimator-gated packed PSO: 6-bit count, 8-bit edges, 2 spare bits | Replaces AO resource at same two bytes/pixel; no separate edge texture | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | All 33 counts, all 256 edge bytes, AO decode, image/timing | Popcount AO is exactly `1-countbits(mask)/32`; cosine-weighted mass is continuous | Exact for compatible AO; edge quality pending | Blocked by architecture | Clean lossless AO case without extra storage, but must be excluded for cosine-weighted estimator. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| P-013 | R8 temporal AO with range-preserving encoding | Supplied prompt | Temporal | Numerical unless encoding is lossless for active estimator/history | Temporal AO is R16F | Estimator-gated encoded R8 history | Conditional R8 history replaces R16F | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | History range, long-run drift, disocclusion and image tests | No validated encoding exists | Pending | Blocked by correctness | Raw estimator values and EMA state can exceed one; validation precedes exposure. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-014 | No final resolved AO intermediate | Supplied prompt | Reconstruction/application | Exact with explicit half-rounding | Final AO is full-resolution R16F | Fused resolve/application PSO; see M-008 | Removes full-resolution R16F texture | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | Exact capture and logical traffic | Controlled local fusion saved 6.250% median and 17.510% p95; Fixed 8 plus fusion saved 7.870% and 19.066%, with 600/600 complete frames per profile. | Clean captures; exact same-phase GPU equivalence pending | Implemented as optional experiment | Format option is realized by fusion, not by keeping a dummy texture. Implementation evidence: the fused plan omitting `FinalAmbientVisibility`. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches. Keep both fused profiles for product validation while target timing remains unclaimed. |
| P-015 | R8 final resolved AO | Supplied prompt | Reconstruction/application | Numerical | Full-resolution final AO is R16F | Bounded final-output R8 profile when an intermediate is required | R8 texture replaces R16F | None | Medium bandwidth estimate - R8 halves plane bytes versus R16F, only if range and validation remain correct. | Quantization/banding captures and timing | Final physical AO can be clamped safely, but precision impact is unknown | Pending | Blocked by correctness | Valid only after final reconstruction, unlike raw/history AO. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-016 | R16F temporal depth history | Supplied prompt | Temporal | Numerical | History depth is R32F | R16F depth-history profile | Conditional R16F histories replace R32F | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Reprojection validation false-positive/negative fixtures across depth; timing | Not tested | Disocclusion risk | Blocked by correctness | Halves history bytes but validation thresholds must absorb quantization safely. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-017 | R32F temporal depth history | Supplied prompt | Temporal | Exact reference | Two R32F histories exist | Preserve Reference temporal PSO | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection | Present | Canonical | Already equivalent | Required reference for lower-precision experiments |
| P-018 | RG8 octahedral temporal normal history | Supplied prompt | Temporal | Numerical | RGBA8_UNORM history stores decoded XYZ | Oct encode/decode history profile | Conditional RG8 histories replace RGBA8 | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Angular error, validation acceptance, motion captures, timing | Not tested | Pending | Blocked by correctness | Halves normal-history bytes but adds decode and changes validation. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-019 | No temporal normal history | Supplied prompt | Temporal | Algorithmic | Canonical temporal retains normal validation; both PS4 profiles allocate/read only R32F depth history plus R16F AO history | Retain the depth-only history contract inside PS4; do not apply it silently to canonical temporal | Removes normal-history allocation, binding, and reads in the selected PS4 path | None | Included in the PS4 comparison; zero impact in the primary temporal-off Reference profile | Thin/disocclusion/normal-only motion suite and timing | Both PS4 variants completed 600/600 controlled frames with no normal-history resource or read; temporal medians were 0.036864 ms scalar and 0.037888 ms packed. | Clean static captures; normal-only motion and dynamic disocclusion remain pending | Implemented as optional experiment | The profile intentionally trades normal-edge rejection for the source-informed depth/history contract. It is fully isolated from canonical temporal behavior and should remain an algorithm-comparison setting until dynamic stress passes. |
| P-020 | R8_UINT packed edge metadata | Supplied prompt; [pinned XeGTAO `PackEdges`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Trace/reconstruction | Numerical metadata feeding Algorithmic filter | Bitmask packed-edge profiles use R8_UINT; XeGTAO separately uses R8_UNORM | Retain each format only with its matching unpack/filter contract | One R8_UINT at trace resolution in bitmask edge profiles | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | All 256 values, reverse-depth/edge fixtures, timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Integer loads avoid normalized conversion and preserve exact 2-bit codes for the UVSR packed-edge experiments. The Xe source port's R8_UNORM edge plane is inventoried separately in P-021. |
| P-021 | R8_UNORM packed edge metadata | Supplied prompt; XeGTAO/ASSAO/CACAO | Trace/reconstruction | Numerical metadata feeding Algorithmic filter | XeGTAO main pass writes one R8_UNORM packed edge plane | Retain inside the source-port denoiser; keep the separate R8_UINT bitmask edge experiment for its own kernels | One R8_UNORM in XeGTAO only | None | Included in the XeGTAO pipeline forecast; the format itself is not an isolated saving | Byte round-trip, edge/filter tests, and source-port captures | Implemented and compiled in all source-port profiles | Pinned XeGTAO packing with UVSR bounds guards | Implemented and retained | The R8_UNORM resource is created only for XeGTAO, binds only to its main/denoise passes, and costs Reference nothing. It must not be confused with the R8_UINT packed-edge bitmask profiles. |
| P-022 | Packed RGBA8 noise | Supplied prompt | Noise/trace | Exact for prepacked existing FAST values; Algorithmic for changed sequences | Scalar R8 FAST requires separate semantic reads | Lazy packed FAST texture with current semantic offsets; separate vector/noise profiles | 512 KiB for 64x64x32 RGBA8 if retained | None; texture absent | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Value-for-value lookup test, load count/cache/timing, spectral tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact for current FAST delivery | Implemented and retained | One fetch can reproduce four current independent R8 values without regenerating noise. Implementation evidence: `PackVisibilityFilterAdaptedNoiseRgba8` and `PACKED_FAST_NOISE`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| P-023 | Packed R32_UINT noise | Supplied prompt | Noise/trace | Exact for byte-packed current FAST | Scalar R8 FAST | R32_UINT load and byte unpack fixed-count PSO | 512 KiB if stored one uint per texel/layer | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Exact unpack fixtures, DXIL/load/timing | Not present | Exact delivery expected | Not applicable | Compare integer unpack and texture format against RGBA8 sampling. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| P-024 | Vector-valued FAST volume | Supplied prompt; [FAST generator](https://github.com/electronicarts/fastnoise) | Noise/trace | Algorithmic | Scalar filter-adapted volume sampled at semantic offsets | Jointly optimized 4D scheduler volume matched to active filter/history | Conditional generated asset | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Marginals, cross-correlation, spectra, RMSE/temporal capture, timing | Generator supports vector distributions | Quality pending | Blocked by architecture | Joint optimization avoids deriving four semantics from one scalar. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| P-025 | Mixed-precision scheduler values | Supplied prompt | Noise/trace | Numerical | Scheduler results are FP32 | Conservative mixed-precision scheduler block | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Sample-coordinate and mask disagreement, DXIL | Not tested | Pending | Blocked by correctness | Candidate avoids precision-sensitive interval math while reducing benign arithmetic. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-026 | Mixed-precision normalized sample positions | Supplied prompt | Trace | Numerical | FP32 | Conservative mixed-precision normalized offsets only | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Pixel-coordinate equality and duplicate-rate tests | Not tested | Pending | Blocked by correctness | A tiny error can choose a different integer texel, so exact coordinate comparison is mandatory. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-027 | Mixed-precision radial offsets | Supplied prompt | Trace | Numerical | FP32 | Conservative mixed-precision radial distribution block | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Physical stratum/pixel coordinate comparison and timing | Not tested | Pending | Blocked by correctness | Isolate from view/interval math; reject if texel identity changes excessively. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-028 | Mixed-precision spatial filter weights | Supplied prompt; CACAO FP16 LDS filter | Reconstruction | Numerical | FP32 filter weights | Mixed-precision filter/fused PSO | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Weight/image error, edge suite, register/timing | CACAO demonstrates packed FP16 filtering, not UVSR equivalence | Pending | Blocked by correctness | Bounded local weights are safer than interval math and may reduce registers. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-029 | Mixed-precision normal and slice-basis intermediates | Supplied prompt | Trace/filter | Numerical | FP32 | Conservative isolated half intermediates | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Angular, mask, normal-edge and image tests | Not tested | Pending | Blocked by correctness | Basis error can rotate sector intervals, so keep out of initial conservative profile unless validated. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-030 | Mixed-precision final bounded AO application | Supplied prompt | Application | Numerical | AO strength/application arithmetic is FP32 after R16F read | Native-half application profile | None | None | Medium potential, low confidence - selected width can fall by up to 50%, but conversions, occupancy, and sector errors can negate it. | Final HDR error/banding, DXIL/timing | Not tested | Expected low risk after bounded resolve | Blocked by correctness | Safest precision domain, but composition ordering and emissive/direct exclusions must remain. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| P-031 | Existing R16F raw and temporal AO | Supplied prompt | Trace/temporal | Exact reference | Reduced raw and optional temporal AO are R16F | Preserve Reference resource plan | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection and reference image | Present | Canonical | Already equivalent | Required range-preserving baseline for all estimators |
| P-032 | Existing RGBA8 temporal normal history | Supplied prompt | Temporal | Exact reference | Two RGBA8_UNORM histories exist | Preserve Reference temporal plan | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection | Present | Canonical | Already equivalent | Reference for oct/no-normal comparisons |
| P-033 | Existing R16_UNORM toroidal and scalar R8 FAST storage | Supplied prompt | Noise | Exact reference | 64x64x8 R16_UNORM toroidal and 64x64x32 R8 FAST textures | Preserve current noise modes | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Asset/resource inspection and value tests | Present | Canonical blue-noise modes | Already equivalent | Product requirement retains both current sampling modes |

## Dispatch, Memory, and Resource Policy

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-001 | 8x8 thread groups | Supplied prompt; [Intel Arc guide](https://www.intel.com/content/www/us/en/developer/articles/guide/arc-a-series-gaming-api-developer-optimization.html); AMD RDNA guide | Dispatch | Exact | Visibility/depth work predominantly uses 8x8 groups | Preserve Reference and include in group-shape comparison | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | DXIL, occupancy, EU/sampler counters, median/p95 | Present and vendor-aligned as general starting point | Exact | Already equivalent | Target-specific winner still requires comparison with other shapes |
| D-002 | 16x8 thread groups | Supplied prompt | Dispatch | Exact | Reference trace uses 8x8 | `ExactGroup16x8` compiles and dispatches a fixed-8 AO trace with 16x8 threads; later/temporal stages retain their own 8x8 dispatch geometry | One lazy PSO | None | Uncertain estimate: -0.08 ms regression to 0.14 ms saving; no physical reads are removed and occupancy/locality dominate. | Occupancy/register/cache and complete-effect timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact dispatch-shape change | Implemented as optional experiment | `VISIBILITY_ALGORITHM=7` sets `VISIBILITY_GROUP_SIZE_X=16`, while the runtime separates trace dispatch divisors from the 8x8 sampling/post stages. The execution plan validates the 16x8 workload and Reference selects no candidate PSO. |
| D-003 | 8x16 thread groups | Supplied prompt | Dispatch | Exact | Reference trace uses 8x8 | `ExactGroup8x16` compiles and dispatches a fixed-8 AO trace with 8x16 threads; later/temporal stages retain their own 8x8 dispatch geometry | One lazy PSO | None | Uncertain estimate: -0.08 ms regression to 0.12 ms saving; no physical reads are removed and vertical locality can lose. | Occupancy/register/cache and complete-effect timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact dispatch-shape change | Implemented as optional experiment | `VISIBILITY_ALGORITHM=8` sets `VISIBILITY_GROUP_SIZE_Y=16`, and the CPU plan validates the requested shape. Reference remains the canonical 8x8 path with no candidate PSO. |
| D-004 | Additional measured group shapes | Supplied prompt | Dispatch | Exact | No profile | Curated only after evidence, never unrestricted product control | None | None | Low-to-medium estimate - occupancy, locality, descriptors, or overlap change without reducing eight physical reads; counters required. | Generated-code and isolated timing | Pending | Exact | Not applicable | Vendor guidance is heuristic; only measured shapes should remain. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| D-005 | Auto fixed-sample selection | Supplied prompt | CPU pipeline selection | Exact | Generic PSO receives runtime sample count; explicit Fixed 8/12/16/20 profiles select their own PSOs | CPU maps supported exact counts to fixed PSO and all others to generic | Lazy PSOs only | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Active key assertions; image/order fixtures | Explicit fixed profiles are implemented, but an `Auto Fixed` selector and automatic supported-count resolver are not | Exact when explicitly selected; automatic selection is pending | Blocked by architecture | The compiled fixed shaders and CPU-selected explicit profiles prove that automatic mapping could be added with zero Reference GPU cost, but the current resolver does not perform that mapping and the UI has no `Auto Fixed` control. No correctness or API barrier has been demonstrated. This is an explicit unmet control/integration item, not a measured rejection; unsupported counts continue to use the canonical generic path. |
| D-006 | Direct device-depth trace | Supplied prompt | Depth/trace | Exact reference | Target radius 3 path reads device depth and builds no hierarchy | Preserve Reference depth profile | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource/dispatch inspection and timing | Present | Canonical | Already equivalent | Baseline against every preparation mode |
| D-007 | Existing full-resolution linear-depth hierarchy | Supplied prompt | Depth/trace | Numerical/Algorithmic | Five R16F mips are conditionally built for AO-only perspective radius at least 8 | Keep explicit profile rather than hidden radius-only behavior | Existing conditional texture | None while predicate false | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Prep plus trace and image comparison | Present but inactive for target profile | Existing behavior | Already equivalent | Expose accounting; do not call it a target win without total-cost evidence |
| D-008 | Half-resolution linear-depth base | Supplied prompt; Activision slide 85 | Depth/trace | Algorithmic plus Numerical if storage changes | The PS4 profiles build a half-resolution closest-depth guide and preserve the chosen 2x2 source identity | Retain inside the coupled PS4 profiles; a generic bitmask base would remain a separate experiment | Conditional R32_UINT packed-FP32 depth plus two-bit source offset | None | Included in the PS4 comparison; direct Reference depth still has zero preparation cost at radius 3 | Total cost, masks, odd dimensions, reconstruction/history checks | Both PS4 variants completed 600/600 controlled frames; depth preparation measured 0.008192 ms median and all downstream PS4 passes consumed the guide. | Clean captures; intentional foreground-biased receiver | Implemented as optional experiment | The dedicated pass selects an actual closest valid surface rather than averaging, packs its source offset, and handles clamped odd-edge footprints. This clears the PS4 architecture gap without claiming that a prepared half base improves the canonical Reference path. |
| D-009 | Reuse compatible renderer Hi-Z | Supplied prompt | Depth/trace | Algorithmic unless representation and reduction exactly match | No compatible shared hierarchy is bound | CPU-selected reuse only after format, convention, extent, reduction, and lifetime validation | None if truly reused | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Resource audit, mip-value comparison, total timing | No proven compatible resource at canonical base | Unknown | Not applicable | Sharing is beneficial only when semantics and synchronization are compatible. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| D-010 | Closest/foreground depth reduction | Supplied prompt; Activision slide 85 | Depth preparation | Algorithmic | The PS4 prepared-depth pass selects minimum positive linear view depth and retains the winning 2x2 source offset | Retain as the PS4 guide reduction; other estimators require their own receiver proof | Conditional packed depth/offset texture | None | Included in the PS4 comparison; not a Reference-path product saving | Thin-edge/mask tests and total timing | Both controlled PS4 runs used the reduction for 600/600 complete frames; prepared-depth median was 0.008192 ms and final captures were clean in the prior artifact scene. | Intentional conservative foreground bias | Implemented as optional experiment | The shader validates device depth under both reverse- and forward-depth conventions, compares positive linear view depth, and keeps an actual receiver surface through the packed offset. This implementation is deliberately scoped to the PS4 approximation. |
| D-011 | Minimum positive linear depth | Supplied prompt | Depth preparation | Algorithmic | No half-base minimum | Conditional reduction profile | Half-depth texture | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Reverse-depth/clear-value fixtures and image/timing | Pending | Bias depends on convention | Blocked by correctness | Linear-depth minimum is foreground only when positive depth convention is fixed. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| D-012 | XeGTAO far-anchored smart average | Supplied prompt; [pinned XeGTAO `DepthMIPFilter`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Depth preparation | Algorithmic | Implemented in the XeGTAO source-port hierarchy; generic UVSR hierarchy has different semantics | Retain inside XeGTAO only unless a separate bitmask experiment proves interval safety | Conditional XeGTAO depth hierarchy | None | Included in M-010; at radius 3 Reference still has zero preparation cost | Representative-depth/source fixture, total timing, and image review | Implemented for the source port; no bitmask transfer is claimed | Source-defined for XeGTAO; unsafe as an exact bitmask substitution | Implemented and retained | The far-anchored reduction is part of the complete alternative estimator. Coarse representatives can still erase gaps or over-cover UVSR finite-thickness sectors, so this implementation does not clear the separate product correctness gate. |
| D-013 | Current UVSR smart average | Supplied prompt | Depth preparation | Algorithmic reference for hierarchy | Existing hierarchy reduction | Preserve selectable existing hierarchy | Existing conditional texture | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Existing/reference capture | Present | Existing hierarchy behavior | Already equivalent | Needed to compare alternative reductions without redefining reference |
| D-014 | Hybrid edge-aware representative depth | Supplied prompt | Depth preparation | Algorithmic | No hybrid selector | Conditional reduction that chooses an actual source sample and records offset | Half-depth and optional offset metadata | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Edge/receiver identity/history/image/timing suite | Pending | Pending | Blocked by correctness | May reduce mixed-surface bias while preserving anchor identity. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| D-015 | Runtime `log2` mip selection | Supplied prompt; XeGTAO README | Trace | Algorithmic selection; Numerical implementation | Current UVSR uses fixed distance thresholds | Separate depth-profile PSO | None beyond hierarchy | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Mip-selection fixture, DXIL, timing/image | Not current | Pending | Not applicable | General but potentially costly; compare against fixed thresholds and `firstbithigh`. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| D-016 | Fixed per-tap mip selection | Supplied prompt; SAO p.3; ASSAO | Trace | Exact only for a fixed current mapping | Current thresholds evaluate per tap | Fixed-profile compile-time mapping | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Exhaustive distance/mip comparison and DXIL | Pending | Exact only if mapping matches | Not applicable | Removes dynamic LOD math in curated radius/count profiles. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| D-017 | Configurable mip bias | Supplied prompt; XeGTAO README | Trace | Algorithmic | Hard-coded UVSR thresholds | Algorithmic hierarchy profile parameter | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Bias sweep, masks/cache/image/timing | Pending | Surface mixing/detail tradeoff | Not applicable | Expose only in advanced experiment; changing LOD changes sampled geometry. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| D-018 | Checkerboard sampling layout | Supplied prompt; XeGTAO README suggestion | Trace/reconstruction | Algorithmic | Half-resolution full grid | Checkerboard trace plus matching resolve/history PSOs | Conditional reconstruction/history handling | None | High producer estimate - roughly 50% fewer trace invocations, with algorithmic reconstruction/history cost and quality risk. | Sample traffic, flicker/edge/image/timing | Pending | Expected temporal/spatial artifacts | Blocked by correctness | Reduces samples but changes coverage and requires reconstruction. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| D-019 | Deinterleaved sampling layout | Supplied prompt; [NVIDIA deinterleaved texturing](https://d29g4g2dyqv443.cloudfront.net/sites/default/files/akamai/gameworks/samples/DeinterleavedTexturing.pdf); ASSAO/CACAO | Trace/data layout | Exact only if same samples; otherwise Algorithmic | Straight texture layout | Experimental AoS-to-SoA depth and matching trace/reinterleave pipeline | Multiple quarter-resolution layers and preparation/reinterleave passes | None | Low-to-medium estimate - occupancy, locality, descriptors, or overlap change without reducing eight physical reads; counters required. | Total prep/trace/reinterleave, cache and image equality | ASSAO/CACAO benefit; XeGTAO rejects for straight-line pattern | Pending | Blocked by architecture | Exact evaluation needs a layered depth-preparation pass, a trace address mapping that preserves the selected physical samples, and a matching reinterleave/receiver mapping before application. None exists in the current straight-layout resource contract. Separate resources and PSOs could preserve zero Reference cost, so conflicting ASSAO/CACAO and XeGTAO evidence still calls for an experiment; this is a concrete multi-pass prerequisite, not proof that the experiment is unreasonable. |
| D-020 | Group-shared sampling tile | Supplied prompt; AMD RDNA guide | Trace/filter | Exact if identical texels and math | Direct texture reads | Separate tiled PSO with halo | Group-shared memory only | None | Low-to-medium estimate - occupancy, locality, descriptors, or overlap change without reducing eight physical reads; counters required. | LDS bank conflicts, occupancy, hit rate, timing, image equality | Pending | Exact if complete halo preserved | Blocked by architecture | Reference permits an unbounded projected-radius footprint, so no finite per-group halo can stage every exact radial tap. A clamp-bounded algorithmic profile could make a finite tile possible, but it would overlap the separate radius-clamp experiment and still needs halo sizing, boundary rules, and target LDS/occupancy measurement. No such combined PSO was attempted; zero Reference cost remains feasible through CPU selection. |
| D-021 | Duplicate-pixel rejection | Supplied prompt | Trace | Exact for repeated identical sample contribution under current semantics | Reference skips consecutive duplicate pixel coordinates per side | Preserve On and compare with CPU-selected `ExactDuplicatePixelRejectionOff` fixed-8 benchmark control | One lazy off-control PSO | None | Expected 0 ms incremental because On is already Reference; the off-control delta quantifies the retained saving. | Sample-coordinate/count, mask/AO/GI ownership fixtures and timing | On is canonical; the packaged Off control completed the comprehensive Intel smoke. | Exact under the current consecutive-side rule | Already equivalent | The benchmark-only wrapper compiles `DUPLICATE_PIXEL_REJECTION=0` without changing Reference. Broader dedup or traversal reordering would require a separate algorithmic classification. |
| D-022 | Full-mask early exit | Supplied prompt; visibility paper Algorithm 1 | Trace | Exact with contribution-order condition | Reference exits when the register mask is full | Preserve On and compare with CPU-selected `ExactFullMaskEarlyExitOff` fixed-8 benchmark control | One lazy off-control PSO | None | Expected 0 ms incremental because On is already Reference; the off-control delta quantifies data-dependent saved iterations. | Full-mask fixtures including final GI contribution; divergence/timing | On is canonical; the packaged Off control completed the comprehensive Intel smoke. | Exact | Already equivalent | The benchmark-only wrapper compiles `FULL_MASK_EARLY_EXIT=0`; Reference still exits only after the completing sample contributes any newly claimed GI. |
| D-023 | Texture-gather filter storage | Supplied prompt; XeGTAO/ASSAO | Reconstruction | Exact if identical taps/LODs | PS4 packed reconstruction and XeGTAO denoise use explicit gathers; canonical compact/Gaussian filters still use individual loads | Retain profile-specific gather PSOs; generic compact gather remains a separate experiment | None beyond selected profile resources | None | Included in the PS4 and XeGTAO comparisons; a generic exact-kernel benefit remains unknown | Logical taps, DXIL, sampler/cache, image/timing | PS4 packed uses four AO plus four depth gathers and saved 1.338% median/7.845% p95 versus its scalar oracle. Xe one-pass gather denoise completed all three controlled 600-frame profiles. | Same intended PS4 texels and pinned Xe source order; clean captures | Implemented as optional experiment | The technique is implemented and measured in two profile-specific kernels, so it is not an architecture absence. A generic compact 2x2 gather still needs component-order fixtures and target timing and is not implied by this disposition. |
| D-024 | Group-shared filter tile | Supplied prompt; CACAO | Reconstruction | Exact if kernel/order retained | Direct loads | Tiled filter/resolve PSO | Group-shared memory only | None | Low-to-medium estimate - occupancy, locality, descriptors, or overlap change without reducing eight physical reads; counters required. | LDS conflicts, occupancy, image/timing | Not implemented or measured | Exact if math retained | Blocked by architecture | The current fixed compact filter has a bounded halo, so a separate tiled PSO could preserve taps, weights, and zero Reference cost. Synchronization, bank behavior, and occupancy may make it slower, but that requires generated code and timing. This is deferred rather than blocked by a demonstrated API or correctness invariant. |
| D-025 | Two-output thread coarsening | Supplied prompt; [pinned XeGTAO entry point](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/vaGTAO.hlsl) | Reconstruction | Exact | One output invocation in UVSR generic filters; XeGTAO denoise follows the pinned two-horizontal-pixel entry structure | Preserve the Xe source behavior; add a generic horizontal two-pixel PSO only after register/edge proof | None | None | Included in M-010 for Xe; low-to-medium potential remains for generic reconstruction | Register count, shared gathers, image/timing | The two-output Xe denoiser completed 600/600 controlled frames in all three profiles; spatial medians were 0.029696, 0.028672, and 0.023552 ms. A generic-filter transfer is absent. | Exact in source port with clean captures; generic proof pending | Implemented as optional experiment | The source port is a concrete CPU-selectable implementation, so the technique is not architecture-blocked. It does not prove that UVSR's different kernels benefit; a generic transfer remains a separate candidate with Reference unchanged. |
| D-026 | Four-output thread coarsening | Supplied prompt; CACAO blur | Reconstruction | Exact | One output invocation | Four-pixel vectorized PSO | None | None | Low-to-medium estimate - occupancy, locality, descriptors, or overlap change without reducing eight physical reads; counters required. | Registers/occupancy/cache/image/timing | Not implemented or measured | Exact if each output retains its texels and math | Blocked by architecture | A four-output PSO is likewise isolatable with zero Reference cost. Higher live state may spill or reduce occupancy, but physical register/spill evidence is pending and no implementation was attempted. The architecture disposition is therefore a disclosed deferral, not proof that coarsening is unsuitable. |
| D-027 | Fused resolve and apply | Supplied prompt | Reconstruction/application | Exact with intermediate rounding | Separate filter and composite | CPU-selected fused PSO | Removes full-resolution AO texture | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | See M-008 | Controlled local fusion saved 6.250% median and 17.510% p95; Fixed 8 plus fusion saved 7.870% and 19.066%, with 600/600 complete frames per profile. | Clean captures; exact same-phase GPU equivalence pending | Implemented as optional experiment | Required first fusion experiment. Implementation evidence: `ExactFusedResolveApply`. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches. Keep both fused profiles for product validation while target timing remains unclaimed. |
| D-028 | Apply AO inside deferred lighting | Supplied prompt | Deferred lighting/application | Exact for AO-only when ordering and equations match | PBR writes BaseLighting, then AO composition rereads material | AO-aware PBR deferred permutation | Can remove BaseLighting allocation/clear and separate composition for AO-only | None | High estimated upside - can avoid the 15.8 MiB 1080p RGBA16F BaseLighting surface plus material rereads, non-additive with fusion. | Complete-frame timing, lighting equation fixtures, exact capture | Architecture inspection identifies repeated material reads and 15.8 MiB BaseLighting | Exactness pending | Blocked by architecture | Largest fusion opportunity; GI still requires source radiance before traversal. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| D-029 | Minimal AO-only binding layout | Supplied prompt; Intel Arc guide | Trace/application | Exact | Reference first-trace/filter/composite layouts bind broad dummy-compatible sets | Advanced AO PSOs build layouts from the reflected resource subset; fixed direct-depth AO uses only its constants, depth/normal/noise inputs, and AO output | Removes unused bindings; no new resource | None | Low estimate: 0.00-0.10 ms, driver-sensitive and non-additive with fixed/fused profiles. | Active SRV/UAV counts, DXIL reflection, timing, exact capture | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | `screen_space_visibility.cpp` constructs profile-specific layouts/binding sets for fixed, diagnostic, packed, filter, and fused candidates. Reference still selects `LegacyBroad`, so it gains no branch, resource, or candidate descriptor cost. |
| D-030 | Minimal GI-only binding layout | Supplied prompt | Trace/application | Exact | Reference uses broad common layouts | Fixed GI/AO+GI and later-bounce candidates bind only depth, normal, hierarchy/noise/source/frontier resources actually reflected by that shader | Removes unused AO or feedback bindings | None | Expected 0 ms in primary AO-only; low-to-medium potential in GI/multi-bounce workloads. | Counts, DXIL reflection, GI ownership fixtures, timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Consumer-specific layout construction keeps AO-only formats and resources out of GI-only candidates while preserving source-radiance ownership. Reference remains broad and unchanged. |
| D-031 | Consumer-driven allocation | Supplied prompt | Resource planning | Exact | Resources are conditionally allocated for major modes, but noise/dummies are permanent | Resolved CPU resource/pass plan per curated profile | Removes unused textures | None | Low-to-medium estimate - reduces binding, traffic, or scheduling overhead without reducing eight physical reads. | Resource-plan tests and byte counters | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Required zero-cost contract and visible optional-byte accounting. Implementation evidence: conditional allocation in `EnsureResources`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| D-032 | Lazy advanced PSO creation | Supplied prompt | Pipeline creation | Exact CPU optimization | 53 visibility compute PSOs are created eagerly | Reference eager set plus on-demand curated advanced PSOs | CPU/driver cache entries only after use | None on GPU | Low-to-medium estimate - reduces binding, traffic, or scheduling overhead without reducing eight physical reads. | Startup/PSO count and cache tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | No image effect | Implemented and retained | Avoids multiplying startup and memory cost by experimental permutations. Implementation evidence: `GetOrCreateAdvancedPipeline`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| D-033 | Cached curated profile permutations | Supplied prompt | Pipeline creation | Exact CPU optimization | Eager arrays, no full advanced key cache | Stable profile-keyed PSO cache | CPU/driver cache | None on GPU | Low-to-medium estimate - reduces binding, traffic, or scheduling overhead without reducing eight physical reads. | Key collision/history invalidation tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | No image effect | Implemented and retained | Supports useful specialization without a Cartesian product. Implementation evidence: `m_AdvancedPipelines`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| D-034 | Active resource and traffic counters | Supplied prompt | UI/instrumentation | Exact instrumentation; descriptor counts cover first trace and peak per pass | No unified optional-byte/intermediate/traffic/dispatch report | Verification drawer exposes plan masks, logical texture/traffic bytes, exact first-trace and peak-per-pass SRV/UAV counts, and active-dispatch count | CPU metadata only | None | Negligible estimated GPU impact - reporting only, with no extra dispatch. | Compare every displayed value with created resources, final binding layouts, and dispatched passes | Resource/pass identities, bytes, first-trace/peak descriptors, and dispatch planning are integrated; a whole-effect per-layout inventory remains outside the current counter. | No image effect | Implemented and retained | `BuildVisibilityExecutionPlan` derives first-trace counts from the selected broad Reference, minimal candidate, or diagnostic contract, then takes the maximum simultaneous SRV/UAV count across depth, later bounce, temporal, reconstruction/fusion, application, and composition layouts. The renderer labels both scopes. The binding mask remains a conceptual effect-wide union, and peak is not a sum or enumeration of every layout; use reflection/capture for that broader inventory. |

## Fixed Sample-Count Specialization

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S-001 | Generic arbitrary-count fallback | Supplied prompt | Trace | Exact reference | Runtime generic loop and adaptive paths | Preserve unchanged Reference Generic PSOs | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Reference image/order fixtures | Present | Canonical | Already equivalent | Required for unsupported counts, projections, and adaptive behavior |
| S-002 | Fixed 8 AO-only, four samples per side | Supplied prompt; target profile | Trace | Exact | Generic AO PSO with runtime count | `AO/Fixed8` PSO preserving rotated nested prefix and interleaved sides | One lazy PSO | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Mask/AO/sample-coordinate equality, DXIL/register/timing | Controlled local Reference and Fixed 8 trace medians were both 0.101376 ms; Fixed 8 regressed complete median by 0.926% despite a lower p95. | Exact fixtures pass; clean capture | Implemented and retained | Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The specialization remains a benchmark and component of Fixed 8 plus fusion, but its standalone profile did nothing useful locally and should be dropped from production consideration. Reference retains canonical work, and Xe-LPG timing remains unclaimed. |
| S-003 | Fixed 12 AO-only, six samples per side | Supplied prompt | Trace | Exact | Generic | `AO/Fixed12` PSO | One lazy PSO | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Same as S-002 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested common variant. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-004 | Fixed 16 AO-only, eight samples per side | Supplied prompt | Trace | Exact | Generic | `AO/Fixed16` PSO | One lazy PSO | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Same as S-002 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested common variant. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-005 | Fixed 20 AO-only, ten samples per side | Supplied prompt | Trace | Exact | Generic | `AO/Fixed20` PSO | One lazy PSO | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Same as S-002 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested common variant even though not in one UI shorthand list. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-006 | Fixed 8 GI-only | Supplied prompt | GI trace | Exact | Generic GI | `GI/Fixed8` preserving near-to-far owner identity | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Mask/new-count/source-owner/raw-GI fixtures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | GI correctness requires more than final mask equality. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-007 | Fixed 12 GI-only | Supplied prompt | GI trace | Exact | Generic GI | `GI/Fixed12` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-006 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested GI investigation. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-008 | Fixed 16 GI-only | Supplied prompt | GI trace | Exact | Generic GI | `GI/Fixed16` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-006 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested GI investigation. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-009 | Fixed 20 GI-only | Supplied prompt, common variants | GI trace | Exact | Generic GI | `GI/Fixed20` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-006 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Common fixed-count coverage unless measurement rejects retention. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-010 | Fixed 8 combined AO and GI | Supplied prompt | Combined trace | Exact | Generic combined PSO | `AO+GI/Fixed8` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | AO plus GI owner fixtures and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Shared depth traversal with consumer-specific specialization. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-011 | Fixed 12 combined AO and GI | Supplied prompt | Combined trace | Exact | Generic combined PSO | `AO+GI/Fixed12` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-010 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested profile. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-012 | Fixed 16 combined AO and GI | Supplied prompt | Combined trace | Exact | Generic combined PSO | `AO+GI/Fixed16` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-010 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Requested profile. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-013 | Fixed 20 combined AO and GI | Supplied prompt, common variants | Combined trace | Exact | Generic combined PSO | `AO+GI/Fixed20` | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Same as S-010 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Complete requested count inventory. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-014 | Fixed first multi-bounce traversal | Supplied prompt | First-bounce GI trace | Exact | Runtime generic first-bounce variants | Curated Fixed8/12/16/20 first-bounce PSOs | Lazy PSOs | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Owner/raw-GI/bounce-metadata fixtures and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | First bounce has distinct source bindings and deserves its own specialization evidence. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-015 | Fixed later-bounce eight-sample reinjection | Supplied prompt | Later-bounce GI trace | Exact | Generic later-bounce PSO as count falls to floor eight | `LaterBounce/Fixed8` only when actual count equals eight | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Frontier/owner/metadata fixtures and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Use generic or exact matching specialization for every other actual count. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-016 | Fixed later-bounce 12/16/20 exact matches | Supplied prompt | Later-bounce GI trace | Exact | Generic later bounce | Optional exact matching PSOs only if those counts occur and measure useful | Lazy PSOs | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Actual-count telemetry, fixtures, timing | Applicability pending bounce schedule | Exact required | Not applicable | Do not select a nearby specialization that changes sample count. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| S-017 | Remove runtime sample-count clamping | Supplied prompt | Fixed trace | Exact | Generic clamps runtime count | Compile valid fixed count into PSO; retain host validation | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL and invalid-setting fallback tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Fixed key makes hot clamp redundant. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-018 | Remove runtime side-count division | Supplied prompt | Fixed trace | Exact | Generic divides/derives counts | Compile samples-per-side | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL and order fixtures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Even counts have fixed symmetric sides. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-019 | Remove odd-side random lookup for even counts | Supplied prompt | Fixed trace/noise | Exact | Generic supports odd allocation dimension | Compile out odd-side dimension for 8/12/16/20 | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Noise-load count and exact scheduler value fixtures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | The dimension is semantically unused for requested even counts. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-020 | Remove arbitrary prefix selection | Supplied prompt | Fixed trace | Exact | Generic rotates runtime prefix mask | Compile exact count prefix while retaining rotation | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | All 32 rotations, strata/order equality | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Fixed count removes count lookup, not scheduler rotation or nested-set semantics. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-021 | Fixed loop termination | Supplied prompt | Fixed trace | Exact | Mask-driven `while` plus dynamic side state | Compile N iterations or fixed unrolled scan with full-mask exit | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL, mask/order/GI owner fixtures | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Unknown-count loop control is avoidable; full-mask early exit remains. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-022 | Remove general radial exponent handling | Supplied prompt | Fixed trace | Exact for exponent-2 key | Runtime comparisons and general `pow` fallback | Fixed2 profile emits multiplication only | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL and sample-coordinate equality | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Profile validity must include exponent 2.0. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-023 | Remove unused adaptive resources/code | Supplied prompt | Fixed trace | Exact | Fixed/adaptive compile variants exist, but common layouts/resources remain | Minimal fixed layouts and resource plan | Removes feedback bindings/resources | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Binding/alloc/dispatch assertions | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Disabled adaptive sampling must incur no fixed-profile GPU/resource cost. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-024 | Remove unused feedback resources/code | Supplied prompt | Fixed trace | Exact | Broad layout supports feedback | Same fixed plan as S-023 | Removes feedback textures and UAVs | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Resource-plan tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Explicit supplied requirement. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-025 | Ordered unrolled bit scan | Supplied prompt | Fixed trace | Exact | `firstbitlow` consumes rotated masks dynamically | Unroll fixed number of iterations while preserving `firstbitlow` order | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Physical-stratum, mask, owner and raw-GI equality | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Safest fixed specialization for GI ownership. Implementation evidence: `FIXED_SAMPLE_COUNT`, `src/screen_space_visibility_fixed_cs.hlsl`, and `src/shaders.cfg`. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| S-026 | Precomputed ordered-strata table | Supplied prompt | Fixed trace | Exact | Runtime bit scan | Table for each count and 32 radial rotations | Small constant table | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Exhaustive table-versus-generic order test, load/DXIL/timing | Pending | Exact required | Not applicable | Trades bit-scan ALU for constant access; target measurement decides. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| S-027 | Fully unrolled fixed list | Supplied prompt | Fixed trace | Exact only with identical rotated near-to-far list | Dynamic generic order | Benchmark fixed list PSO | None or constant table | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Exhaustive owner and order fixture | Pending | Exact required | Not applicable | Reject or downgrade if it groups sides or changes GI ownership. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| S-028 | Preserve independent within-stratum jitter | Supplied prompt | Fixed trace/noise | Exact invariant | Current scheduler provides per-side dimensions | Fixed PSOs consume the same values | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Value/sample-position equality | Required | Exact | Already equivalent | Specialization is not allowed to replace jitter with stratum centers |
| S-029 | Preserve duplicate-coordinate behavior | Supplied prompt | Fixed trace | Exact invariant | Current consecutive per-side duplicate skip | Fixed PSOs retain same skip/order | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Degenerate radius/near-camera fixtures | Required | Exact | Already equivalent | A faster altered dedup rule can change new-sector ownership |
| S-030 | Direct-depth and hierarchy-aware fixed permutations | Current implementation; supplied fixed/depth requirements | Fixed trace/depth acquisition | Exact | Generic trace carries runtime hierarchy selection and broad depth bindings | Compile `FIXED_DIRECT_DEPTH=1` for radius-3/direct depth and `0` for the existing hierarchy mapping; select on the CPU | Uses the existing conditional hierarchy only when its reference eligibility is true | None; radius-3 Reference and fixed direct-depth profiles build no hierarchy | 0.00 ms at the target radius 3 beyond fixed specialization; forecast 0.01-0.10 ms when an already-profitable hierarchy is active. | Direct/hierarchy sample-position and LOD equality, bindings, total preparation-plus-trace timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact for the selected reference hierarchy mapping | Implemented and retained | Fixed 8/12/16/20 and packed FAST pipelines receive `FIXED_DIRECT_DEPTH` at PSO creation. The hierarchy-aware shader retains the reference distance thresholds and falls back to device depth for invalid prepared samples; it does not claim jittered LODs are compile-time constants. |

## Noise and Sample Scheduling

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| N-001 | Independent hash scheduler | Supplied prompt | Noise/trace | Exact reference | `VisibilityRandom` hashes pixel, dimension, and phase | Preserve selectable reference mode | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Existing distribution and image tests | Present | Canonical mode | Already equivalent | Required inventory baseline and constant-texture-free alternative |
| N-002 | Current toroidal blue noise | Supplied prompt; NVIDIA STBN guidance | Noise/trace | Exact reference | 64x64x8 R16_UNORM toroidal texture with dimension/phase cycle | Preserve current mode | Existing permanent texture under reference policy | None beyond current reference behavior | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Existing blue-noise statistics and captures | Present | Product-required blue noise | Already equivalent | Must remain available and must not be replaced by a structured cheaper sequence |
| N-003 | Current scalar FAST volume | Supplied prompt; [FAST paper](https://arxiv.org/pdf/2310.15364) | Noise/trace | Exact reference | 64x64x32 R8_UNORM volume sampled at semantic offsets | Preserve current mode | Existing permanent texture under reference policy | None beyond current reference behavior | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Existing spectral/distribution and image tests | Present | Product-required FAST mode | Already equivalent | Baseline for packed and regenerated variants |
| N-004 | Activision 4x4 spatial rotation pattern | Supplied prompt; [Activision deck slides 90-93](https://blog.selfshadow.com/publications/s2016-shading-course/activision/s2016_pbs_activision_occlusion.pdf#page=90) | Noise/trace | Algorithmic | Current hash/toroidal/FAST rotations | Algorithmic scheduler PSO using the documented 16-value spatial ordering | None | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | 4x4 phase fixtures, spectra, filter-matched image/timing | Both PS4 profiles exercised the pattern for 600/600 controlled frames after warm-up and produced clean static captures. | Valid inside the coupled PS4 filters; dynamic stability remains pending | Implemented as optional experiment | Deliberately filter-correlated sequence; do not treat as an implementation-only change. Implementation evidence: `ACTIVISION_SCHEDULE`. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target Xe-LPG timing remains unclaimed. |
| N-005 | Activision six temporal rotation phases | Supplied prompt; Activision deck slides 86-93 | Noise/trace | Algorithmic | Current temporal phase behavior | Six-phase scheduler option with degree table `{60,300,180,240,120,0}` | None | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Phase fixture and temporal stability/capture | Both PS4 profiles completed 600/600 controlled frames; one axis per frame converged through history and was stable after the 120-frame warm-up. | Clean static captures; dynamic stability remains pending | Implemented as optional experiment | Physical reads remain unchanged; quality depends on reconstruction/history. `ActivisionTemporalRotation` supplies six distinct bidirectional axes over `[0, pi)`. Reference has no candidate-only bindings, resources, or dispatches. |
| N-006 | Activision spatial and temporal radial offsets | Supplied prompt; Activision deck slides 86-93 | Noise/trace | Algorithmic | Independent per-side radial jitter | Spatial `((pixel.y - pixel.x) & 3) / 4` plus temporal `{0,.5,.25,.75}[(frame/6)%4]`, combined modulo one over the fixed taps | None | None | -0.04 to 0.05 ms for the complete Activision scheduler family; unchanged read count and principally quality-led. | Exhaustive 4x4 values, 24-frame cycle, banding/flicker, image/timing | Both PS4 profiles ran 600/600 controlled frames, covering 25 complete 24-frame radial cycles after warm-up, with clean final captures. | Source formula retained; dynamic flicker review remains pending | Implemented as optional experiment | The shader follows slide 93's separate spatial and temporal formulas and applies the shared offset to both sides of the fixed inner integral. It is CPU-selected; Reference retains its independent jitter and has no candidate resource or dispatch. |
| N-007 | XeGTAO Hilbert plus R2 sequence | Supplied prompt; [pinned XeGTAO README](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/README.md) | Noise/trace | Algorithmic | LUT and inline profiles implement the pinned sequence inside XeGTAO | Keep source-port scheduler separate from UVSR's four-semantic bitmask schedulers | Optional 8 KiB LUT under N-008 | None | LUT versus inline forecast -0.05 to 0.15 ms; sequence quality is not an exact product speedup | Complete Hilbert fixtures, source comparison, timing, and image review | Implemented with static phase under the no-matching-TAA host contract | Valid for source port; not a four-dimensional bitmask scheduler | Implemented as optional experiment | The source port needs only its two R2 values. Reusing it for UVSR's independent slice/CDF/budget/side decisions would still require a separate quality design. |
| N-008 | Precomputed Hilbert-index lookup | Supplied prompt; XeGTAO README | Noise/trace | Exact relative to computed Hilbert sequence | XeGTAO LUT profiles bind a conditional 64x64 R16_UINT table; the inline profile computes the same mapping | Compare the lookup against inline integer Hilbert only inside N-007 | Conditional 8 KiB LUT | None | LUT versus inline forecast -0.05 to 0.15 ms; the upstream percentage is not portable | Exhaustive index equality, timing, and captures | All 4096 LUT indices pass range/uniqueness/adjacency fixtures. In the controlled local run, LUT mixed measured 0.413696 ms versus 0.419840 ms inline mixed, a 1.463% median saving with equal p95. | Exact mapping; clean captures | Implemented as optional experiment | `m_XeGtaoHilbertLut` is allocated, uploaded, bound, and dispatched only for LUT profiles; Reference and inline profiles pay no LUT resource/binding cost. Prefer LUT locally, retain inline as the isolated control, and do not generalize the result to Xe-LPG. |
| N-009 | Packed four-channel toroidal blue noise | Supplied prompt | Noise/trace | Algorithmic unless every unbounded temporal/dimension lookup is preserved | Scalar layered R16 toroidal reads | Conditional precomposed RGBA texture or runtime-compatible pack | Conditional packed texture | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Long-period value identity, spectra, memory/cache/timing | Current semantic layers use different temporal steps/cycle hashes | Exact finite precomposition not established | Blocked by correctness | A finite volume can repeat or change the current sequence; classify honestly rather than claiming lossless packing. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| N-010 | Packed four-channel current FAST | Supplied prompt | Noise/trace | Exact | Scalar FAST reads semantic offsets 0, 1, 4, and 5 for fixed even counts | P-022/P-023 packed delivery profile | Conditional 512 KiB packed asset | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Byte-for-byte values and one-fetch timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Reduces four semantic reads without changing sequence. Implementation evidence: the one-fetch RGBA8 FAST path. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| N-011 | Vector-valued filter-adapted noise | Supplied prompt; FAST generator | Noise/trace | Algorithmic | Scalar FAST offsets | P-024 jointly optimized 4D volume | Conditional generated asset | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Joint distributions, cross-correlation, spectra, image/RMSE/timing | Generator supports vector distributions | Pending | Blocked by architecture | Joint objective is preferable to correlated scalar derivation but changes samples. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| N-012 | Constant scheduler diagnostic | Supplied prompt | Noise/trace | Diagnostic | No Reference constant mode | `DiagnosticConstantScheduler` compiles fixed-8 bitmask tracing with load-free constant stochastic dimensions | One lazy PSO; no scheduler texture | None | 0.00 ms product saving; the delta from fixed-8 Reference estimates scheduler load/address cost only. | Trace median/p95, sampler/traffic counters, and structured-artifact capture | Packaged profile completed both the noise matrix and comprehensive Intel smoke; controlled timing remains pending. | Intentionally structured and invalid for production quality | Implemented as benchmark-only control | A separate wrapper and CPU profile isolate scheduler cost without changing the product default. Reference selects its normal scheduler PSO and no diagnostic work/resource. |
| N-013 | FAST regenerated for selected packed-edge filter | Supplied prompt; [importance-sampled FAST](https://jcgt.org/published/0014/01/08/paper.pdf) | Noise/reconstruction | Algorithmic | Existing FAST objective targets Gaussian spatial plus alpha 0.35 EMA | Separate asset per retained filter/history objective | Conditional generated volume | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Filter-specific RMSE, spectra, static/motion captures, timing | Primary source shows filter matching matters | Pending | Blocked by architecture | Product quality candidate, not an exact delivery optimization. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| N-014 | Importance-sampled FAST lookup | Further research; [importance-sampled FAST](https://jcgt.org/published/0014/01/08/paper.pdf) | Noise/trace | Algorithmic | Current uniform scalar FAST | Distribution-matched texture that avoids runtime transform/search | Conditional importance-sampled volume | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Target-distribution histogram, render error/timing | Paper uses a 128x128x32 volume and one lookup | Pending | Not applicable | Promising when UVSR needs nonuniform vector distributions; current radial transform may already be cheap. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| N-015 | FAST cycle-shuffle temporal indexing | Further research; importance-sampled FAST | Noise/trace | Algorithmic | Current global cycle offset differs | `S(I)=(D+10127I) mod 16384`-style optional scheduler adapted to actual volume | None | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Period/low-discrepancy and temporal captures | Primary source found higher-dimensional Hilbert slower without visible gain in its case | Pending | Blocked by correctness | Changes temporal sequence and must match actual asset dimensions. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| N-016 | R2-spaced distant offsets for multiple stochastic dimensions | Further research; FAST and NVIDIA STBN | Noise/trace | Algorithmic | Current semantic offset table | Alternative multidimensional texture-address schedule | None | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Cross-correlation by distance, spectra, render error | NVIDIA warns correlations can appear near roughly seven pixels | Pending | Blocked by correctness | Only retain if joint/vector asset is not better and correlation tests pass. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| N-017 | Direct vector-valued directional samples | Further research; original FAST and NVIDIA STBN | Noise/trace | Algorithmic | Scalars are transformed to slice/radial semantics | Vector distribution asset consumed directly | Conditional vector texture | None | Low-to-medium speed estimate - reduces scheduler fetch/address work or improves filter complementarity; principally quality-led. | Distribution on circle/vector space, warp spectrum, image/timing | Primary sources warn scalar-to-vector mappings can damage spectra | Pending | Blocked by architecture | Potential quality and ALU improvement, but it changes the scheduler and asset contract. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |

## Depth Preparation and Acquisition

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H-001 | Full-resolution device-depth sampling | Supplied prompt | Depth/trace | Exact reference | Target profile reads device depth directly | Preserve Direct Device Depth | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Baseline trace timing/cache and image | Present | Canonical | Already equivalent | Baseline for every hierarchy and half-base comparison |
| H-002 | Full-resolution R16F linear-depth pyramid | Supplied prompt | Depth preparation | Numerical/Algorithmic | Existing conditional five-mip hierarchy | Preserve explicit profile | Existing conditional texture | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Prep plus trace, image/mask and cache | Present but target radius 3 leaves it off | Existing hierarchy quality | Already equivalent | Must include construction cost and duplicate mip0 traffic in comparison |
| H-003 | Half-resolution R16F linear-depth base | Supplied prompt; Activision slide 85 | Depth preparation | Numerical/Algorithmic | None | D-008 half-base profile | Half-depth texture and optional selected offset | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Total cost and receiver mapping suite | Pending | Pending | Blocked by architecture | Intended bandwidth reduction; actual receiver identity is mandatory. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| H-004 | R32F linear-depth reference | Supplied prompt | Depth preparation | Numerical relative to direct device depth | None for trace hierarchy | P-008 R32F profile | Conditional R32F texture | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Quantization-isolation image/mask and total timing | Pending | High-precision prepared reference | Blocked by architecture | Separates reduction/mip behavior from R16 storage error. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| H-005 | Direct half-base generation from a 2x2 full-resolution block | Supplied prompt; Activision/XeGTAO/CACAO | Depth preparation | Algorithmic | The PS4 profiles dispatch a dedicated 2x2 closest-valid-depth reduction and pack source identity with the result | Retain the PS4-specific reduction; other estimators need their own receiver contract | Conditional R32_UINT packed FP32 depth plus 2-bit source offset | None | Low or negative for the Reference radius-3 path; included in the PS4 comparison where the prepared guide is required | Odd extents, reverse depth, receiver/history/reconstruction tests, and total timing | Both PS4 variants completed 600/600 controlled frames. Prepared-depth median was 0.008192 ms for each, and the trace, temporal, spatial, and upsample consumers used the packed guide. | Clean captures; foreground-biased algorithmic receiver | Implemented as optional experiment | `screen_space_visibility_activision_depth_cs.hlsl` scans the clamped 2x2 block, selects the closest valid linear depth, and stores its two-bit texel offset in the depth's low mantissa bits. This implementation is PS4-profile-specific and does not claim a generic Reference-path win. |
| H-006 | Closest-depth reduction | Supplied prompt; Activision slide 85 | Depth preparation | Algorithmic | The PS4 prepared-depth pass selects minimum positive linear view depth from each 2x2 footprint | Retain the PS4 closest-depth guide separately from other reduction choices | Conditional packed depth/offset texture | None | Included in the PS4 comparison; it is not a Reference-path speed optimization | Thin-geometry/mask/image and total timing | Controlled PS4 scalar and packed runs used the closest-depth guide for all 600/600 complete frames; prepared-depth median was 0.008192 ms in both. | Intentional foreground bias; clean captures including the prior artifact scene | Implemented as optional experiment | This row records the reduction semantic, while H-005 records direct base generation. The closest surface and its two-bit source identity are consumed by trace and temporal reprojection; no averaging or fixed `samplingPixel*2+1` receiver remains in the PS4 path. |
| H-007 | XeGTAO far-anchored smart average transferred to the UVSR bitmask hierarchy | Supplied prompt; XeGTAO `DepthMIPFilter` | Depth preparation | Algorithmic | The XeGTAO source port implements the pinned reduction; the generic UVSR hierarchy retains different finite-interval semantics | Keep the implemented reduction inside XeGTAO; require a separate bitmask transfer experiment and interval-safety proof | Conditional hierarchy only in the selected source port or future transfer profile | None | Included in M-010 for XeGTAO; generic Reference radius 3 has no preparation cost | Representative/mask/quality and timing | The source-port implementation completed all three 600/600 controlled local profiles. No generic bitmask transfer is implemented or claimed. | Source-defined for XeGTAO; bitmask interval safety remains unproven | Blocked by correctness | D-012 records the implemented source-port feature. This duplicate category row now scopes only the proposed transfer to UVSR's finite-thickness bitmask estimator, where a far-anchored representative can erase gaps or over-cover sectors. The block applies to that transfer, not to the XeGTAO port. |
| H-008 | UVSR smart average | Supplied prompt | Depth preparation | Algorithmic reference | Existing hierarchy uses it | D-013 | Existing conditional hierarchy | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Existing/reference comparison | Present | Existing | Already equivalent | Required comparison point |
| H-009 | Hybrid representative-depth selection | Supplied prompt | Depth preparation | Algorithmic | None | D-014 | Conditional depth/offset | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Edge and receiver mapping suite | Pending | Pending | Blocked by correctness | Candidate to avoid cross-surface averages while retaining cache benefits. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| H-010 | One-dispatch LDS mip generation | Supplied prompt; [pinned XeGTAO `PrefilterDepths16x16`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli); AMD SPD | Depth preparation | Exact architecture for same reduction | Generic hierarchy and XeGTAO source port each create five local mips from a 16x16 tile in one compute dispatch | Preserve both selected reductions; compare SPD/wave only if semantics stay exact | Existing LDS only | None | Expected 0 ms incremental - already active in both hierarchy families | Dispatch/source inspection and prep timing | Present and compiled | Existing family-specific reduction semantics | Already equivalent | Alternate wave/SPD reduction or precision remains a separate experiment. |
| H-011 | Build only mips actually consumed | Supplied prompt; AMD SPD | Depth preparation | Exact | Current builder writes all five levels whenever hierarchy is enabled | Profile-derived maximum mip and specialized builder | Smaller subresource use or same texture with fewer writes | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Active tap/mip audit, UAV bytes, prep plus trace timing | Pending | Exact | Not applicable | Avoids work for unreachable levels without changing sampling. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| H-012 | Reuse compatible renderer Hi-Z | Supplied prompt | Depth preparation | Exact only if semantic identity is proven | No compatible resource | D-009 | None if reused | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Format/reduction/lifetime/value audit | Not proven | Pending | Not applicable | Reuse cannot override finite-interval depth semantics. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| H-013 | Point sampling selected depth mips | Supplied prompt; SAO/XeGTAO | Trace | Exact reference for hierarchy | Existing hierarchy uses integer `Load` at explicit mip | Preserve in every hierarchy profile | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Source/DXIL inspection | Present | Avoids cross-surface interpolation | Already equivalent | Required invariant |
| H-014 | Avoid trilinear depth mixing | Supplied prompt; SAO p.6 | Trace | Correctness constraint | Current explicit `Load` avoids filtering | No trilinear control | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Source inspection and mixed-surface fixtures | Trilinear would interpolate unrelated surfaces | Incorrect intervals | Blocked by correctness | Off would be free, but an enabled trilinear option violates finite-surface visibility semantics and has no defensible product/benchmark purpose |
| H-015 | Fixed per-tap mip selection | Supplied prompt; SAO/ASSAO | Trace | Exact for same mapping | Runtime thresholds | D-016/F-023 | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Mip identity and DXIL | Pending | Exact required | Not applicable | Requested inventory duplicate consolidated by cross-reference. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| H-016 | Runtime `log2` mip selection | Supplied prompt; XeGTAO | Trace | Algorithmic/Numerical | Fixed thresholds | D-015 | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | DXIL/mip/image/timing | Pending | Pending | Not applicable | More general selection can add SFU/integer work and changes LOD map. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| H-017 | GatherRed depth acquisition | Supplied prompt; XeGTAO/ASSAO/CACAO | Depth preparation/trace/filter | Exact if identical four texels and LOD | XeGTAO uses pinned depth gathers; the PS4 packed spatial profile uses four matching depth gathers for its 4x4 support | Retain gathers only in source-port/profile-specific PSOs with explicit component mapping | None beyond selected profile resources | None | Included in the XeGTAO and PS4 comparison paths; generic Reference direct depth remains unchanged | Texel-order fixtures, DXIL, sampler/cache, and timing | Strict XeGTAO compilation/profile tests pass. The controlled PS4 packed spatial median was 0.023552 ms versus 0.031744 ms scalar; all source-port profiles completed 600/600 frames. | Clean captures; D3D component order is explicit | Implemented as optional experiment | Gather use is now CPU-selectable and isolated, but its benefit remains kernel- and adapter-dependent. This row records depth gather acquisition; R-025 separately records the complete four-AO-plus-four-depth 4x4 reconstruction. |
| H-018 | Cache-safe maximum projected radius | Supplied prompt; Activision memo | Trace | Algorithmic | Unlimited radius | F-005/T-008 separate 32/64/128-pixel clamp PSOs | None | None | Low or negative at target radius 3 because the projected radius is usually below the clamps; potentially useful only in large-radius/cache-stress views. | Cache/miss and image/radius sweep | Three candidate clamp permutations are compiled and selectable; target timing and quality acceptance remain pending | May lose far occlusion | Implemented as optional experiment | The approximation is explicit rather than hidden: each clamp uses a separate CPU-selected shader and Reference retains the unlimited-radius shader with zero candidate cost. The 32/64/128 variants allow quality/cache sweeps without conflating the clamp with other hot-shader branches. |
| H-019 | Subpixel-radius early out | Supplied prompt | Trace | Algorithmic unless a zero-contribution proof is established | Sample distance is clamped to at least 0.5 pixel and duplicates may be skipped | Algorithmic small-radius shortcut or exact proof-based variant | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Radius/normal/thickness stress fixtures and timing | Pending | Pending | Blocked by correctness | A small projected radius can still represent local occlusion; no blanket exact claim. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| H-020 | Background or clear-depth early out | Supplied prompt; [SAO p.4](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=4); SSRT3 `CSMain` | Trace | Exact if all outputs have defined clear values | Current receiver reconstruction already rejects invalid/background depth | Preserve and specialize before scheduler/sample work | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Clear-depth fixtures for every consumer/output and divergence/timing | Existing rejection path present | Exact if auxiliary outputs initialized | Already equivalent | Verify every raw/GI/metadata output before claiming an earlier return |
| H-021 | Do not duplicate full-resolution linear-depth mip for half trace | Supplied prompt | Depth preparation | Exact resource optimization | Existing full hierarchy duplicates full-resolution linear depth as mip0 when active; the radius-3 target path instead reads device depth and builds no hierarchy | Half-base profile begins at trace resolution; direct-depth profile builds nothing | Removes full-resolution mip/write when a hierarchy is selected | None | Expected 0 ms at the radius-3 target because direct depth already builds nothing; potentially low-to-medium for large-radius hierarchy profiles by removing one full-resolution mip/write. | Resource bytes, prep traffic, value mapping | Not implemented for hierarchy-enabled half-resolution tracing | Exact relative to a validated half-base algorithm | Blocked by architecture | `screen_space_visibility.cpp` allocates the optional hierarchy at full output size, so mip 0 is a duplicate full-resolution linear-depth surface whenever that large-radius path is active. A half-base hierarchy needs a receiver-identity-preserving reduction and corresponding LOD remap; no such CPU-selectable path or experiment is present. It could be isolated with zero Reference cost, so this is a disclosed unmet implementation item rather than an impossibility or measured rejection. |
| H-022 | Preserve half-resolution receiver-surface identity | Supplied prompt | Depth/trace/history/reconstruction | Exact invariant | Canonical mapping is concrete; the PS4 closest-depth guide packs and carries the winning 2x2 source offset | Continue carrying selected source identity through every consumer whenever a reduction changes the receiver | Two-bit offset is packed into the conditional PS4 depth guide | None | Expected 0 ms incremental - active as a correctness invariant, not a standalone saving | Odd sizes, motion/history, edge, and reconstruction fixtures | Canonical paths retain concrete mapping. PS4 trace and temporal decode the packed offset, and spatial masks it before depth use; both controlled profiles completed 600/600 frames. | Required; clean final captures | Already equivalent | A smart-average value without a physical source cannot anchor a receiver. The PS4 repair now satisfies this invariant without a second metadata texture by preserving the selected source identity in the packed guide. |
| H-023 | SAO `findMSB` explicit mip calculation | Further research; [SAO p.3](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=3) | Trace | Exact relative to the same positive `floor(log2)` map | No runtime `log2` path | Integer `firstbithigh`/shift implementation in D-015 benchmark | None | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Exhaustive positive-distance mip equality, DXIL/timing | SAO uses it to avoid runtime log/filtering | Pending | Blocked by correctness | Useful only if runtime mip mode survives measurement. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| H-024 | SAO rotated-grid depth subsampling | Further research; SAO p.6 Table 1 | Depth preparation | Algorithmic | UVSR smart average | Benchmark reduction using a true source value without interpolation | Conditional hierarchy | None | Low or negative at target radius 3 - direct depth already has zero preparation pass, so preparation must repay its full cost. | Mask/image/cache and timing | SAO chose it at similar performance to mean reductions in its estimator | Pending | Blocked by correctness | A true sample avoids blended depth but changes which geometry is represented. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |

## Trace and Visibility

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| T-001 | Register-local 32-sector visibility mask | Supplied prompt; [visibility paper pp.3-7](https://arxiv.org/pdf/2301.11376#page=3) | Trace | Exact product architecture | One `uint` mask per pixel/slice | Preserve in every product profile | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Mask fixtures and DXIL register inspection | Present | Product invariant | Already equivalent | Paper found 32 sectors a quality/performance sweet spot that fits one scalar uint |
| T-002 | Horizon-GTAO benchmark control | Supplied prompt; Activision memo/deck | Trace | Algorithmic benchmark | No horizon product path | M-011 separate benchmark PSO/profile | Separate lazy PSOs | None | Diagnostic only - expected 0 ms product improvement; it isolates a cost floor or matched horizon reference. | Same reads/radius/output/application; trace-only and complete scope | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Different estimator by design | Implemented as benchmark-only control | Must never silently replace finite-thickness bitmask product AO. Implementation evidence: benchmark-only `HORIZON_GTAO_CONTROL`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources. |
| T-003 | Larger mask counts, including 128 sectors | Supplied prompt; visibility paper p.7 | Trace | Algorithmic benchmark | Fixed 32 sectors | Benchmark-only multiword mask PSO | Lazy PSO; more registers, no texture | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Instruction/register/occupancy, timing and sector-resolution captures | Paper found 128 sectors 5-10% slower due four uints/vector packing | Small quality gain reported as nearly invisible beyond 32 | Not applicable | Source evidence argues against production, but isolated benchmark remains technically possible. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| T-004 | Fixed sample permutations | Supplied prompt | Trace | Exact | Generic runtime loop | S-002 through S-027 | Lazy PSOs | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Fixed-order/mask/AO/GI/timing suite | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Central exact optimization family. Implementation evidence: fixed 8/12/16/20 wrappers. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| T-005 | Paired positive and negative depth samples | Supplied prompt; Activision deck slides 54-64; XeGTAO MainPass | Trace | Exact when sample order/ownership remains | Current loop processes both sides of one slice and shares slice setup | Preserve and hoist paired invariants; do not group all negatives before positives for GI | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Coordinate/order/owner fixtures and DXIL | Present structurally | Exact | Already equivalent | Pairing is already architectural; remaining win is invariant/load sharing |
| T-006 | Analytic projected pixel radius | Supplied prompt; Activision memo p.3 | Trace | Exact if equivalent | Two-endpoint projection | F-004 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Projection fixtures and timing | Pending | Pending | Blocked by correctness | Cross-reference records trace-list requirement. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-007 | Current two-endpoint radius projection | Supplied prompt | Trace | Exact reference | Existing method | Preserve Reference Generic | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Reference image and radius fixture | Present | Canonical | Already equivalent | Required fallback for unsupported projections and equivalence testing |
| T-008 | Maximum projected-radius clamp | Supplied prompt; Activision memo p.3 | Trace | Algorithmic | Unlimited Reference radius | F-005's explicit 32/64/128-pixel fixed-8 AO profiles | None | None | 32 px -0.50 to 0.30 ms; 64 px -0.20 to 0.22 ms; 128 px -0.12 to 0.14 ms forecast, non-additive with F-005. | Cache/image/radius sweep | All three packaged profiles completed the comprehensive Intel smoke and were directionally slower than Fixed 8; controlled timing and quality sweeps remain pending. | May omit distant occlusion | Implemented as optional experiment | The clamp is a named CPU-selected algorithmic profile rather than a hidden analytic-radius change. Reference keeps unlimited projection and incurs no candidate branch or resource. |
| T-009 | Cheap linear-depth view reconstruction | Supplied prompt; SAO p.3; XeGTAO reconstruction helpers | Trace | Exact or Numerical | General device-depth reconstruction | F-001 through F-003 | Optional prepared depth depending profile | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Position/mask/DXIL/timing | Pending | Pending | Blocked by correctness | Highest value when a prepared depth resource is already a net win. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-010 | Fixed quadratic sample positions | Supplied prompt; XeGTAO README | Trace | Exact | Runtime exponent branch selects multiplication at 2.0 | F-015/S-022 | None | None | Medium estimate - removes dynamic count/loop/exponent/adaptive control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Coordinate equality and DXIL | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Compile target exponent into key. Implementation evidence: the fixed x-squared block. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| T-011 | Precomputed sample distances | Supplied prompt; ASSAO/XeGTAO | Trace | Exact for fixed profile | Distance derived per sample from progressive jitter and radius | Compile invariant portion while retaining jitter dependence | Optional constant table | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Distance/coordinate equality, DXIL/timing | Pending | Exact required | Not applicable | Do not precompute away independent within-stratum jitter. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| T-012 | Fixed mip choices | Supplied prompt; SAO/ASSAO | Trace | Exact if same mapping | Runtime distance thresholds | F-023/H-015 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Mip identity and DXIL | Pending | Exact required | Not applicable | Applicable only to fixed radius/projection key or threshold-equivalent table. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| T-013 | Full-mask early termination | Supplied prompt; visibility paper Algorithm 1 | Trace | Exact | Reference loop checks `RadialVisibilityFullMask` | D-022 plus `ExactFullMaskEarlyExitOff` benchmark control | One lazy off-control PSO | None | Expected 0 ms incremental because the optimization is already active; the off delta measures its workload-dependent value. | Completing-sample GI fixture and timing | On is canonical; the packaged Off control completed the comprehensive Intel smoke. | Exact | Already equivalent | Required invariant: the On path exits only after final new-sector radiance is accumulated. The Off wrapper isolates performance without changing product output. |
| T-014 | Duplicate-coordinate rejection | Supplied prompt | Trace | Exact under current consecutive-side rule | Reference tracks the previous sample pixel per side | D-021 plus `ExactDuplicatePixelRejectionOff` benchmark control | One lazy off-control PSO | None | Expected 0 ms incremental because the optimization is already active; the off delta measures duplicate frequency and saving. | Duplicate/order/owner tests and timing | On is canonical; the packaged Off control completed the comprehensive Intel smoke. | Exact under the current rule | Already equivalent | The Off wrapper preserves sample order and estimator output. Broader dedup or reordered traversal remains outside this exact control. |
| T-015 | Trusted-input release specialization | Supplied prompt | Trace | Exact under validated contract | Full checks | F-024/F-025 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Invalid-input debug suite and DXIL | Pending | Exact on valid inputs | Blocked by correctness | Keep Reference and Debug variants. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-016 | Fast `acos` | Supplied prompt; Activision slide 63; XeGTAO | Trace | Numerical | Existing `VisibilityFastAcos` in projected-angle path | F-009 and optional fast-sqrt combination | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Mask-boundary and timing | Present in one path | Pending broader validation | Already equivalent | Inventory item exists; expose only where current estimator calls `acos` |
| T-017 | Fast square root | Supplied prompt; Activision/XeGTAO | Trace | Numerical | Native square root | F-011 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Error/mask/DXIL/timing | Pending | Pending | Blocked by correctness | Never use for view-position or interval length without mask validation. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-018 | `sincos` | Supplied prompt | Trace | Exact or Numerical | Separate direction math | F-006 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | DXIL/ULP/mask/timing | Pending | Pending | Not applicable | Compiler evidence decides retention. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| T-019 | Direction lookup | Supplied prompt; Activision fixed schedule | Trace | Numerical or exact relative to table-defined benchmark | Runtime evaluation | F-007 | Small constants | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Direction/mask/timing | Pending | Pending | Blocked by correctness | Table precision and scheduler definition determine classification. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-020 | Precomputed projection constants | Supplied prompt; XeGTAO | Trace | Exact | General helpers | F-002/F-020 | Constant-buffer data if required | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | DXIL/image/timing | Pending | Exact required | Blocked by correctness | Perspective target profile is suitable for specialization. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-021 | One inverse-length reuse | Supplied prompt; Activision | Trace | Exact | Redundant normalization | F-012 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Unit assertions and DXIL | Pending | Exact | Blocked by correctness | Shared normalized direction can feed front and back endpoint mapping. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-022 | Finite-thickness front/back interval preservation | Supplied prompt; visibility paper p.4 Algorithm 1 | Trace | Exact product invariant | Current `AccumulateOccluder` uses front/back interval | Preserve in every product bitmask PSO | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Endpoint/range-mask fixtures | Present | Product invariant | Already equivalent | Cannot be traded for infinite horizon thickness in product mode |
| T-023 | Alternative thin-occluder compensation | Supplied prompt; XeGTAO README | Trace | Algorithmic | Explicit constant thickness only | Separate thin-compensation profile, never combined silently | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Thin foliage/walls, halos/leaks and timing | Pending | Pending | Blocked by correctness | XeGTAO heuristic is horizon-specific; exact finite intervals already model front/back extent differently. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-024 | Near-field falloff | Supplied prompt; XeGTAO README | Trace | Algorithmic | Current sample distribution/thickness without XeGTAO falloff | Explicit algorithmic profile | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Contact/far occlusion captures and timing | Pending | Changes AO shape | Blocked by correctness | Estimator weighting changes even when samples match. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-025 | Small-radius early out | Supplied prompt | Trace | Algorithmic unless proven zero contribution | Current minimum 0.5-pixel sample distance/duplicates | H-019 | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Small-radius/thickness geometry suite | Pending | Pending | Blocked by correctness | Must not discard valid local occlusion based only on projected radius. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| T-026 | Compact AO output where valid | Supplied prompt | Trace/output | Exact for count encoding or Numerical for bounded R8 | R16F | P-010/P-012 | Conditional compact format | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Range/packing/image/timing | Popcount encoding exact for two estimators; not cosine-weighted mass | Pending | Blocked by architecture | Estimator gate is mandatory. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| T-027 | Boundary-safe O(1) interval mask construction | [Visibility paper p.4, Algorithm 1](https://arxiv.org/pdf/2301.11376#page=4) | Trace | Exact | Existing radial mask helper implements range accumulation | Shared helper with explicit zero/full-width handling and parentheses | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Zero/full width, negative clamp, exact endpoints, shift-by-32 tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Paper's half-sector round rule and angular-space sectors must be preserved without undefined shifts. Implementation evidence: safe zero/full-width range masks. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| T-028 | Skip GI light/normal loads when no newly covered sector | Visibility paper p.5; SSRT3 `HorizonSampling` | GI trace | Exact | Current shader computes `newSectorCount` and continues before source texture loads | Preserve and specialize minimal GI PSOs | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | DXIL/control-flow, logical loads and GI fixtures | Present | Exact | Already equivalent | Primary paper's key safe bandwidth optimization is already implemented |
| T-029 | Strict near-to-far new-sector ownership | Visibility paper Algorithm 1 | GI trace | Exact invariant | Rotated prefix is consumed by `firstbitlow`, sides interleave | Preserve in every GI/fixed/cache variant | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Source-owner fixtures with overlapping intervals | Present | Product invariant | Already equivalent | AO union may commute, but GI radiance ownership does not |
| T-030 | Claim visibility before radiance/facing rejection | Visibility paper Algorithm 1; current shader | GI trace | Exact invariant | `AccumulateOccluder` precedes source-light rejection | Preserve | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Dark/back-facing near occluder fixture | Present | Product invariant | Already equivalent | Dark geometry still blocks farther radiance; moving claim inside lighting acceptance is incorrect |
| T-031 | One slice per pixel | Supplied prompt; Activision and visibility paper | Trace | Exact product invariant | One randomized slice per receiver | Preserve | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Source inspection and profile validity | Present | Product invariant | Already equivalent | Multi-slice variants are separate algorithmic estimators and not required product replacements |
| T-032 | Quadratic radial distribution | Supplied prompt; XeGTAO | Trace | Exact product invariant | Exponent 2.0 target and multiplication fast path | Preserve; specialize through S-022 | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Coordinate fixtures | Present | Product invariant | Already equivalent | Product requirement and target profile |
| T-033 | Thirty-two versus larger sector performance control | Visibility paper p.7 | Trace | Algorithmic benchmark | 32 only | T-003 | Lazy PSOs | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Register/instruction/timing and image | Paper reports 128 sectors 5-10% slower | Pending UVSR confirmation | Not applicable | Keeps source finding in ledger without suggesting a product default change. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| T-034 | Radial off-screen early break or continue | SSRT3 `HorizonSampling` and current shader | Trace | Exact only if remaining farther samples are provably off-screen | Current code continues after each out-of-viewport sample | Direction/bounds proof benchmark | None | None | Low-to-medium estimate - reduces trace/filter ALU without reducing eight physical reads; scene dependence and overlap are high. | Projection/viewport fixtures and DXIL/timing | SSRT3 breaks; UVSR current scheduling and projection need proof | Pending | Blocked by correctness | A simple break can be exact along a monotonic ray, but jitter/coordinate mapping and both sides must be verified. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |

## Packed Edge Data

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-001 | Four 2-bit L/R/T/B edge values in R8 | Supplied prompt; XeGTAO `PackEdges`; ASSAO/CACAO | Trace/reconstruction | Numerical metadata feeding Algorithmic reconstruction | No edge metadata | Conditional edge-producing trace/depth PSO and packed-edge consumers | One R8 at trace resolution unless folded into P-012 | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | All 256 bytes, edge fixtures, bandwidth/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Four continuity levels fit one byte and enable cheap symmetric filters. Implementation evidence: conditional `r8ui` output. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-002 | Depth-only edge generation | Supplied prompt; XeGTAO | Trace/depth preparation | Algorithmic | Current filter evaluates continuous depth per tap | Edge profile from relative linear-depth differences | Packed edge output | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Reverse-depth/background/slope/hard-edge suite | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Lowest guide bandwidth; misses normal-only discontinuities. Implementation evidence: runtime edge variant 4. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-003 | Depth-plus-normal edge generation | Supplied prompt | Trace/depth preparation | Algorithmic | Current filter uses both guides continuously | `AlgorithmicPackedEdgesDepthNormal2x2` combines relative depth and normal continuity into R8_UINT edges during trace | Packed edge output; the trace already requires receiver normals | None | Forecast 0.06-0.27 ms complete-effect saving, non-additive with other packed-edge rows. | Normal-only/depth-edge fixtures, edge-byte inspection, timing | Candidate implemented; controlled Intel target timing is not yet measured. | Algorithmic quality review pending | Implemented as optional experiment | Runtime algorithm variant 5 selects `PACKED_EDGE_MODE=2`; the profile has its own CPU plan and trace/filter PSOs. Reference allocates no edge texture and selects no packed-edge pass. |
| E-004 | Slope-adjusted edges | Supplied prompt; ASSAO shader around `CalculateEdges` | Trace/depth preparation | Algorithmic | No prepacked slope compensation | `AlgorithmicPackedEdgesSlope2x2` uses the slope-aware trace producer and the same symmetric R8_UINT consumer | Packed edge output | None | Forecast 0.05-0.25 ms complete-effect saving, non-additive with other packed-edge rows. | Sloped-plane versus silhouette fixtures and timing | Candidate implemented; controlled Intel target timing is not yet measured. | Algorithmic quality review pending | Implemented as optional experiment | Runtime algorithm variant 6 selects `PACKED_EDGE_MODE=3`; this can reduce false edges on continuous slopes but is not claimed equivalent to continuous bilateral weights. Reference has zero candidate cost. |
| E-005 | Symmetric neighbor enforcement | Supplied prompt; XeGTAO/ASSAO/CACAO | Reconstruction | Algorithmic filter detail | Current continuous bilateral weights are evaluated per tap | Multiply/op-min opposite edge values for each adjacency | No extra resource | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Asymmetric-byte fixtures, edge leakage captures, timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Prevents one pixel alone from authorizing a cross-edge sample. Implementation evidence: bidirectional connectivity. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-006 | Controlled leakage near highly enclosed pixels | Supplied prompt; XeGTAO `Denoise` | Reconstruction | Algorithmic | Current joint bilateral has no XeGTAO leak rule | `AlgorithmicPackedEdgesLeakage2x2` selects depth-plus-normal edges and a separately compiled leakage consumer | No extra resource beyond edges | None | Forecast 0.04-0.24 ms complete-effect saving, non-additive with other packed-edge rows. | Enclosed corners/thin gaps, image and timing | Candidate implemented; controlled Intel target timing is not yet measured. | Deliberate leakage tradeoff; visual acceptance pending | Implemented as optional experiment | The packed filter is compiled with `PACKED_EDGE_CONTROLLED_LEAKAGE=1` only for this profile. The visible tradeoff remains isolated and Reference selects neither its PSO nor edge resource. |
| E-007 | Exact XeGTAO edge packing | Supplied prompt; XeGTAO `PackEdges` | Trace/reconstruction | Numerical/Algorithmic | No packed edges | R8_UINT pack/unpack PSO using XeGTAO's L/R/T/B two-bit ordering and quantization; no R8_UNORM storage permutation | E-001 R8_UINT texture | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Byte round-trip and denoise fixture | The byte ordering and quantization semantics are implemented in R8_UINT; the execution plan explicitly rejects R8_UNORM metadata. Controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | UVSR matches XeGTAO's logical packed-edge ordering but not its physical UNORM resource path; edge generation and filtering also remain UVSR-specific algorithmic changes. Reference has no candidate-only binding, resource, or dispatch. |
| E-008 | Alternative R8_UINT edge packing | Supplied prompt | Trace/reconstruction | Numerical/Algorithmic | No packed edges | P-020 integer pack/load profile | E-001 texture in UINT format | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Byte/DXIL/sampler/timing comparison | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Same quantized quality as identical codes | Implemented as optional experiment | Compare integer access against normalized conversion on target. Implementation evidence: R8_UINT storage. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-009 | Generate edges during half-depth preparation | Supplied prompt; CACAO/XeGTAO | Depth preparation | Exact scheduling relative to chosen edge formula | No edge pass | Depth-prep PSO emits depth and edge metadata while 2x2 data is resident | Edge UAV only for enabled profile | None | Medium estimate - one byte/pixel of edges can replace guide evaluation, but adds a trace write. | Prep total, logical reads, edge equality | Pending | Formula-dependent | Not applicable | Avoids a separate pass and duplicate guide loads. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| E-010 | Generate edges during AO trace | Supplied prompt; XeGTAO MainPass | Trace | Exact scheduling relative to chosen edge formula | Trace already has receiver depth but not all neighbor guides | Packed-edge trace PSO loads required half-grid neighbors and emits metadata | Edge or packed AO/edge UAV | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Trace total, logical reads and edge equality | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Formula-dependent | Implemented as optional experiment | May reuse center data; compare against depth-prep generation. Implementation evidence: trace-time edge output. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-011 | Separate full-resolution edge-generation pass | Supplied prompt prohibition | Reconstruction | Algorithmic with avoidable pass | None | No control | Full-resolution edge texture/pass would be required | None if absent | Medium estimate - one byte/pixel of edges can replace guide evaluation, but adds a trace write. | Architecture traffic estimate | Prompt explicitly forbids it and source filters generate metadata earlier | No quality need unique to this architecture | Blocked by architecture | The same experiment can be isolated in trace/depth prep with zero off cost; a full-resolution producer defeats the stated bandwidth objective |
| E-012 | Four continuity levels near 0, 0.33, 0.66, and 1 | Supplied prompt | Edge quantization | Numerical/Algorithmic | Continuous weights | Standard two-bit decode profile | No extra beyond E-001 | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | All codes and filter-response tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Quantized transitions | Implemented as optional experiment | Matches requested representation; exact thresholds remain a tunable algorithmic choice. Implementation evidence: two-bit continuity quantization. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| E-013 | AO count and edges in one R16_UINT | Supplied prompt; visibility paper/XeGTAO | Trace/reconstruction | Exact AO for compatible estimator plus Algorithmic edges | Separate R16F AO, no edges | P-012 | Replaces AO resource; no extra bytes | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Count/byte round-trip, mask/AO and edge tests | Feasible for popcount estimators | Pending edges | Blocked by architecture | Avoids an extra edge allocation/load where mathematically valid. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |

## Spatial Reconstruction

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | Current compact 2x2 joint bilateral | Supplied prompt | Reconstruction | Exact reference | Mandatory reduced-resolution resolve even when Spatial Filtering is off | Preserve Legacy Compact profile | Full-resolution R16F final AO | Existing reference cost | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Baseline median/p95 and reference image | Source inspection estimates about 145 MB/frame uncached traffic floor and expensive per-tap math | Canonical | Already equivalent | Required reference and first exact fusion kernel |
| R-002 | Current Gaussian joint bilateral | Supplied prompt | Reconstruction | Exact reference | Optional Gaussian kernel | Preserve Legacy Gaussian profile | Full-resolution R16F final AO | Existing reference behavior | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Baseline timing/image | Present | Canonical optional filter | Already equivalent | Reference for precomputed-weight and filter-matched FAST work |
| R-003 | Exact current upsample fused with composition | Supplied prompt | Reconstruction/application | Exact with half-rounding | Separate resolve, intermediate, composite | M-008/D-027 | Removes final AO texture | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | Pixel comparison and complete-effect timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented as optional experiment | First mandatory fusion experiment must not alter filter equations. Implementation evidence: the exact fused profile. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-004 | Packed-edge 2x2 reconstruction | Supplied prompt | Reconstruction | Algorithmic | Continuous compact bilateral | Edge-aware 2x2 PSO | Packed edges | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Edge suite, capture, timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Minimal packed-edge replacement with same footprint, not same weights. Implementation evidence: `PACKED_EDGE_RECONSTRUCTION=1`. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-005 | Packed-edge 4x4 reconstruction | Supplied prompt | Reconstruction | Algorithmic | 2x2 compact or larger Gaussian | Four-gather/loads or tiled packed-edge PSO | Packed edges | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Quality/timing/cache and edge suite | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Larger support can exploit complementary scheduler phases but costs more taps. Implementation evidence: `PACKED_EDGE_RECONSTRUCTION=2`. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-006 | Activision-style 4x4 reconstruction | Supplied prompt; Activision deck slides 94-96 | Reconstruction | Algorithmic | PS4 scalar and packed profiles run the same derivative-aware alternating 4x4 kernel | Retain scalar as the readable oracle and packed gathers as the performance variant | Conditional prepared depth and spatial AO | None | Included in the full PS4 comparison; packed-versus-scalar forecast -0.08 to 0.25 ms | Same-engine timing and captures | Both profiles completed 600/600 controlled frames. Scalar spatial median was 0.031744 ms and packed was 0.023552 ms; packed saved 1.338% complete median and 7.845% p95 versus scalar. | Clean captures; source-informed 10% linear depth ramp and disclosed first derivative | Implemented as optional experiment | The coupled profiles use the published 4x4-by-6 schedule, alternating asymmetric footprint, linear relative-depth weighting, spatial output, temporal history, and required upsample. Shipping curvature constants are unpublished, so the derivative adapter remains explicit rather than being called exact PS4 code. |
| R-007 | XeGTAO-style gather denoising | Supplied prompt; [pinned XeGTAO `Denoise`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Reconstruction | Algorithmic | One sharp nine-tap edge-weighted source pass is implemented | Retain one pass in High; add repeated passes only as explicit no-TAA quality variants | One R8 edge and one R16F working AO plane | None | Included in M-010 | Tap/order comparison, timing, and edge/image suite | One pass completed 600/600 controlled frames in all three profiles; spatial medians were 0.029696, 0.028672, and 0.023552 ms. Repeated passes remain unimplemented. | Clean one-pass captures; dynamic-motion review remains pending | Implemented as optional experiment | The documentation does not mislabel one pass as a 25-tap filter. Removing the unused second AO plane is correct only while one pass remains selected; the static evidence does not justify extra passes. |
| R-008 | Five-by-five packed-edge spatial filter | Supplied prompt; XeGTAO README | Reconstruction | Algorithmic | Gaussian/compact | Repeated edge-denoise or explicit 5x5 PSO | Packed edges | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Support/timing/image comparison | XeGTAO documentation describes effective 5x5 | Pending | Blocked by architecture | Compare repeated compact passes against one explicit neighborhood. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| R-009 | Half-resolution spatial resolve followed by full-resolution apply | Supplied prompt | Reconstruction/application | Algorithmic unless half filter already defines product result | Current filtering/upsample happens at full output | Half-grid 4x4 filter then fused full-resolution 2x2 apply | Optional half filtered AO; no final full-res AO | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Half filter plus apply total, quality and traffic | Pending | Pending | Blocked by architecture | Performs expensive support once per half pixel, then cheap full-resolution application. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| R-010 | One full-resolution gather, upsample, and apply pass | Supplied prompt | Reconstruction/application | Algorithmic | Separate resolve/composition | `AlgorithmicFusedPackedEdges4x4` performs the packed 4x4 source resolve and AO application in one full-resolution dispatch | No final AO; one conditional R8_UINT edge texture | None | Uncertain forecast: -0.18 ms regression to 0.25 ms saving; eliminating a pass may not repay sixteen source evaluations per full-resolution pixel. | Compare against separate packed 4x4 and exact fused 2x2; full-resolution tap/traffic/image timing | Candidate implemented; controlled Intel target timing is not yet measured. | Algorithmic quality review pending | Implemented as optional experiment | `FUSED_PACKED_EDGE_RECONSTRUCTION=2` is a distinct fused shader macro and application plan. It proves the requested one-pass architecture without assuming it wins; Reference has no edge texture, fused PSO, or candidate dispatch. |
| R-011 | Group-shared AO/edge tile | Supplied prompt; CACAO | Reconstruction | Exact implementation of selected filter | Direct texture access | D-024 tiled packed-edge or legacy-kernel PSO | LDS only | None | Medium-to-high estimate - reconstruction occupies the reported roughly 1.1 ms bucket; wider kernels may instead be slower. | Bank conflicts/occupancy/image/timing | Pending | Exact if tap math retained | Blocked by architecture | Candidate for repeated neighborhood reuse. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| R-012 | Two-output thread coarsening | Supplied prompt; XeGTAO | Reconstruction | Exact implementation of selected filter | XeGTAO denoise produces two horizontal outputs per invocation; generic reconstruction remains one output/thread | Retain the source-port form and treat generic transfer separately | None | None | Included in M-010; wider generic kernels may still regress from live-state pressure | Register/cache/image/timing | All three Xe profiles completed 600/600 controlled frames with the two-output denoiser and clean captures. | Exact for the pinned source-port kernel | Implemented as optional experiment | The implementation shares overlapping gather data for two horizontal pixels and is CPU-selectable through the Xe profiles. No claim is made for the absent generic compact/Gaussian transfer or Xe-LPG occupancy. |
| R-013 | Four-output thread coarsening | Supplied prompt; CACAO | Reconstruction | Exact implementation of selected filter | One output/thread | D-026 | None | None | Medium-to-high estimate - reconstruction occupies the reported roughly 1.1 ms bucket; wider kernels may instead be slower. | Register/occupancy/image/timing | Pending | Exact if retained kernel | Blocked by architecture | More reuse, higher live state. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| R-014 | Cheap relative-depth weights | Supplied prompt; Activision slides 94-96; XeGTAO edges | Reconstruction | Algorithmic | The PS4 scalar and packed spatial shaders use a plane-predicted linear relative-depth ramp; canonical filters retain their own weights | Retain the 10% ramp only inside the coupled PS4 comparison | Prepared depth already required by the PS4 profile | None | Included in R-006; wider support and algorithmic response can outweigh cheaper weight math | Depth-scale/edge captures and timing | Both PS4 variants completed 600/600 controlled frames with clean captures. The packed spatial median was 0.023552 ms versus 0.031744 ms scalar. | Source-informed linear depth response; algorithmic versus product filters | Implemented as optional experiment | `AccumulateTap` evaluates `saturate(1 - relativeError * 10)` against a first-order depth prediction. This implements the disclosed cheap weight inside PS4 without presenting it as an exact replacement for UVSR's bitmask reconstruction. |
| R-015 | Squared normal-dot weights | Supplied prompt | Reconstruction | Algorithmic/Numerical | `pow(normalDot,16)` | Low-order squared normal weight profile distinct from exact x16 squaring | None | None | Medium-to-high estimate - reconstruction occupies the reported roughly 1.1 ms bucket; wider kernels may instead be slower. | Angular response/capture/timing | Pending | Broader normal acceptance | Blocked by correctness | If exponent remains 16 use F-014 Exact; reducing exponent is Algorithmic. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| R-016 | Linear saturating weights | Supplied prompt; Activision | Reconstruction | Algorithmic | The PS4 profiles implement the disclosed linear saturating depth weight; no generic normal/edge replacement is exposed | Retain the depth-weight component in PS4; treat any normal/edge extension as a separate unimplemented experiment | None beyond selected PS4 resources | None | Included in R-006; response quality dominates any isolated ALU saving | Response plots, edges, and timing | The linear depth ramp ran in both 600-frame controlled PS4 profiles; final captures were clean. | Algorithmic threshold response; normal/edge extension remains unimplemented | Implemented as optional experiment | The implemented component reaches zero at 10% relative depth and deliberately does not invent unpublished second-derivative or normal-weight constants. It is isolated from Reference and should not be generalized to other filters. |
| R-017 | `exp2` weights | Supplied prompt | Reconstruction | Numerical | `exp` | F-017 | None | None | Medium-to-high estimate - reconstruction occupies the reported roughly 1.1 ms bucket; wider kernels may instead be slower. | Error/DXIL/image/timing | Pending | Pending | Blocked by correctness | Separate numerical experiment from linear/packed-edge algorithms. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| R-018 | Precomputed spatial weights | Supplied prompt | Reconstruction | Numerical | Gaussian evaluates a fixed spatial factor with runtime `exp` | Conservative FP32 filter selects the same 16-value offline-rounded table as F-019 | Small shader constant table | None | Part of the 0.02-0.18 ms conservative filter-family forecast; non-additive with F-019/F-021. | FP32 weight/DXIL/image/timing | Candidate implemented; controlled Intel target timing is not yet measured. | Radius cancellation is algebraic, but offline FP32 rounding/reassociation is not claimed bit-exact. | Implemented as optional experiment | `FILTER_EXACT_FAST_MATH` indexes `diskGaussianWeight[16]`; Reference retains runtime `exp`. The profile is correctly labeled Conservative Numerical and selects no table path while off. |
| R-019 | No per-tap view-position reconstruction | Supplied prompt; Activision packed-depth filter | Reconstruction | Algorithmic unless an algebraically equivalent depth metric is used | Reconstructs each guide tap | Relative-linear-depth or derivative edge profile | None | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Weight equivalence where claimed; quality/timing otherwise | Both PS4 variants completed 600/600 controlled frames. The spatial pass used linear depth and first-order slope without per-tap view-position reconstruction; packed measured 0.023552 ms versus 0.031744 ms scalar. | Clean captures; algorithmic relative-depth metric | Implemented as optional experiment | Label exact only if the current plane-distance equation is reproduced without position. This CPU-selected PS4 implementation intentionally changes the metric and leaves Reference bindings, resources, and dispatches untouched. |
| R-020 | No tangent-frame projection | Supplied prompt | Reconstruction | Algorithmic unless algebraically eliminated | Current Gaussian projects into tangent frame | Fixed screen-space/edge filter profile | None | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Kernel response/image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Removing it usually changes world-space radius behavior. Implementation evidence: packed reconstruction without tangent projection. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-021 | No per-tap `pow` | Supplied prompt | Reconstruction | Exact with F-014 or Algorithmic with changed normal weight | Current compact uses `pow(x,16)` | Repeated-squaring exact-fast or packed-edge profile | None | None | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL/weight/image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Classification depends on selected replacement | Implemented as optional experiment | Keep exact and algorithmic controls distinct. Implementation evidence: exact repeated squaring. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-022 | No two-exponential product | Supplied prompt | Reconstruction | Exact apart from reassociation with F-016 | Current compact multiplies two exponentials | Combined-exponent exact-fast PSO | None | None | Medium estimate - removes reconstruction, `pow`, or special-function work from the reported roughly 1.1 ms mandatory resolve; gains overlap. | DXIL/ULP/image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Expected equivalent | Implemented as optional experiment | Eliminates one special-function evaluation. Implementation evidence: combined exponent. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| R-023 | Bilinear reconstruction diagnostic | Supplied prompt | Reconstruction | Diagnostic | No direct bilinear profile | M-006 | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; it isolates a cost floor or matched horizon reference. | Timing and leakage capture | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Diagnostic artifacts expected | Implemented as benchmark-only control | Required null floor. Implementation evidence: bilinear diagnostic. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources. |
| R-024 | Nearest reconstruction diagnostic | Supplied prompt | Reconstruction | Diagnostic | No direct nearest profile | M-007 | One lazy PSO | None | Diagnostic only - expected 0 ms product improvement; it isolates a cost floor or matched horizon reference. | Timing and blockiness capture | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Diagnostic artifacts expected | Implemented as benchmark-only control | Required null floor. Implementation evidence: nearest diagnostic. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources. |
| R-025 | Four GatherRed operations for a 4x4 source neighborhood | Supplied prompt | Reconstruction | Exact implementation for identical texels | The PS4 scalar oracle uses 16 AO plus 16 depth loads | Packed PS4 reconstruction uses four AO and four matching depth gathers | None beyond the selected PS4 resources | None | Included in R-006; gather lowering and cache behavior remain adapter-dependent | API gather-order fixture, DXIL/sampler/cache/timing | The packed shader implements D3D's LL/LR/UR/UL component order and completed 600/600 controlled frames. Spatial median fell from 0.031744 to 0.023552 ms and complete median improved 1.338% versus scalar. | Same intended texel/kernel set; clean captures | Implemented as optional experiment | Four AO gathers cover the 4x4 AO neighborhood; four matching depth gathers preserve the relative-depth weights. The optimization is isolated in a CPU-selected PS4 profile with zero Reference cost. Prefer packed locally, while Xe-LPG gather behavior remains unmeasured. |
| R-026 | Activision linear depth plus first/second derivative guides | Activision slides 94-96 | Reconstruction | Algorithmic | The PS4 spatial shaders reconstruct an on-the-fly first derivative from valid prepared-depth neighbors; no second-derivative guide is implemented | Keep the validated first-order adapter; require published or independently justified curvature semantics before adding second order | Prepared depth only for first order; a future second-order form may need more metadata | None | First-order cost is included in R-006; second-order impact is unknown | Silhouette/sloped-plane quality and timing | First-order central/one-sided slopes ran in both 600-frame controlled PS4 profiles with clean captures. The shipping second-derivative formula and constants are unpublished. | First order source-informed; second order unproven | Blocked by architecture | The block applies only to the missing second-derivative extension, not to the implemented first-order guide. `EstimateDepthSlope` excludes invalid sentinels and uses central or one-sided derivatives; inventing curvature terms would risk silhouette leakage and would falsely imply shipping-code parity. |
| R-027 | ASSAO compact five-sample edge blur | [ASSAO shader `SmartBlur`](https://github.com/GameTechDev/ASSAO/blob/master/Projects/ASSAO/ASSAO/ASSAO.hlsl#L917) | Reconstruction | Algorithmic | UVSR kernels differ | Benchmark edge-blur pass or tiled equivalent | Packed AO/edges | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Timing/support/quality | Primary source supports 1-6 passes | Pending | Blocked by architecture | Relevant compact packed-edge alternative; pass count changes effective support. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| R-028 | CACAO multi-iteration LDS edge blur | [CACAO shader lines 168-412](https://github.com/GPUOpen-Effects/FidelityFX-CACAO/blob/master/ffx-cacao/src/ffx_cacao.hlsl#L168) | Reconstruction | Exact scheduling for selected Algorithmic filter; Numerical FP16 storage | No equivalent | Specialized Blur1 through Blur8 PSOs running iterations in one LDS dispatch | LDS and packed half values only | None | Medium-to-high estimate - replaces repeated guide/SFU work or merges metadata in the reported roughly 1.1 ms resolve; quality/cache dominate. | Iteration-equality, LDS conflicts, occupancy and timing | Primary source vectorizes four pixels and retains tile in LDS | Pending | Blocked by architecture | Can amortize dispatch/global traffic if register/LDS occupancy remains healthy. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |

## Temporal Reconstruction

This section is historical experiment inventory. The current build exposes no
visibility-owned temporal accumulation setting or diagnostic/source profile;
renderer TAA owns temporal stability. The **Removed Feature Disposition** table
overrides rows below that describe a former control as implemented or retained.

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Q-001 | Current separate temporal pass | Supplied prompt | Temporal | Exact reference | Nearest history, depth plus normal validation, four diagonal current bounds, 25% pull, configurable response | Preserve Reference temporal PSO and histories | Existing conditional histories | None when temporal is disabled | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Baseline median/p95 and temporal capture | Present; user reports about 1.0 ms but controlled reproduction is pending | Canonical | Already equivalent | Required comparison path; no suitable shipping scene TAA exists |
| Q-002 | Temporal copy diagnostic | Supplied prompt | Temporal | Diagnostic | No copy-only path | M-005 | Existing temporal outputs only | None | Diagnostic only - expected 0 ms product improvement; it isolates a cost floor or matched horizon reference. | Timing floor | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | No temporal stability | Implemented as benchmark-only control | Separates pass/read/write cost from reprojection and validation. Implementation evidence: `TEMPORAL_COPY_DIAGNOSTIC`. It is benchmark-only; Reference selects the canonical product path with no diagnostic work/resources. |
| Q-003 | Combined spatial and temporal resolve | Supplied prompt | Reconstruction/temporal | Algorithmic unless exact current stages are fused with preserved quantization/order | Separate temporal then filter | One pass gathers current neighborhood, computes spatial estimate/bounds/confidence, reprojects, validates, clamps/pulls, blends, writes one result | Existing histories; may remove intermediate | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Stage and complete timing, exact/quality capture depending math | Pending | Pending | Blocked by architecture | Can share current-neighborhood reads and remove an intermediate dispatch. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| Q-004 | Reuse gathered current samples for min/max bounds | Supplied prompt | Temporal | Exact if current four diagonal samples are included with identical values | Temporal rereads four diagonals separately | Combined-resolve PSO derives bounds from already gathered current taps | None | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Bound equality fixtures, logical reads, image/timing | Pending | Exact only if neighborhood matches | Blocked by architecture | Do not silently substitute a different support set. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| Q-005 | Remove four extra diagonal reads | Supplied prompt | Temporal | Exact through Q-004 or Algorithmic if omitted | Four diagonal reads define current bounds | Combined-resolve reuse; separate omission only as algorithmic experiment | None | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Bound/image and timing comparison | Pending | Pending | Blocked by architecture | Reads may be removed only after their information is reused or intentionally approximated. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| Q-006 | Bilinear history sampling | Supplied prompt; Activision slides 97-102 | Temporal | Algorithmic | Both PS4 profiles manually reconstruct bilinear history and validate every contributing AO/depth sample before accepting its weight | Retain inside the coupled Activision temporal PSO | Existing PS4 AO/depth histories | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Disocclusion/motion/flicker captures and timing | Both profiles completed 600/600 controlled frames. The shader validates four history samples independently, renormalizes accepted weights, and rejects invalid reprojection/depth. | Clean static captures; dynamic motion/disocclusion stress remains pending | Implemented as optional experiment | Manual bilinear reconstruction avoids blending invalid history before validation. The implementation is source-informed and isolated to PS4 profiles; it does not change canonical temporal behavior. |
| Q-007 | Nearest history sampling | Supplied prompt | Temporal | Exact reference | Current behavior | Preserve Reference | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Source inspection | Present | Canonical | Already equivalent | Baseline for bilinear comparison |
| Q-008 | Current R16F AO history | Supplied prompt | Temporal | Exact reference | R16F ping-pong | Preserve Reference | Existing histories | None when temporal off | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection | Present | Range-preserving baseline | Already equivalent | Required for unbounded estimators |
| Q-009 | R16F depth history | Supplied prompt | Temporal | Numerical | R32F ping-pong | P-016 | Smaller conditional histories | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Validation error/motion/timing | Pending | Pending | Blocked by correctness | Bandwidth reduction with disocclusion precision tradeoff. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| Q-010 | R32F depth history | Supplied prompt | Temporal | Exact reference | Current behavior | Preserve Reference | Existing histories | None when temporal off | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection | Present | Canonical | Already equivalent | Reference for P-016 |
| Q-011 | RGBA8 normal history | Supplied prompt | Temporal | Exact reference | Current behavior | Preserve Reference | Existing histories | None when temporal off | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Resource inspection | Present | Canonical | Already equivalent | Reference for compact/no-normal experiments |
| Q-012 | RG8 octahedral normal history | Supplied prompt | Temporal | Numerical | RGBA8 | P-018 | Smaller histories | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Angular/validation/motion/timing | Pending | Pending | Blocked by correctness | Compact but decode cost and quantization require measurement. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| Q-013 | No normal history | Supplied prompt | Temporal | Algorithmic | PS4 temporal uses AO plus R32F depth history and does not allocate or bind normal history | Retain only in the explicit PS4 profiles; canonical temporal keeps depth plus normal | Removes normal history for PS4 only | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Normal-only disocclusion and timing | Both PS4 variants completed 600/600 controlled frames with the no-normal-history layout. | Clean static captures; ghosting risk remains a dynamic-motion gate | Implemented as optional experiment | This is an explicit algorithmic profile contract, never an implication of a precision mode. It saves the normal-history surface/read only while PS4 is selected and leaves Reference unchanged. |
| Q-014 | Confidence or history-length storage | Supplied prompt | Temporal | Algorithmic | Fixed response, no persistent confidence/length | Optional compact confidence channel/history | Conditional history channel/texture | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Convergence, motion/ghosting and timing | Pending | Pending | Blocked by architecture | Can adapt response but adds traffic/state. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| Q-015 | Reuse previous scene depth | Supplied prompt | Temporal | Exact only if resolution, convention, lifetime, and sample identity match | Dedicated R32F history is written | CPU-selected compatible scene-depth history | Removes dedicated depth history if compatible | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Resource/value/reprojection audit and timing | No proven compatible history at base | Pending | Not applicable | Do not bind a superficially similar but differently jittered depth buffer. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| Q-016 | Depth-only temporal validation | Supplied prompt | Temporal | Algorithmic | PS4 validates four bilinear history contributors against the current packed linear depth; canonical temporal still uses depth plus normal | Retain inside PS4 and preserve canonical validation elsewhere | Removes normal reads/history for PS4 only | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Disocclusion/normal-change suite | Both PS4 variants completed 600/600 controlled frames. The temporal shader applies a symmetric 10% relative-depth test and renormalizes only accepted bilinear contributors. | Clean static captures; more false acceptance at normal-only changes remains possible | Implemented as optional experiment | The bandwidth reduction is real and profile-scoped, but dynamic normal-only and disocclusion stress remains required before any production use. Reference allocates and validates its normal history normally. |
| Q-017 | Depth-plus-normal temporal validation | Supplied prompt | Temporal | Exact reference | Current behavior | Preserve Reference | Existing histories | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Source and fixture inspection | Present | Canonical | Already equivalent | Product reference |
| Q-018 | Packed-edge temporal validation | Supplied prompt | Temporal | Algorithmic | Continuous depth plus normal validation | Edge/confidence-guided history validation profile | Packed edges and possibly history edge metadata | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Motion/disocclusion/normal-edge suite and timing | Pending | Pending | Blocked by architecture | Reuses compact topology but changes acceptance semantics. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| Q-019 | Temporal-response sweep | Supplied prompt | Temporal | Algorithmic parameter | Configurable response exists | Benchmark action over curated responses with history reset | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Static convergence, motion lag, flicker and timing | Control exists; systematic sweep pending | Quality tradeoff | Already equivalent | Parameter is already exposed; required work is controlled evidence |
| Q-020 | History pull versus hard clamp | Supplied prompt; Activision slides 99-102 | Temporal | Algorithmic | Canonical temporal uses its current pull; the PS4 approximation implements a source-informed diagonal low-pass bound with a 25% history pull | Expose a separate hard-clamp comparison only if dynamic validation warrants it | None beyond the selected temporal path | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Ghosting/flicker/motion captures | The PS4 low-pass pull completed 600/600 controlled frames with clean static captures; a distinct hard-clamp permutation is not implemented. | Low-pass variant implemented; hard-clamp and dynamic-motion comparison remain pending | Blocked by architecture | This row retains the unresolved explicit comparison family rather than relabeling its hard-clamp member as complete. Q-026 records the implemented PS4 low-pass behavior; the remaining block applies to a separately selectable hard-clamp control and dynamic quality evidence. |
| Q-021 | Six-phase accumulation | Supplied prompt; Activision schedule | Temporal/noise | Algorithmic | Both PS4 profiles pair the six published bidirectional axes with persistent AO/depth history | Retain the coupled Activision scheduler, reconstruction, and EMA profile | Conditional AO/depth history and motion input | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Six-phase convergence/flicker capture | Both profiles completed 600/600 controlled frames after a 120-frame warm-up. One direction is evaluated per frame and the six-phase history was stable by measurement. | Filter/scheduler coupled; clean final captures | Implemented as optional experiment | `ActivisionTemporalRotation` supplies six distinct axes over `[0, pi)`, and `screen_space_visibility_activision_temporal_cs.hlsl` accumulates reprojected AO/depth history. The coupled path is an algorithm comparison, not an isolated temporal production optimization. |
| Q-022 | TAA-assisted AO accumulation | Supplied prompt | Temporal | Algorithmic integration | No proven shipping scene TAA | No current control | None | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket. | Architecture inspection | Required dependency does not exist | Not currently evaluable | Not applicable | Prompt explicitly forbids assuming TAA support; revisit only after a suitable scene TAA exists |
| Q-023 | Activision alternating asymmetric filter origin | Activision slides 97-98 | Temporal/reconstruction | Algorithmic | The PS4 spatial shader alternates `[-2,+1]` and `[-1,+2]` 4x4 footprints by frame parity | Retain inside the coupled PS4 profiles | None beyond the selected spatial pass | None | Included in the PS4 comparison | Phase fixture, flicker/ghosting, and timing | Scalar and packed implementations share the alternating origin and completed 600/600 controlled frames each; packed spatial median was 0.023552 ms versus 0.031744 ms scalar. | Clean captures; coupled to the 4x4 pattern and EMA | Implemented as optional experiment | `footprintStart` is `-2` on odd frames and `-1` on even frames, matching the published top/left versus bottom/right alternation. Reference selects neither the pass nor this frame-dependent kernel. |
| Q-024 | Activision motion-responsive convergence | Activision slides 97-102 | Temporal | Algorithmic | The PS4 temporal shader increases current-frame weight with reprojected motion magnitude and rejects invalid motion/history | Retain inside the coupled PS4 profiles | Motion is bound only for the selected temporal layout | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Static/motion response and ghosting | Both profiles completed 600/600 controlled static-scene frames; the shader computes `saturate(length(motion.xy) * 0.125)` and blends the configured response toward full current-frame weight. | Static captures clean; dynamic motion/disocclusion stress remains pending | Implemented as optional experiment | Motion-responsive convergence is present and CPU/profile-selected rather than absent. The static controlled run proves runtime coverage, not moving-object quality; retain dynamic stress as the explicit remaining gate. |
| Q-025 | Activision one-sided disocclusion depth test | Activision slides 97-102 | Temporal | Algorithmic | The repaired PS4 approximation deliberately uses a symmetric 10% relative-depth test | Add a signed one-sided control only after adapting and validating the UVSR depth convention | None | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket | Moving occluder/disocclusion fixtures under depth convention | The current symmetric rule completed both 600-frame static runs; the one-sided source rule is not implemented or dynamically validated. | Dynamic disocclusion evidence pending | Blocked by correctness | The deliberate deviation avoids accepting stale background when new foreground arrives, which previously risks dark trails. The source's one-sided rule cannot be copied until its sign and motion behavior are proven under UVSR's positive linear-depth convention; this is an explicit approximation gap, not a hidden claim of parity. |
| Q-026 | Activision low-pass neighborhood clamp from four diagonal bilinear fetches | Activision slides 99-102 | Temporal | Algorithmic | The PS4 temporal shader derives neighborhood bounds from four diagonal half-resolution taps and pulls history 25% toward those bounds | Retain the source-informed low-pass pull inside the coupled profile | None beyond the selected temporal pass | None | Included in the PS4 comparison; no primary temporal-off product saving is claimed | Ghosting/flicker and timing | Both profiles completed 600/600 controlled frames and clean static captures with the diagonal neighborhood pull active. | Source-informed adaptation; dynamic motion/disocclusion stress remains pending | Implemented as optional experiment | The current shader implements the intended diagonal low-pass clamp behavior with four explicit diagonal taps, then applies a 25% history pull. It does not claim the unpublished shipping filter or literal bilinear instruction footprint; Reference selects no such temporal work. |
| Q-027 | Velocity-agreement adaptive clamp width | Activision slides 99-102 | Temporal | Algorithmic | PS4 consumes motion for reprojection and response, but does not compare velocity agreement to widen/tighten the clamp | Optional Activision-like mode: agreement widens, disagreement tightens, camera rotation fallback tight | Additional velocity-agreement inputs/logic only while selected | None | Expected 0 ms in the primary temporal-off profile; medium-to-high only when enabled against the user-reported roughly 1 ms temporal bucket | Object/camera motion suite and timing | Not implemented; the controlled static captures cannot evaluate it. | Explicit remaining Activision approximation gap | Blocked by architecture | This feature is distinct from Q-024's implemented motion-responsive convergence. It needs a defined object/camera velocity-agreement contract, its own PSO/layout, and dynamic quality evidence before exposure; Reference must retain zero inactive cost. |

## Full-Resolution Pass Fusion

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| U-001 | Exact compact 2x2 resolve fused with AO composition | Supplied prompt | Reconstruction/application | Exact with R16F round-trip emulation | Separate filter and composite | M-008/R-003 | Fused PSO; removes a texture/pass | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | Exact HDR image and complete-effect median/p95 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented as optional experiment | Mandatory first experiment; equations and ordering stay unchanged. Implementation evidence: the exact fused shader and half round-trip. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| U-002 | Omit `FinalAmbientVisibility` allocation in fused mode | Supplied prompt | Resource planning | Exact | Full-resolution R16F always exists when reduced output needs reconstruction | Profile resource plan omits it | Removes about 3.96 MiB at 1080p | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | Allocation assertions and GPU capture | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented as optional experiment | Reduced output needing reconstruction does not imply needing an intermediate texture. Implementation evidence: fused resource omission. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| U-003 | Omit full-resolution AO store, read, transition, and filter dispatch | Supplied prompt | Reconstruction/application | Exact | Separate passes and UAV-to-SRV transition | Fused profile plan | Removes traffic/dispatch/barrier | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | Dispatch/transition and logical-byte counters; timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented as optional experiment | Realizes the fusion benefit rather than leaving hidden work active. Implementation evidence: fused pass plan. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| U-004 | Reuse full-resolution depth and normal values in fused registers | Supplied prompt | Reconstruction/application | Exact | Filter reads guides; composition rereads normal/material separately | Fused PSO retains decoded guide values where equations share them | None | None | High estimate - removes a 1080p R16F AO store plus read, about 7.9 MiB/frame, and a dispatch/transition; overlaps all fusion rows. | DXIL/register count, logical loads, exact image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented as optional experiment | Reuse must not raise register pressure enough to erase traffic savings. Implementation evidence: shared center guide values. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| U-005 | Explicit debug full-resolution AO output | Supplied prompt | Debug reconstruction | Exact diagnostic | Normal reference path produces the texture, but fused mode omits it | `Fused Debug Full-Resolution AO Output` uses the same generic trace/bindings and exact legacy resolve math while restoring the R16F output and separate composition | Full-resolution R16F only while selected | None | Diagnostic only - expected 0 ms product improvement; it restores observability for fused-image comparison. | Image/debug view comparison | Separate debug profile implemented; target image comparison remains pending | Exact debug observability | Implemented as benchmark-only control | The CPU-selected diagnostic profile preserves the exact fused candidate's trace inputs and reconstruction equations but executes the real full-resolution R16F resolve plus separate composition instead of emulating the half round-trip in the fused shader. Reference and fused production profiles allocate or omit resources independently, so the control adds no inactive-path cost. |
| U-006 | Packed-edge reconstruction inside fused apply | Supplied prompt | Reconstruction/application | Algorithmic | Continuous compact bilateral | `AlgorithmicFusedPackedEdges2x2` and `AlgorithmicFusedPackedEdges4x4` select distinct compiled fused macros and omit the final AO intermediate | Packed edges; no final AO | None | 2x2 forecast 0.12-0.40 ms saving; 4x4 forecast -0.18 ms regression to 0.25 ms saving. Both overlap exact fusion and edge rows. | Quality/timing versus separate equivalent packed filters and exact fused baseline | Candidates implemented; controlled Intel target timing is not yet measured. | Algorithmic quality review pending | Implemented as optional experiment | The exact fused profile exists as the clean reference. Packed fusion uses `FUSED_PACKED_EDGE_RECONSTRUCTION=1/2`, requires reduced AO-only output with spatial filtering disabled, and conditionally retains only the R8 edge texture. Reference selects no candidate resource or dispatch. |
| U-007 | Apply AO in existing deferred-lighting pass | Supplied prompt | Deferred lighting | Exact for AO-only if semantics match | PBR writes BaseLighting before separate composition | D-028 AO-aware PBR permutation | Can remove BaseLighting and composition resources/work for AO-only | None | High estimated upside - can avoid the 15.8 MiB 1080p RGBA16F BaseLighting surface plus material rereads, non-additive with fusion. | Equation fixtures, exact HDR capture, complete-frame timing | Architecture supports AO-only ordering; GI requires source radiance first | Pending | Blocked by architecture | Preserve: AO affects fallback diffuse/specular, not direct/emissive/GI; material AO behavior unchanged. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| U-008 | Eliminate AO-only BaseLighting allocation and clear | Architecture research; pass-fusion requirement | Deferred lighting/resource planning | Exact under U-007 | Full-resolution RGBA16F BaseLighting is always allocated/cleared before AO composition | AO-aware PBR writes HDR directly | Removes about 15.82 MiB plus clear/read/write path | None | High estimated upside - can avoid the 15.8 MiB 1080p RGBA16F BaseLighting surface plus material rereads, non-additive with fusion. | Allocation/clear/capture and complete-frame timing | BaseLighting cost is outside current visibility timers | Exactness depends on U-007 | Blocked by architecture | Necessary to measure true end-to-end fusion rather than only visibility timer delta. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |

## GI and Multi-Bounce Optimizations

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G-001 | Fixed ordered 8/12/16 GI shaders | Supplied prompt | GI trace | Exact | Generic GI | S-006 through S-008 | Lazy PSOs | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Mask/new-count/source-owner/raw-GI fixtures and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Preserve near-to-far ownership, not merely final union. Implementation evidence: fixed GI/AO+GI 8/12/16/20 entries. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| G-002 | Fixed later-bounce eight-sample shader | Supplied prompt | Later GI bounce | Exact | Generic later bounce | S-015 | One lazy PSO | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Frontier/owner/metadata/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact required | Implemented and retained | Matches common eight-sample floor only when actual count is eight. Implementation evidence: fixed later-bounce 8. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| G-003 | Minimal consumer-specific bindings | Supplied prompt | GI trace | Exact | Reference uses a broad common layout | Fixed GI, AO+GI, first multi-bounce, and later-bounce pipelines build the reflected source/frontier/output subset | Removes unused bindings | None | Expected 0 ms in primary AO-only because GI is off; low-to-medium potential in GI/multi-bounce workloads. | SRV/UAV counts, DXIL reflection, GI fixtures/timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | The runtime constructs consumer-specific advanced layouts and preserves FP32 GI/source ownership. GI candidates do not inherit AO-only packed formats or unused AO outputs; Reference remains broad and unchanged. |
| G-004 | Source-normal approximation from depth neighbors | Supplied prompt; SSRT3 `NORMAL_APPROXIMATION`; SAO p.3 | GI trace | Algorithmic/Numerical | Reads G-buffer source normal | Optional depth-derived normal PSO | May remove normal SRV but adds depth reads | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Angular error, GI edge/light leakage and timing | Primary implementations reconstruct normals to reduce bandwidth | Pending | Blocked by correctness | Shading/geometric normal semantics differ; isolate and label visibly. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| G-005 | Source-radiance and normal reads only for newly claimed sectors | Supplied prompt; visibility paper p.5; SSRT3 | GI trace | Exact | Current `newSectorCount==0` continue precedes source reads | Preserve/minimal GI PSO | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Logical-load/DXIL and GI fixtures | Present | Exact | Already equivalent | Key paper optimization is implemented |
| G-006 | Compile-time source activity | Supplied prompt | GI trace | Exact | Runtime source-potential gate remains in active GI paths; AO-only compiles GI out | F-029 | None | None | Expected 0 ms in primary AO-only because GI is off; no exact active-GI saving without a uniform source fact. | DXIL, inactive/active source fixtures | No uniformly known active source class exists in the current GI workload | Exact only when uniform inactivity is proven | Not applicable | Material/radiance, emissive, and bounce inputs make source activity vary per pixel. The AO-only shader already eliminates all GI work; an active GI profile cannot compile out this per-pixel gate without restricting inputs or changing output. F-029 records the same invariant. |
| G-007 | Early receiver rejection | Supplied prompt; SSRT3 background early-out | GI trace | Exact when all outputs define rejected result | Current invalid/background receiver rejection | Move before scheduler/bind-heavy work in specialized PSO | None | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Background/invalid receiver fixtures and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Audit multi-bounce metadata/output initialization before changing location. Implementation evidence: early receiver rejection. The path is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| G-008 | GI contribution cutoffs | Supplied prompt | GI trace | Algorithmic unless formal final bound proves zero effect | Multi-bounce has conservative contribution gating; first bounce rejects only invalid/nonpositive source | Preserve existing proven gate; optional configurable cutoff separately | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Energy-bound fixtures, HDR error and timing | Later-bounce bound exists | Existing bound exact-to-threshold policy; new threshold changes result | Already equivalent | Current later-bounce optimization exists; arbitrary stronger cutoffs need a separate algorithmic row before implementation |
| G-009 | Compact GI metadata | Supplied prompt | GI trace/history | Numerical/Exact if integer domain fits | Multi-bounce alpha stores integer metadata in half precision | Bit-packed integer metadata profile only after full value-domain proof | Replaces no resource or uses compatible channel | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Exhaustive metadata round-trip, bounce fixtures, timing | Current half alpha represents required small integers exactly | No quality effect if lossless | Not applicable | Packing may save nothing if it does not reduce format/binding; measure before retention. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| G-010 | FP32 versus FP16 GI accumulation | Supplied prompt | GI trace | Numerical | FP32 | P-007 | None | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | HDR/bounce error and target timing | Pending | Pending | Blocked by correctness | Keep FP32 default; indirect accumulation compounds error. Current-build inspection found no validated candidate in the cited shader/configuration paths. A separate PSO could preserve zero Reference cost, but it must not be exposed until the row's stated range, discontinuity, ownership, depth, or image invariant passes the listed fixtures. No such proof or isolated experiment exists, so this remains an explicit deferred correctness block, not a measured rejection. |
| G-011 | Reuse prepared linear depth for GI | Supplied prompt | GI trace | Algorithmic unless exact full-resolution depth identity retained | GI intentionally never uses coarse hierarchy | Full-resolution exact linear-depth reuse only; no unrelated coarse ownership | Conditional shared depth if already built | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Depth identity, GI owner/image and total timing | Existing hierarchy is AO-only because coarse geometry cannot safely own radiance | Pending | Blocked by architecture | Coarse smart-average GI depth is unsafe; exact full-resolution reuse may still be viable. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| G-012 | Reuse packed edges for GI where helpful | Supplied prompt | GI reconstruction/temporal | Algorithmic | GI uses existing filters/history without edge metadata | Consumer-specific edge-aware reconstruction only | Packed edges | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | GI leakage/temporal/timing | Pending | Pending | Blocked by architecture | Do not force AO packed format onto HDR GI data. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| G-013 | Shared AO/GI depth traversal | Supplied prompt | Combined trace | Exact | Combined consumer already shares geometry traversal | Preserve and fixed-specialize | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | DXIL/load and AO/GI equality | Present | Exact | Already equivalent | Major combined-consumer optimization already architectural |
| G-014 | Separate AO-only fast reconstruction | Supplied prompt | Reconstruction/application | Exact or Algorithmic by selected filter | Broad visibility filter supports both outputs | Minimal AO-only fused/packed PSOs | Removes GI resources/bindings | None | Medium-to-high AO-only estimate - avoids HDR GI bindings/work and enables the fused or packed AO paths; expected 0 ms in GI-only because those profiles are intentionally inapplicable there. | AO exact/quality and timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | AO-specific | Implemented as optional experiment | Primary target should not pay HDR GI reconstruction cost. Implementation evidence: AO-only fused/packed profiles. It is CPU-selected and optional; Reference has no candidate-only bindings, resources, or dispatches, and target timing/quality remain unclaimed. |
| G-015 | Preserve near-to-far source ownership | Supplied prompt; visibility paper Algorithm 1 | GI trace | Exact invariant | Current interleaved ordered bit scan | Preserve in all fixed/cache variants | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Overlapping interval owner fixtures | Present | Product invariant | Already equivalent | No optimization may let farther radiance claim a sector first |
| G-016 | Activision multi-bounce AO polynomial | [Activision memo p.5, Eq.10](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf#page=5) | Lighting | Algorithmic | UVSR has true screen-space multi-bounce GI, not this color-remap approximation | Benchmark-only AO color-remap profile if useful | None | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Albedo/AO response and capture | Formula coefficients are source-documented; memo says uniform formulation cannot use it | Different lighting model | Not applicable | Do not substitute for product GI; gate to compatible cosine-weighted formulation only. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| G-017 | Bent-normal output from horizon vectors/angles | Activision deck slide 84; XeGTAO README | Trace/lighting | Algorithmic | No requested bent-normal product output | Optional benchmark profile only if consumer exists | Extra output channel/texture | None | Expected 0 ms in primary AO-only because GI is off; medium potential in GI/multi-bounce workloads. | Angular quality and cost; consumer audit | XeGTAO reports about 25% extra in its tested path | No current consumer established | Not applicable | Adding unused output is unjustified; if exposed, resource exists only while selected. Current-build inspection found no active prerequisite/consumer or the candidate is cross-referenced as stated earlier in this row. If the preceding reason does not establish that fact, this is a disclosed deferred/unimplemented candidate: a separate PSO or conditional resource could preserve zero Reference cost and no target timing rejects it. The Not applicable label must not be read as a measured rejection. |
| G-018 | Multi-bounce temporal light-buffer reinjection | Visibility paper p.8 | GI multi-bounce | Algorithmic | UVSR already supports multi-bounce frontier/cumulative reinjection | Preserve current behavior | Existing conditional GI resources | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression rather than adding a new win. | Bounce energy/feedback and image tests | Present | Product-required multi-bounce | Already equivalent | Paper warns intensity balance is needed to prevent feedback accumulation |
| G-019 | GPU contribution-terminated extended bounces | PBRT throughput-based path termination; NVRHI `dispatchIndirect`; Microsoft `ExecuteIndirect` | Later GI bounces | Algorithmic threshold policy with conservative next-bounce test | Explicit one-through-eight bounces and a constant cutoff | Default-on **Limit Bounces** preserves the explicit path; disabling it raises the contribution bar 4x per bounce and uses a GPU continuation flag to write the next indirect dispatch | One 4-byte flag, one 24-byte current/maximum indirect-argument buffer, and continuation PSOs | No added dispatch while limited; 28 persistent buffer bytes plus PSO memory | Versus naive fixed 16: convergence after bounce 4 avoids 12/15 later full-screen traces (80%); after bounce 8 it avoids 8/15 (53%). It is not expected to beat an intentionally limited eight-bounce render. | Isolated Intel run with the same scene/exposure/cutoff; report Later Bounces and complete-effect median/p95 plus image review | Controlled Intel generic and fixed-eight runs each completed 240/240 measured frames with zero incomplete frames and clean captures. Both converged by bounce four. Fixed-eight versus generic reduced complete median 10.15%, complete p95 8.41%, and Later Bounces median 18.50%. | Threshold-controlled extra transport; 16 is a fault guard rather than the intended terminator | Implemented as optional experiment | A wave-coalesced flag avoids per-pixel global atomic contention; a one-thread control pass writes full or zero dispatch dimensions after every possible bounce. No CPU readback stalls the frame. |

## Cache and Shared-Memory Data Layout

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L-001 | Aligned and packed resources | Supplied prompt; Intel Arc and AMD RDNA guides | All GPU stages | Exact if packing is lossless; Numerical for compact formats | Mixed existing formats | Per-profile layouts using the narrowest validated formats and aligned access | Conditional resources only | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Resource bytes, transaction/counter data, image/round-trip tests | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Classification depends on encoding | Implemented as optional experiment | Packing must reduce actual transactions or bindings, not merely bit count. Implementation evidence: conditional packed FAST/edge resources. It is CPU-selected and optional; Reference has no candidate-only resources or dispatches, and target results remain unclaimed. |
| L-002 | AO plus edges in one resource | Supplied prompt | Trace/reconstruction | Exact AO for compatible popcount estimator plus Algorithmic edges | Separate R16F AO and no edges | P-012/E-013 | Replaces current AO resource at same two bytes/pixel | None | Medium estimate - packing 2-byte AO plus 1-byte edges into 2 bytes cuts those plane bytes about 33%, estimator-gated. | Packing, cache and timing | Feasible for compatible estimator | Pending | Blocked by architecture | Avoids a separate edge texture and load. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| L-003 | Separate AO and edge resources when packing harms cache behavior | Supplied prompt | Trace/reconstruction | Exact resource-layout alternative | No edges | Separate conditional R16F/R8 or R8/R8 resources | Separate R8 edge texture | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Cache/sampler/timing versus L-002 | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Same decoded values | Implemented as optional experiment | Hardware format and consumer access patterns may favor separate planes. Implementation evidence: R16F AO plus separate R8 edges. It is CPU-selected and optional; Reference has no candidate-only resources or dispatches, and target results remain unclaimed. |
| L-004 | Deinterleaved texturing | Supplied prompt; NVIDIA presentation; ASSAO/CACAO | Depth/trace | Exact data layout only with identical samples; otherwise Algorithmic | Linear textures | D-019 | Layered quarter-resolution resources and extra passes | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Total pipeline/cache/image | Conflicting primary evidence | Pending | Blocked by architecture | Must include deinterleave/reinterleave overhead and UVSR access pattern. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| L-005 | Checkerboard trace | Supplied prompt; XeGTAO README | Trace/reconstruction | Algorithmic | Full half-grid | D-018 | Reconstruction/history support | None | High producer estimate - roughly 50% fewer trace invocations, with reconstruction/history cost and quality risk. | Total traffic, flicker and image/timing | Pending | Pending | Blocked by correctness | Separate from half resolution and deinterleaving. Correctness evidence: `src/screen_space_visibility.cpp`, `src/visibility_performance_plan.*`, and the curated wrappers retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| L-006 | Quarter-resolution trace | Supplied prompt | Trace/reconstruction | Algorithmic | Half-linear-resolution target | Quarter-resolution profile with matching reconstruction/history | Smaller AO resources; no extra when active replacement | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Total timing, edge/flicker/static/motion quality | Present and selectable; target timing and dedicated static/motion quality acceptance remain pending. | Expected detail loss | Already equivalent | Potential large producer saving, but reconstruction dominates quality and may erase benefit. Code evidence: `src/screen_space_visibility.cpp`, `src/visibility_performance_plan.*`, and the curated wrappers. The current path already performs the work, so no extra option or inactive cost is required. |
| L-007 | Full-resolution guide reuse | Supplied prompt | Reconstruction/application | Exact | Filter and composition reread depth/normal/material | Fused PSO retains/reuses guides; deferred integration where possible | None | None | High estimate - can remove full-resolution traffic/pass work; non-additive with the 7.9 MiB/frame fused-AO saving. | Logical loads, registers, image/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented as optional experiment | Register pressure must be tracked alongside avoided reads. Implementation evidence: fused guide reuse. It is CPU-selected and optional; Reference has no candidate-only resources or dispatches, and target results remain unclaimed. |
| L-008 | Remove dummy-resource hot-path bindings | Supplied prompt | Binding/resource plan | Exact | Reference broad layouts retain canonical dummy-compatible binding sets | Advanced fixed, diagnostic, packed, exact-filter, temporal-copy, and fused layouts include only reflected resources | Removes dummy bindings from candidate paths; reference dummy resources remain for Reference | None | Low estimate: 0.00-0.10 ms and highly driver-dependent; overlaps D-029/D-030. | Binding/resource-plan assertions, DXIL reflection, timing | Candidate implemented; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | `GetOrCreateAdvancedPipeline` receives a curated layout and the runtime builds matching binding sets. This is a candidate optimization, not a global reference-layout rewrite, so selecting Reference incurs no advanced binding or PSO cost. |
| L-009 | Consumer-driven allocation | Supplied prompt | Resource planning | Exact | Major mode resources are conditional; both noise textures and dummies remain resident | D-031 | Removes inactive optional bytes | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Allocation/dispatch assertions | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Every experimental texture/history must exist only while its profile is selected. Implementation evidence: profile-derived resource flags. It is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| L-010 | Minimize resource-state transitions and UAV barriers | Supplied prompt; Intel Arc guide | Pass orchestration | Exact | Separate resolve/composite and optional passes require transitions | Fused passes, batched barriers, no barrier between independent prep outputs | None | None | High estimate - can remove full-resolution traffic/pass work; non-additive with the 7.9 MiB/frame fused-AO saving. | GPU capture barrier list, timing and exact image | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented as optional experiment | Remove only dependencies made unnecessary by the selected resource plan. Implementation evidence: the fused resource plan. It is CPU-selected and optional; Reference has no candidate-only resources or dispatches, and target results remain unclaimed. |
| L-011 | Share depth hierarchy with another effect | Supplied prompt | Depth preparation | Exact only with identical semantics | Visibility owns conditional hierarchy | Shared producer/consumer plan after compatibility proof | Shared existing resource; no duplicate | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Value/lifetime/barrier audit and total frame timing | No compatible consumer established at base | Pending | Not applicable | A shared hierarchy is a win only if reduction, convention, extent, and timing all match. Applicability evidence: `src/screen_space_visibility.cpp`, `src/visibility_performance_plan.*`, and the curated wrappers. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| L-012 | Share denoiser with SSR or another reduced-resolution effect | Supplied prompt; Activision slide 89 discussion | Reconstruction | Algorithmic unless the exact same filter inputs/outputs are required | Dedicated visibility filter | Shared dispatch/tile only after a real compatible effect exists | Shared tiles/resources depending integration | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Full-frame timing and per-effect quality | No compatible effect established at base | Pending | Not applicable | "Amortizable" external cost is not evidence of actual sharing. Applicability evidence: `src/screen_space_visibility.cpp`, `src/visibility_performance_plan.*`, and the curated wrappers. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| L-013 | Async compute overlap | Supplied prompt; [AMD Total War AO case](https://gpuopen.com/learn/anatomy-total-war-engine-part-4/) | Scheduling | Algorithmic scheduling | Visibility runs in current graphics schedule | Optional queue plan only with a demonstrably independent overlap window | Queue synchronization only | None | Low-confidence, high-upside estimate - queue overlap can hide stage time but only whole-frame latency counts. | PIX Timing Capture/end-to-end frame time; not replayed capture duration | AMD hid about 1.4 ms at 4K Fury X in a different engine/GPU | No image change if synchronization correct | Blocked by architecture | Retain only when whole-frame time improves; queue timestamp alone is insufficient. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| L-014 | Read-only resource access for immutable inputs | Intel Arc guide | Bindings/cache | Exact | Depth/G-buffer SRVs are read-only; broad resources may carry unnecessary capabilities | Preserve SRV-only bindings in minimal layouts | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Resource flags, DXIL and cache/timing | Major inputs already SRVs | Exact | Already equivalent | General vendor guidance already followed for immutable visibility inputs |
| L-015 | Phase-separated sample setup, depth fetch, and accumulation for ILP | CACAO shader around sample loop | Trace | Exact if same order/ownership | Current loop is largely serial per sample | Fixed trace PSO groups independent arithmetic/fetch preparation without reordering GI claims | None | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | DXIL dependency chains, registers, masks/owners and timing | CACAO splits phases to expose ILP; UVSR variant not implemented | Exactness pending | Blocked by architecture | AO-only union semantics permit a fixed-8 PSO to stage independent setup/fetch work while GI requires near-to-far ownership during accumulation. Separate consumer PSOs can preserve Reference and GI ordering. Extra live values may raise registers, but no implementation or register evidence exists; this is an unmet AO-only experiment, not an established architecture block. |
| L-016 | Wave operations for hierarchy/filter reductions | Shader Model 6 wave guidance; AMD SPD | Depth/filter | Exact if reduction/order and wave-size independence are preserved | LDS and barriers | Wave-enabled specialized PSO with portable fallback | None | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Feature/wave-size tests, DXIL/ISA, image/timing | Not implemented or measured | Exact required | Blocked by architecture | A portable variant must handle supported wave sizes, dispatch tails, and devices lacking the required wave feature. Integer min/max reductions can remain exact; floating reductions change order and must be Numerical. The current shader/configuration contract has no feature-gated fallback pair. CPU PSO selection could keep Reference free of cost, so the concrete feature/semantic prerequisites remain unevaluated rather than impossible. |
| L-017 | Cache-aware HZB sample reordering | [AMD Unreal Engine guidance](https://gpuopen.com/unreal-engine/) | Trace | Exact only for AO and for GI when ownership order remains | Current near-to-far interleaved order | AO-only cache-order benchmark; GI retains ownership order | None | None | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Sample/mask/owner fixtures, cache and timing | AMD reports reordering HZB samples but linked patch is unavailable | Pending | Not applicable | AO union may commute; GI radiance ownership does not, and absent source code prevents copying details. Applicability evidence: `src/screen_space_visibility.cpp`, `src/visibility_performance_plan.*`, and the curated wrappers. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| L-018 | Procedural noise instead of texture fetch | AMD Unreal Engine guidance | Noise/trace | Algorithmic unless sequence is identical | Texture FAST/toroidal or hash mode | Existing independent hash is the safe procedural control | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Value/spectral/image/timing | AMD reports replacing a random texture load; patch unavailable | Existing hash quality differs | Already equivalent | UVSR already exposes a procedural hash; an undocumented AMD sequence cannot be reproduced responsibly |
| L-019 | Visibility caching by line sweep | [Line-Sweep AO paper](https://diglib.eg.org/server/api/core/bitstreams/786e1ab5-c669-48ac-9e99-56a1564edcad/content) | Trace architecture | Algorithmic replacement | Randomized one-slice per-pixel bitmask | No product control; possible external benchmark only | Substantial sweep/cache structures | None if absent | Low-to-medium estimate - changes locality, bindings, barriers, or scheduling without reducing eight physical depth reads. | Architecture comparison only | Paper reports radius-independent queries and 1.7 ms 1080p GTX480 | Different estimator/dataflow | Blocked by architecture | It replaces the required per-pixel randomized bitmask traversal; a checkbox would require a second AO architecture, and Off could be free but enabled output would not test an incremental UVSR optimization |
| L-020 | Compute full-screen application instead of pixel shader | AMD RDNA guide | Application | Exact | Visibility composition already uses compute | Preserve compute path | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Source inspection | Present | Exact | Already equivalent | Vendor guidance notes modest helper/cache benefits; UVSR already follows it |

## Activision Occlusion Source Candidates

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A-001 | Binary two-horizon cosine-weighted analytic estimator | [Activision memo p.3, Eqs.5-8](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf#page=3); deck slides 54-64 | Trace | Algorithmic benchmark | 32-sector finite-thickness estimator | PS4 closest-match horizon PSO only | One lazy PSO | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Same-engine matched read/radius/application timing and captures | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Different estimator | Implemented as benchmark-only control | Required diagnostic control, never a product replacement. Implementation evidence: benchmark horizon control. It is benchmark-only and Reference runs no diagnostic work. |
| A-002 | Project normal into slice plane and multiply by projected-normal length | Activision memo p.3; deck slides 58-64 | Horizon trace | Exact implementation of A-001; Algorithmic relative to product | Product slice measure has its own estimator math | Include in PS4 benchmark exactly | None | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Analytic fixture against numerical integration | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Correctness requirement for control | Implemented as benchmark-only control | Omitting it would not be Activision's estimator. Implementation evidence: projected-normal correction. It is benchmark-only and Reference runs no diagnostic work. |
| A-003 | Retain only maximum horizon state per side | Activision memo p.3; deck slides 54-64 | Horizon search | Exact implementation of A-001 | Product retains a 32-bit union, not two maxima | A-001 keeps two maxima and no distance state | None | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | DXIL/register/timing and estimator fixture | Benchmark control implemented in the current build; controlled Intel target timing is not yet measured. | Correct for horizon control only | Implemented as benchmark-only control | Incompatible with finite-thickness multi-layer visibility, so isolate in benchmark PSO. Implementation evidence: one maximum horizon per side. It is benchmark-only and Reference runs no diagnostic work. |
| A-004 | Published PS4 workload approximation | Activision 2016 deck slides 85-89; expanded 2019 memo | Benchmark profile | Algorithmic bundle | Scalar and packed-gather coupled profiles replace the older partial trace-only control | Half resolution, one direction/pixel, eight total samples/four per side, 4x4 reconstruction, six bidirectional axes, temporal accumulation, required upsample, and matched UVSR composition | Conditional depth, spatial AO, temporal AO/depth history, and motion resources | None | Expected 0 ms exact-product saving; the profile is a quality/performance comparison and the gather delta is ranked separately | Persist complete profile key and report every stage plus complete median/p95 | Both forms completed 600/600 controlled local frames with zero incomplete frames. Scalar measured 0.306176/0.340019 ms and packed 0.302080/0.313344 ms; both were slower than Reference. | Clean captures; closest practical approximation because exact parity requires unpublished shipping source/constants | Implemented as optional experiment | The 2016 deck identifies eight total taps and the coupled order. The closest valid linear depth guides each 2x2 footprint; one direction per frame converges through the six-phase history and was stable after the 120-frame warm-up. Retain both forms as algorithm comparisons, not local performance profiles, and prefer packed when using them. |
| A-005 | Half-resolution closest-depth base and farther mips | Activision deck slide 85 | Depth preparation | Algorithmic | PS4 approximation builds and consumes a half-resolution R32_UINT guide containing FP32 linear-depth bits plus a two-bit closest-source offset | Retain the implemented base; add farther mips only if the selected radius law consumes them and total timing justifies preparation | Conditional packed-depth guide texture | None | Included in the PS4 profile; additional unused mips would regress | Total prep/trace and thin-edge image review | The closest-depth base measured 0.008192 ms median in both controlled PS4 variants and was consumed by trace, spatial, temporal, and guide-pixel reconstruction for all 600/600 complete frames. Farther levels are intentionally absent because the approximation does not sample them. | Approximate source layout; closest valid reduction and source identity are explicit | Implemented as optional experiment | R32_UINT storage masks two low mantissa bits into the FP32 linear-depth payload for source identity at no extra texture cost; consumers mask them before `asfloat`. This fixes the prior receiver mismatch without creating unconsumed pyramid levels merely to resemble a diagram. |
| A-006 | Deliberate 4x4 spatial rotations and six temporal rotations | Activision deck slides 86-93 | Noise | Algorithmic | N-004/N-005 | A-004 sets exact formulas/tables | None | None | Low speed estimate - unchanged read count; principal value is scheduler/filter quality and effective support. | Phase fixture and filter-coupled quality | The coupled schedule ran for 600/600 controlled frames in both PS4 profiles and was stable after warm-up. | Clean static captures; dynamic stability pending | Implemented as optional experiment | Already inventoried individually; profile coupling is recorded here. The implementation uses 4x4-by-6 scheduling and costs Reference no candidate resource or dispatch. |
| A-007 | Separate four-phase spatial radial offsets | Activision deck slides 86-93 | Noise | Algorithmic | Current independent jitter | `((pixel.y - pixel.x) & 3) / 4`, shared across both radial sides for the pixel | None | None | Low speed estimate - unchanged read count; principal value is scheduler/filter quality and effective support. | Exhaustive 4x4 formula fixture, banding/flicker | Both controlled PS4 profiles exercised all spatial offsets for 600/600 frames and produced clean static captures. | Source formula retained; dynamic banding/flicker review pending | Implemented as optional experiment | `ActivisionSpatialRadialOffset` implements the slide-93 formula, distinct from the six temporal angle phases and four temporal offsets. Reference selects no Activision scheduling code or resource. |
| A-008 | Separate four-phase temporal radial offsets | Activision deck slides 86-93 | Noise | Algorithmic | Current temporal sequence | Temporal offsets `{0,.5,.25,.75}` indexed by `(frame/6)%4` | None | None | Low speed estimate - unchanged read count; principal value is scheduler/filter quality and effective support. | 24-frame-cycle fixture and capture | Each controlled PS4 run covered 25 complete 24-frame cycles after warm-up and ended with a clean capture. | Source formula retained; dynamic temporal review pending | Implemented as optional experiment | Effective support is not physical fetch count. The 24-frame phase is CPU-profile-selected; Reference has no Activision scheduling resource or dispatch. |
| A-009 | Four-by-four gather reconstruction with relative linear-depth weights | Activision deck slides 94-96 | Reconstruction | Algorithmic | Dedicated derivative-aware scalar and packed-gather 4x4 filters | Compare scalar 16 AO plus 16 depth reads against four AO plus four depth gathers under the same kernel/profile | Spatial R16F AO plus prepared depth; no extra resource for gather | None | Packed versus scalar forecast -0.08 to 0.25 ms | Generated-code inspection, stage median/p95, scalar/gather image comparison, and edge/motion review | In the controlled local run, packed spatial median was 0.023552 ms versus 0.031744 ms scalar. Packed saved 1.338% complete median and 7.845% p95, with 600/600 complete frames per variant. | Clean captures; relative 10% depth ramp follows publication and UVSR curvature allowance is disclosed | Implemented as optional experiment | The implementation does not misstate “one gather if packed”: it uses four AO and four depth gathers. The scalar profile remains a readable correctness oracle; prefer packed locally for the faithful comparison, not as a faster-than-Reference production profile. |
| A-010 | Source-like exponential moving-average temporal accumulation | Activision deck slides 97-102 | Temporal | Algorithmic | Dedicated half-resolution AO/depth history with motion reprojection | Keep explicit current response, depth rejection, diagonal bounds, and motion response inside the PS4 profile | Conditional R16F AO and R32F depth histories plus motion input | None | Expected 0 ms exact-product saving; included in the complete PS4 profile cost | Step response, disocclusion, motion, camera cut, and long-run captures plus stage timing | Both profiles completed 600/600 controlled frames with temporal medians of 0.036864 ms scalar and 0.037888 ms packed; static captures are clean, while dynamic motion/disocclusion acceptance remains pending. | Source-like rather than exact because shipping thresholds/constants are unpublished | Implemented as optional experiment | The pass rejects stale foreground history, clamps toward current diagonal bounds, raises current weight under motion, and resets with normal history invalidation. Calling it exact Activision EMA would overstate public evidence. |
| A-011 | Screen-space-size horizon thickness EMA | [Activision memo p.4, Eq.9](https://www.activision.com/cdn/research/PracticalRealtimeStrategiesTRfinal.pdf#page=4) | Horizon trace | Algorithmic | Explicit front/back finite thickness | Only A-001 benchmark may expose original receding-horizon EMA | Horizon state only inside benchmark shader | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Thin geometry/halo/leak capture | Source heuristic is horizon-specific | Incompatible with product interval semantics | Blocked by correctness | Do not combine with bitmask thickness; isolation in horizon benchmark preserves zero off cost. Correctness evidence: `src/screen_space_visibility_algorithmic_cs.hlsl`, horizon/schedule macros, and the partial-control status in `src/visibility_performance_plan.cpp` retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| A-012 | Linear far-field attenuation | Activision memo p.4; deck slides 103-104 | Trace/lighting | Algorithmic | PS4 approximation fades candidate horizon contribution through the outer 25% of its selected world radius | Keep scale-independent outer-quarter fade as a disclosed UVSR approximation | None | None | Expected 0 ms exact-product saving; quality stabilization inside the PS4 comparison | Distance response, thin/far geometry, image, and timing | The fade ran for 600/600 controlled frames in both profiles; final static captures were clean, including the prior artifact scene. | Content-scale choice differs from the publication's example distances; dynamic/thin-far stress remains pending | Implemented as optional experiment | The fade avoids a hard dark cutoff and is isolated from UVSR bitmask thickness/radius clamps. It is not represented as Activision's unpublished shipping constant. |
| A-013 | AO/albedo cubic multi-bounce compensation | Activision memo p.5, Eq.10 | Lighting | Algorithmic | G-016 | Benchmark-only compatible cosine-weighted application | None | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Response/energy captures | Source formula documented | Not true GI | Not applicable | Cannot be used with the memo's uniform formulation and cannot replace UVSR multi-bounce GI. Applicability evidence: `src/screen_space_visibility_algorithmic_cs.hlsl`, horizon/schedule macros, and the partial-control status in `src/visibility_performance_plan.cpp`. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| A-014 | Depth-derived receiver normals | Activision implementation discussion; SAO reconstruction | Horizon trace | Algorithmic/Numerical | UVSR uses G-buffer normals | Optional PS4/XeGTAO control mode, with supplied-normal reference | May remove normal SRV but needs depth neighbors | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Angular/silhouette/timing | Pending | Pending | Blocked by correctness | Useful bandwidth control but changes shading-normal semantics. Correctness evidence: `src/screen_space_visibility_algorithmic_cs.hlsl`, horizon/schedule macros, and the partial-control status in `src/visibility_performance_plan.cpp` retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| A-015 | Bent normal from average horizon vectors or angles | Activision deck slide 84 | Trace/lighting | Algorithmic | No active bent-normal consumer | G-017 only if consumer exists | Extra output | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Consumer and angular-quality audit | Source provides full and cheap variants | No established product use | Not applicable | Optional isolation is possible, but must not charge AO-only mode when unused. Applicability evidence: `src/screen_space_visibility_algorithmic_cs.hlsl`, horizon/schedule macros, and the partial-control status in `src/visibility_performance_plan.cpp`. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| A-016 | PS4 GCN memory-bound horizon loop evidence | Activision memo/deck | Evidence/trace | Diagnostic evidence | UVSR bitmask loop has more ALU/state | Use M-003/M-004/A-001 counters rather than an option | None | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Sampler/EU/cache counters and matched trace timings | Source says horizon loop was memory-bound with quarter-rate `rsqrt` | No image effect | Not applicable | This is a profiling finding, not a selectable optimization; the controls above test whether it holds on Xe-LPG |

## XeGTAO Source Candidates

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| X-001 | Compile-time default-constant specialization | [Pinned XeGTAO shader `XE_GTAO_USE_DEFAULT_CONSTANTS`](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/XeGTAO.hlsli) | Trace | Exact | Fixed UVSR bitmask profiles and the XeGTAO High profiles each have curated constant specializations | Keep generic fallbacks separate from compiled finalist settings | None | None | Included in fixed-bitmask and XeGTAO family forecasts; not additive | DXIL constant folding/register/timing and image equality | Both candidate families completed controlled local profiles: Reference/Fixed/Fused finalists and all three XeGTAO High variants ran 600/600 frames with zero incomplete frames. Target timing remains pending. | Exact within each selected profile | Implemented and retained | The Xe source port preserves the pinned constant-specialization policy, including fields the source deliberately keeps dynamic. UVSR Reference retains canonical work and no target-GPU timing is claimed. |
| X-002 | Low quality: one slice by two steps per side | [pinned XeGTAO entry points](https://github.com/GameTechDev/XeGTAO/blob/a5b1686c7ea37788eeb3576b5be47f7c03db532c/Source/Rendering/Shaders/vaGTAO.hlsl#L101) | XeGTAO benchmark | Algorithmic profile | High is implemented; Low is compiled by the shader manifest but has no CPU profile | Add a Low profile only if High fails the target budget and image tradeoff is worth testing | No new resource beyond existing XeGTAO resources | None | Potentially large algorithmic cost reduction; upstream describes lower presets but UVSR has no target estimate | Same-engine timing and image review | Shader permutation compiles; no selectable benchmark entry or runtime result | Different estimator/quality | Blocked by architecture | The complete High pipeline now exists, so the remaining gap is a deliberate curated-profile decision rather than missing XeGTAO infrastructure. Do not expand the UI until target High evidence shows a need. |
| X-003 | Medium quality: two slices by two steps per side | Pinned XeGTAO entry points lines 109-113 | XeGTAO benchmark | Algorithmic profile | High is implemented; Medium is compiled but not CPU-selectable | Add a Medium profile only as a bounded finalist if High is too slow | No new resource beyond existing XeGTAO resources | None | Upstream says Medium costs roughly two-thirds of High; this is provenance, not a UVSR forecast | Timing and image review under the same source-port scope | Shader permutation compiles; no selectable runtime profile | Different estimator/quality | Blocked by architecture | Medium's eight reads numerically match the target count but use two slices and a different estimator, so it is not an equal-quality UVSR replacement. |
| X-004 | High quality: three slices by three steps per side | Pinned XeGTAO entry points lines 117-121 | XeGTAO benchmark | Algorithmic profile | Three selectable source-port profiles use High's 18 physical reads | Retain LUT/mixed as closest port and LUT/FP32 plus inline-Hilbert as isolated controls | Existing XeGTAO conditional resources | None | Complete pipeline forecast -0.40 to 0.80 ms versus canonical Reference, low confidence | Per-stage/complete timing, precision comparison, and image review | All three profiles completed 600/600 controlled local frames with zero incomplete frames. LUT mixed, inline mixed, and LUT FP32 measured 0.413696/0.454656, 0.419840/0.454656, and 0.349184/0.384000 ms; each was slower than Reference. | Clean captures; different estimator and adapter-qualified output | Implemented as optional experiment | The High profile is copied closely from pinned Intel math and constants. Retain all three as quality/reference controls, prefer LUT/FP32 locally, and never infer Xe-LPG ordering from this NVIDIA run. Published timings exclude UVSR-specific integration differences and cannot fill the target result. |
| X-005 | Ultra quality: nine slices by three steps per side | Pinned XeGTAO entry points lines 125-129 | XeGTAO benchmark | Algorithmic profile | High only | Compile-time Ultra exists in the manifest but is not exposed | No new resource beyond existing XeGTAO resources | None | Expected regression versus High; value would be a quality ceiling rather than a speed candidate | Timing/quality only if High image validation needs a ceiling | Shader permutation compiles; no selectable runtime profile | Different estimator/quality | Not applicable | The user asked for performance candidates and the source-like High path. Adding a 54-read UI profile before High validation would add breadth without a plausible speed benefit. |
| X-006 | More slices with fewer steps instead of original thickness heuristic | Pinned XeGTAO README | XeGTAO trace | Algorithmic | The High port uses three slices and three steps per side, separate from the one-slice bitmask | Retain only inside XeGTAO profiles | None beyond the source-port PSOs | None | Included in X-004, not independently additive | Thin geometry, quality, and timing | Implemented as part of High | Source-defined for XeGTAO; incompatible with bitmask equivalence | Implemented and retained | This is a defining source-port choice, not a general product checkbox or proof against UVSR's finite-thickness estimator. |
| X-007 | Per-step R1/golden-ratio sequence | Pinned XeGTAO README and MainPass | XeGTAO trace | Algorithmic | High source port keeps the per-step golden-ratio term separately from screen noise | Retain inside all XeGTAO profiles | None | None | Included in X-004 | Source comparison and image review | Implemented in the pinned High loop | Source-defined | Implemented and retained | The loop uses the pinned slice-plus-step index and golden-ratio factor; it is not collapsed into Hilbert/R2 noise or exposed as a bitmask setting. |
| X-008 | Exact 64x64 Hilbert tile with R2 and temporal offset 288 times phase modulo 64 | Pinned XeGTAO README; `SpatioTemporalNoise` | Noise | Algorithmic | LUT and inline-Hilbert source-port profiles implement the same 4096-entry mapping | Compare 8 KiB R16_UINT LUT against inline integer Hilbert under a static phase | Optional Hilbert LUT only in LUT profiles | None | LUT versus inline forecast -0.05 to 0.15 ms | Exhaustive index/range/sum/adjacency/anchor fixtures, DXIL, timing, and captures | All 4096 indices are unique/in range and both profile contracts pass. In the controlled local run, LUT mixed measured 0.413696 ms versus 0.419840 ms inline mixed, a 1.463% median saving; their p95 values were equal. | Clean captures; static phase is intentional because UVSR supplies no matching TAA history | Implemented as optional experiment | The source formula retains `288 * (phase % 64)`, while the host sets phase zero under Intel's no-TAA contract. Prefer LUT locally and retain inline as the isolated ALU/traffic control; target Xe-LPG behavior remains unmeasured. |
| X-009 | Normal-relative hemisphere-horizon falloff target | Pinned XeGTAO README/MainPass | XeGTAO trace | Algorithmic | Implemented inside the separate source-port horizon estimator | Retain only inside XeGTAO profiles | None | None | Included in X-004 | Analytic/source comparison and image review | Implemented and compiled | Different visibility behavior by design | Implemented and retained | This is defining XeGTAO math and remains incompatible with exact transfer into uniformly angular bitmask sectors. |
| X-010 | Cosine-space horizon interpolation | Pinned XeGTAO README/MainPass | XeGTAO trace | Numerical/Algorithmic | Implemented inside the source port | Retain only inside XeGTAO profiles | None | None | Included in X-004 | Horizon/source comparison and DXIL | Implemented and compiled | Source-defined for XeGTAO | Implemented and retained | The port preserves signed horizon cosine values and the pinned interpolation/integration order. It is not an exact optimization for the bitmask estimator. |
| X-011 | Optional final horizon-clamp removal | Pinned XeGTAO README version notes | XeGTAO trace | Algorithmic/Numerical | The pinned 1.30 source-port path follows the current upstream clamp policy | No additional toggle unless a pinned-source regression comparison is required | None | None | Included in X-004; no incremental forecast | Source diff, quality, and timing if reintroduced | Current pinned behavior is implemented | Source-defined | Already equivalent | The port should not add a version-history toggle that is absent from the selected 1.30 High path. It remains inapplicable to the bitmask product. |
| X-012 | Format-dependent center-depth bias | Pinned XeGTAO source constants | XeGTAO trace | Numerical | Working depth is always R16F, so every source-port profile selects the pinned 0.99920 bias | Retain the R16F bias; no misleading FP32-depth toggle exists | None | None | Included in X-004 | Self-occlusion/source fixtures and image review | Implemented and compiled | Matches the selected upstream depth format | Implemented and retained | LUT/FP32 refers to arithmetic precision, not depth storage; all profiles therefore use the FP16-depth bias. |
| X-013 | Minimum sample-distance push-out | XeGTAO MainPass | Trace | Algorithmic | UVSR clamps sample distance to at least 0.5 pixel | Preserve current equivalent in bitmask; reproduce XeGTAO value in control | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Coordinate/duplicate and quality fixtures | Similar mechanism exists | Existing product behavior | Already equivalent | Exact constants/coordinate snap differ and belong to benchmark profile |
| X-014 | Snap sample offsets to texel centers | Pinned XeGTAO MainPass | Trace | Algorithmic/Numerical | Source port rounds sample offsets before depth lookup and reconstruction | Retain inside XeGTAO only | None | None | Included in X-004 | Coordinate/source fixture, image, and timing | Implemented and compiled | Source-defined | Implemented and retained | Snapping changes selected texels and therefore remains algorithmic relative to UVSR bitmask sampling. |
| X-015 | Small projected-radius visibility fade | Pinned XeGTAO README/MainPass | Trace | Algorithmic | Source port initializes visibility from the pinned screen-radius fade | Retain inside XeGTAO only | None | None | Included in X-004 | Subpixel geometry and captures | Implemented and compiled | Source-defined | Implemented and retained | This smooth small-radius behavior is part of the source port, not a replacement for the separate UVSR hard/early experiment. |
| X-016 | Depth-scaled thin-occluder falloff | Pinned XeGTAO README/MainPass | Trace | Algorithmic | Source port retains pinned distance falloff and default zero thin-occluder compensation | Retain source defaults; expose nonzero compensation only as a new quality experiment | None | None | Included in X-004 | Thin/far geometry captures and timing | Default High behavior is implemented | Incompatible with exact finite-interval semantics | Implemented and retained | Do not merge this falloff into Activision horizon approximation or UVSR front/back interval thickness. Nondefault thin compensation remains unexposed. |
| X-017 | Precomputed falloff multiply/add | Pinned XeGTAO MainPass | Trace | Exact implementation of selected falloff | Source port currently preserves the pinned dynamic falloff computation | Optional port-only hoist after source-equivalence and DXIL proof | None | None | Low potential; included constants are invariant but compiler folding and live ranges decide value | DXIL and selected-falloff equality plus target timing | Not independently implemented or measured | Exact within selected algorithm | Blocked by architecture | The complete port now exists, but its source intentionally keeps the value dynamic even under default-constant specialization. A separate hoist is a new optimization, not source parity, and should be added only with generated-code evidence. |
| X-018 | R8_UINT raw AO with 1.5 overshoot scale and final decode | Pinned XeGTAO source `XE_GTAO_OCCLUSION_TERM_SCALE` | Trace/reconstruction | Numerical | Source port preserves the 1.5 convention and explicit UNORM8 rounding in R16F storage | Native R8_UINT remains a possible port-only bandwidth experiment with a UVSR decoder | Would replace one R16F plane with R8_UINT | None | Up to one byte per output pixel of working storage; runtime saving unknown | Range/quantization/denoise/image/timing | Arithmetic/rounding behavior is implemented; native R8 storage is not | Pinned quantization retained, storage differs | Blocked by architecture | UVSR's generic float consumer needs no uint decoder, so the adapter deliberately spends one extra byte per working pixel. This limitation does not apply to generic unbounded UVSR estimators because the encoding is confined to XeGTAO. |
| X-019 | One-, two-, and three-pass gather denoising with ping-pong | Pinned XeGTAO README and `Denoise` | Reconstruction | Algorithmic | One sharp pass is implemented; two/three-pass controls are not exposed | Retain one pass for High; add extra passes only as explicit no-TAA quality controls | One R8 edge plane and one R16F working AO; extra passes would require ping-pong | None | One-pass cost is included in X-004; removed unused second AO plane saves 3.96 MiB at 1080p and 15.82 MiB at 4K | Per-count timing/quality and dispatch/bytes | One pass and source tap order compiled and completed 600/600 controlled frames in all three XeGTAO profiles; local spatial medians were 0.029696, 0.028672, and 0.023552 ms. | Clean one-pass captures; source's TAA-oriented fast path uses a static phase without matching TAA | Implemented as optional experiment | The current path needs no second AO plane. Clean final captures remove the immediate need for extra passes; multi-pass variants remain a bounded follow-up only if dynamic-motion validation exposes objectionable noise. |
| X-020 | Standalone depth-derived-normal generation | XeGTAO README/source | Normal preparation | Algorithmic/Numerical | G-buffer normals | Separate normal-prep PSO for benchmark/reuse study | Conditional normal texture | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Prep plus trace, angular/quality/timing | Source supports generated normals | Pending | Blocked by architecture | Include prep cost and do not assume reconstructed normals match shading normals. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| X-021 | In-main-pass depth-derived normals | XeGTAO source | Trace | Algorithmic/Numerical | G-buffer normal read | XeGTAO benchmark compile option | Extra depth neighbor reads, no texture | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Trace total and angular/quality | Source option | Pending | Blocked by architecture | Compare against standalone generation and supplied normals. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| X-022 | Reusable packed R11G11B10 generated normals | XeGTAO README/source | Normal preparation | Numerical | RGBA G-buffer normals | Conditional generated-normal resource shared only by compatible consumers | R11G11B10 texture | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Angular error, sharing/traffic/total timing | Source documents packed output path | Pending | Blocked by architecture | Resource is justified only if benchmark or another effect consumes it. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| X-023 | Final visibility power heuristic | Pinned XeGTAO README/source | Application | Algorithmic quality calibration | Source port applies pinned dynamic final power before quantized output; UVSR bitmask strength remains separate | Retain only inside XeGTAO profiles | None | None | Included in X-004; quality calibration rather than a speed optimization | Response curve and image comparison | Implemented and compiled | Different appearance by design | Implemented and retained | This heuristic is necessary for pinned High response and must not leak into the UVSR bitmask product equation. |
| X-024 | Hand-optimized bent-normal from/to rotation matrix | XeGTAO README/source | Bent-normal trace | Exact implementation optimization | No bent-normal output | No current control unless G-017 gains a consumer | None if absent | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Consumer audit; DXIL if implemented | Source reports nine fewer multiplies | No active product effect | Not applicable | Without bent-normal output the math does not execute; adding an unused checkbox/resource would violate the zero-cost purpose, while future bent-normal work can adopt it directly |
| X-025 | Delay bent-normal normalization until output | XeGTAO README/source | Bent-normal trace | Exact/Numerical by reassociation | No bent-normal output | No current control unless G-017 is implemented | None | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Consumer audit | Source optimization | No active product effect | Not applicable | Same applicability reasoning as X-024; it cannot speed AO that does not compute bent normals |
| X-026 | Packed R8G8B8A8 bent normal plus visibility | XeGTAO README | Output | Numerical | AO R16F, no bent normal | No current control unless a bent-normal consumer is added | Conditional packed output | None | Benchmark/quality estimate - expected 0 ms product improvement; horizon state should be cheaper but scopes are not yet comparable. | Angular/AO quantization and consumer audit | Source reports bent normals add about 25% in tested path | No active product use | Not applicable | Adding unused output is not defensible; Off would be free, but enabled mode has no consumer or requested benefit |
| X-027 | Automatic reference-scene heuristic tuning | Pinned XeGTAO README | Offline quality tuning | Algorithmic tool | Source port uses Intel's tuned High defaults; UVSR has no ray-traced MSE tuner | Optional offline tool only after runtime/image validation identifies a concrete retuning need | Offline artifacts only | None at runtime | Expected 0 ms runtime saving; this is a quality-calibration tool | Ground-truth metric and held-out scenes | Intel's defaults are ported; no UVSR tuner is implemented | Pending applicability | Not applicable | The direct source port satisfies the requested runtime implementation without inventing a new offline system. Retuning against UVSR scenes could overfit and is outside the performance profile unless manual validation demonstrates a reproducible quality gap. |

## Additional Primary-Source Candidates

| ID | Optimization | Source | Pipeline Stage | Exact / Numerical / Algorithmic | Current UVSR Equivalent | Proposed Control or Shader Permutation | Extra Resources While Enabled | Cost While Disabled | Expected Impact (Estimate, Not Measured) | Measurement Method | Result | Quality Result | Final Disposition | Reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| O-001 | Distance-increasing finite thickness | Visibility paper p.4 | Trace | Algorithmic | Constant thickness 0.5 target | Explicit distance-thickness profile | None | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Near/far thin geometry, camera-motion stability and timing | Paper says it reduces far over-attenuation but introduces slight camera dependence | Pending | Blocked by correctness | Distinct from horizon EMA and XeGTAO thin falloff; keep separate. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-002 | Constant versus exponential radial spacing | Visibility paper pp.7-9; SSRT3 `HorizonSampling` | Trace | Algorithmic | UVSR supports general exponent and target quadratic spacing | Curated exponent profiles; preserve target x^2 | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Near-detail/banding/cache/timing | Paper found exponential spacing improves near detail but costs more in its table | Pending | Already equivalent | Existing general exponent makes the behavior available; fixed specialization and controlled evidence remain. Code evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The current path already performs the work, so no extra option or inactive cost is required. |
| O-003 | Radial along-ray jitter | Visibility paper p.8 | Noise/trace | Algorithmic | Current scheduler already jitters within strata | Preserve current independent jitter and compare alternate schedules | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Banding/spectral/sample-position tests | Present conceptually | Existing quality | Already equivalent | Paper's anti-banding mechanism matches UVSR's product requirement |
| O-004 | Conditional GI source fetch after `newMask` | Visibility paper p.5; SSRT3 | GI trace | Exact | T-028/G-005 | Preserve | None | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Logical load and fixture inspection | Present | Exact | Already equivalent | Repeated here as the paper's central GI bandwidth result |
| O-005 | Full-mask early completion after final contribution | Visibility paper Algorithm 1 | Trace | Exact | T-013 | Preserve/test and compare against `ExactFullMaskEarlyExitOff` | One lazy off-control PSO | None | Expected 0 ms incremental because the safe optimization is already active; the off delta measures its value. | Completing-sample fixture and isolated timing | On is canonical; the packaged Off control completed the comprehensive Intel smoke. | Exact | Already equivalent | The retained path exits after the completing sample's newly visible radiance contribution; the benchmark-only Off wrapper does not weaken that product invariant. |
| O-006 | SSRT3 fixed dynamic-loop specialization | [SSRT3 compute shader](https://github.com/cdrinmatane/SSRT3/blob/main/HDRP/Shaders/Resources/SSRTCS.compute) | Trace | Exact | S-002 through S-027 | Fixed UVSR PSOs, not copied source | None | None | Medium estimate - removes dynamic trace control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL/order/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Pending | Implemented and retained | Reinforces compile-time specialization opportunity. Implementation evidence: fixed wrapper surface. It is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| O-007 | SSRT3 mip schedule `(step+1)/2`, capped at four | SSRT3 `HorizonSampling` lines 236-299 | Trace | Algorithmic | UVSR distance thresholds | Optional hierarchy benchmark only | Existing conditional hierarchy | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Mip/map/image/cache/timing | Source-defined | Pending | Not applicable | A simple fixed schedule but not equivalent to current geometry sampling. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-008 | SSRT3 ambient-subdivision fallback | SSRT3 `CSMain` | Trace/lighting | Algorithmic | No identical fallback | No product control unless a matching failure mode is identified | None | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Source and applicability inspection | Source-specific fallback | No demonstrated UVSR need | Not applicable | Current UVSR has explicit valid/background handling; copying a source-specific fallback without the same inputs would alter lighting and provide no measured performance purpose |
| O-009 | SSRT3 source-normal approximation | SSRT3 `NORMAL_APPROXIMATION` | GI trace | Algorithmic/Numerical | G-004 | Optional source-normal PSO | May remove normal binding | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Angular/GI/timing | Pending | Pending | Blocked by correctness | Source implementation uses adjacent reconstructed positions and cross product. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-010 | ASSAO 2x2 deinterleaved depth and AO domains | [ASSAO paper p.1](https://www.intel.cn/content/dam/develop/external/us/en/documents/adaptive-screen-space-ambient-occlusion-702913.pdf#page=1) | Depth/trace/filter | Exact data layout or Algorithmic sampling | D-019/L-004 | Experimental deinterleaved pipeline | Layered textures and passes | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Total pipeline/cache/image | Primary source designed its full pipeline around deinterleaving | Pending | Blocked by architecture | Keep filtering deinterleaved until final interleave if the experiment survives. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-011 | ASSAO paired mirrored sample taps | [ASSAO shader `SSAOTap`](https://github.com/GameTechDev/ASSAO/blob/master/Projects/ASSAO/ASSAO/ASSAO.hlsl#L554) | Trace | Exact when UVSR paired texels/order match | T-005 shares sides but not ASSAO estimator | Use paired invariant/load scheduling in fixed PSOs | None | None | Medium estimate - removes dynamic trace control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | Coordinate/order/DXIL/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exactness pending | Implemented and retained | Transfer only the implementation sharing, not ASSAO's estimator. Implementation evidence: paired negative-positive traversal. It is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| O-012 | ASSAO sample-table precomputed `log2(length)` | ASSAO `SSAOTap` and sample table | Trace | Exact for fixed chosen mip map | F-023/H-015 | Fixed sample constants | Small constant table | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Mip/value equality and DXIL | Source avoids runtime mip math | Pending | Not applicable | Relevant if hierarchy profile uses fixed offsets. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-013 | ASSAO adaptive base pass and importance-map refinement | ASSAO paper pp.1-2; shader lines 758-809 | Trace | Algorithmic | UVSR already has a different Adaptive Sparse Sampling path | Benchmark comparison only or reuse exact implementation ideas inside current adaptive path | Importance map and counter | None when adaptive/reference selected | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Quality/work distribution and total timing | Source ranges roughly 10-64 taps | Different adaptive algorithm | Not applicable | Product must retain current adaptive path; source can inform isolated improvement. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-014 | Every-ninth importance-map atomic update | ASSAO shader lines 1095-1183; CACAO around line 1409 | Adaptive trace | Algorithmic statistic | UVSR adaptive feedback differs | Optional current-adaptive counter-subsampling experiment if semantics allow | Existing/conditional counter | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Allocation statistic error, quality and atomic/timing | Both sources reduce atomic congestion this way | Pending | Not applicable | Changes global statistic; not an exact atomic optimization. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-015 | ASSAO detail AO | ASSAO paper p.3/shader | Trace/filter | Algorithmic | No identical detail term | Benchmark-only quality mode | None | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Stability/noise/timing | Paper says detail AO can be unstable and increases blur need | Likely quality risk | Not applicable | Not a speed optimization; retain only if needed for closest ASSAO comparison. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-016 | Omit reconstructed-normal normalization | ASSAO shader around line 525 | Trace | Numerical | UVSR normalizes relevant normals | Aggressive approximate-math PSO only | None | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Angular/mask/image/timing | ASSAO reports about 2.5% and PSNR 66.7 on its test | Pending UVSR | Blocked by correctness | Source evidence is implementation/scene specific and sector mapping is discontinuous. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-017 | AO guard-band and scissor optimization | ASSAO paper p.3; SAO performance accounting | Depth/trace/filter | Algorithmic/Exact scheduling by camera region | UVSR dispatches viewport extent | Optional valid-region/scissor plan if off-screen AO support is required | May enlarge or shrink resources by profile | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Processed-pixel count, edge behavior and total timing | SAO notes guard bands can add large pixel cost | Pending | Already equivalent | Must define camera-edge reconstruction semantics; headline resolution alone is insufficient. Code evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The current path already performs the work, so no extra option or inactive cost is required. |
| O-018 | Direct multiplicative AO blend/application | ASSAO paper pp.4-5 | Application | Exact only if UVSR composition equations match | Separate compute composition with nuanced fallback/direct/emissive rules | Use U-007 only after equation proof | None | None | High estimate - can remove full-resolution traffic/pass work; non-additive with the 7.9 MiB/frame fused-AO saving. | Lighting fixture and exact capture | Source allows direct blend for its AO semantics | UVSR semantics are richer | Blocked by correctness | A generic multiply blend would be incorrect; deferred integration must preserve UVSR rules. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-019 | CACAO independent depth and normal preparation without an inter-pass barrier | [CACAO manual](https://gpuopen.com/manuals/fidelityfx_sdk/techniques/combined-adaptive-compute-ambient-occlusion/) | Preparation | Exact scheduling | UVSR has no paired optional prep passes in target path | If both generated resources are enabled, dispatch independently and batch synchronization before consumer | None | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | GPU capture barrier/dependency and timing | Source explicitly states no barrier is required between independent dispatches | Exact | Not applicable | Apply only when new depth and normal prep coexist. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-020 | CACAO FidelityFX SPD hierarchy builder | CACAO manual; [AMD SPD](https://gpuopen.com/manuals/fidelityfx_sdk/techniques/single-pass-downsampler/) | Depth preparation | Exact architecture for same reduction; Numerical optional FP16 | Current one-dispatch local five-mip builder | Compare only if it builds the same consumed mips/reduction | Counter/LDS depending SPD integration | None | Expected 0 ms incremental - already active in the comparison path; retention prevents regression. | Value equality, barrier/dispatch and total timing | Current builder already captures core single-dispatch idea | Existing likely sufficient | Already equivalent | A wholesale SPD dependency is redundant unless measurements show current builder deficient |
| O-021 | CACAO packed FP16 depth/AO in LDS | CACAO shader lines 168-412 and 1419 onward | Filter/upsample | Numerical | FP32 local arithmetic/direct textures | P-028/R-028 mixed-precision LDS PSO | LDS only | None | Medium potential, low confidence - half-width blocks can cut selected live/storage width up to 50%, with conversion/error risk. | Half round-trip, edge/image, occupancy/timing | Primary source packs two halves per uint | Pending | Blocked by correctness | Bounded filter data is a conservative mixed-precision target. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-022 | CACAO split sample setup, fetch, and hit accumulation | CACAO shader around line 458 | Trace | Exact if ownership remains | L-015 | Fixed AO-only ILP PSO | None | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | DXIL dependencies/registers/mask/timing | Source explicitly separates phases; UVSR variant not implemented | Pending | Blocked by architecture | GI ordering limits transfer, but AO-only union semantics permit an isolated fixed-count PSO and zero Reference cost. The open questions are register pressure and whether dependency overlap survives DXC, which require F-030 plus target timing. This row is deferred with L-015; no architecture impossibility or measured loss has been shown. |
| O-023 | CACAO 5x5 and 7x7 LDS bilateral upsample | CACAO shader around line 1419 | Reconstruction | Algorithmic | UVSR compact/Gaussian | Packed-depth/AO LDS upsample profiles | LDS and packed source resources | None | Medium estimate - may reduce neighborhood traffic or improve reuse, but staging and synchronization can erase the gain. | Quality/timing/occupancy | Source provides both footprints | Pending | Blocked by architecture | Broader kernels are useful comparisons, not exact replacements. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-024 | CACAO lowest-quality 50% pixel discard | CACAO manual | Trace | Algorithmic | Half-resolution grid retains all half pixels | Checkerboard/lowest benchmark profile | Matching reconstruction | None | High producer estimate - roughly 50% fewer trace invocations, with reconstruction/history cost and quality risk. | Quality/flicker/timing | Source lowest preset discards half its deinterleaved pixels | Expected artifact risk | Blocked by correctness | Equivalent family to checkerboarding but source-specific profile warrants evidence. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-025 | SAO linearize depth once at mip zero | [SAO p.3, Eq.2](https://research.nvidia.com/sites/default/files/pubs/2012-06_Scalable-Ambient-Obscurance/McGuire12SAO.pdf#page=3) | Depth/trace | Exact or Numerical by rounding | F-001/P-008/P-009 | Prepared-depth profiles | Conditional depth | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Division count, depth/mask/image and total timing | Source amortizes division | Pending | Not applicable | At target radius 3 construction may cost more than saved trace work. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-026 | SAO reconstruct positions and normals from depth | SAO p.3, Eqs.3-4 | Trace | Exact position if proven; Algorithmic normal | Positions already reconstruct; normals come from G-buffer | F-003 and G-004/A-014 separate controls | Optional prepared depth; may remove normal read | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Position/angular/image/timing | Primary bandwidth strategy | Pending | Not applicable | Keep position and normal classifications separate. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-027 | SAO spiral samples and XOR per-pixel rotation | SAO p.3, Eqs.5-10 | Trace/noise | Algorithmic | Progressive radial strata and current schedulers | Benchmark-only sample-layout profile | None | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Distribution/cache/quality/timing | Source uses nine samples and seven turns | Different sample set | Blocked by correctness | May be cache-friendly but violates exact fixed-prefix identity. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-028 | SAO 2x2 bilateral support inside AO using quad derivatives | SAO p.4 | Trace/filter | Algorithmic | Separate compute resolve | Benchmark compact in-pass support if compute quad/wave semantics are reliable | None | None | Medium estimate - may reduce neighborhood traffic or improve reuse, but staging and synchronization can erase the gain. | Support/image/timing and portability | Source uses graphics quad derivatives without explicit synchronization | Pending | Blocked by architecture | Current compute architecture differs; wave assumptions must not leak into reference. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-029 | SAO RGB8 packed AO plus depth | SAO p.4 | Output/filter | Numerical | Separate R16F AO and depth sources | No direct product format; compare against P-012 and compact depth profiles | Conditional packed texture | None | Medium bandwidth estimate - compact AO can halve plane bytes only with a correct range-preserving encoding. | Quantization/cache/image/timing | Source halves fetch instructions/input bandwidth for its next pass | UVSR range/depth precision risk | Blocked by correctness | R16_UINT count+edges is cleaner for compatible bitmask AO; RGB8 depth is high risk. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-030 | SAO separable seven-tap bilateral with stride three | SAO p.4 | Reconstruction | Algorithmic | UVSR filters differ | Benchmark-only filter profile | Ping-pong texture | None | Low-to-medium estimate - plausible ALU, bandwidth, or quality leverage without reducing eight physical reads; no target measurement. | Quality/timing | Stride assumes AO pass already has 2x2 support | Pending | Blocked by correctness | Copying stride without the preceding support would create holes. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-031 | SAO CPU double-precision projection setup and infinite far plane | SAO p.2 | Camera/depth setup | Numerical robustness, not AO hot-path optimization | Renderer camera contract is outside isolated AO profiles | No AO control unless camera precision audit finds a defect | None | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Matrix/depth precision audit | Source presents convention-specific precision guidance | No demonstrated AO performance relevance | Not applicable | It changes global camera/projection behavior and cannot be isolated as an AO speed option; current task has no evidence of projection precision failure |
| O-032 | NVIDIA 4x4 deinterleaved AoS-to-SoA texturing | [NVIDIA presentation pp.5-7](https://d29g4g2dyqv443.cloudfront.net/sites/default/files/akamai/gameworks/samples/DeinterleavedTexturing.pdf#page=5) | Depth/trace | Exact layout if samples match | D-019 | D-019/L-004 | Sixteen quarter-resolution layers and preparation/reinterleave | None | Medium estimate - may reduce neighborhood traffic or improve reuse, but staging and synchronization can erase the gain. | Total pipeline/cache/image | Source reports up to 2.3x for large sparse jittered filters; four-by-four example uses two draws times eight MRT | Pending | Blocked by architecture | Headline win is pattern-specific and extra passes are material. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-033 | Filter-adapted product objective for spatial plus temporal filters | [Original FAST](https://arxiv.org/pdf/2310.15364) | Offline noise | Algorithmic | Existing FAST targets Gaussian plus EMA | N-013 vector/scalar asset regenerated for actual retained combined filter | Offline asset | None | Low speed estimate - unchanged read count; principal value is scheduler/filter quality and effective support. | RMSE/spectra/static-motion captures | Paper finds product objective best when both filters are active | Pending | Blocked by architecture | Use additive/separate objective when spatial filtering is absent, per source. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-034 | True vector distribution optimization instead of scalar-to-vector warp | FAST and NVIDIA STBN | Offline noise | Algorithmic | Scalar semantic offsets | N-011/N-017 | Conditional vector asset | None | Low speed estimate - unchanged read count; principal value is scheduler/filter quality and effective support. | Vector distribution and render error | Both sources warn mapping can damage spectra | Pending | Blocked by architecture | Particularly relevant if direction is packed with noise. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |
| O-035 | FP16 conversion-block specialization | AMD FP16 and Intel Arc guides | Trace/filter | Numerical | P-002/P-003 broad experiments | Use sizable homogeneous half blocks, not scattered casts | None | None | Medium potential, low confidence - half-width blocks can cut selected live/storage width up to 50%, with conversion/error risk. | Conversion count, registers/occupancy/timing, quality | Vendor guidance warns conversion overhead can erase benefit | Pending | Blocked by correctness | Defines how mixed precision should be implemented and measured. Correctness evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column retains validated behavior and exposes no unvalidated candidate. Reference has zero inactive cost; required range, mask, depth, ownership, or image proof is missing. |
| O-036 | Read-only cache versus LDS staging comparison | AMD GCN/RDNA guidance | Trace/filter | Exact | Direct SRV paths | Keep direct and tiled PSOs for isolated comparison | LDS only in tiled mode | None | No primary-profile impact - prerequisite/consumer absent, target stage inactive, or another row subsumes it. | Cache/LDS conflict/occupancy/timing | Vendor guidance says read-only cache can beat LDS when reuse is low | Exact | Not applicable | Prevents assuming a tile is always faster for sparse radial access. Applicability evidence: the linked primary source and corresponding UVSR files cited in the Current UVSR Equivalent column. The prerequisite/consumer is absent or another row subsumes it; Reference has no candidate cost. |
| O-037 | Minimize descriptors and use direct constants | Intel Arc guide | Binding/trace | Exact | Minimal candidate descriptor layouts are implemented; constants remain in a buffer | Direct/root constants where the NVRHI and D3D12 layout contract permits | None | None | Low estimate - descriptor minimization is already ranked; incremental constant-delivery benefit is unknown. | Root-layout/API audit, DXIL, timing | Minimal descriptors implemented; direct constants not implemented | Exact | Blocked by architecture | The retained half of this candidate is D-029/D-030. The remaining direct-constant path requires an NVRHI binding-layout/root-signature contract and a size/visibility audit; changing the global Reference root signature without evidence is not justified. A separate candidate layout may be possible and would preserve zero Reference cost, so the direct-constant portion remains deferred pending API inspection rather than proven impossible. |
| O-038 | Avoid uniform-array dynamic indexing | Intel Arc guide | Trace/noise | Exact through specialization | Runtime tables/indices in generic paths | Compile fixed table selections or direct constants in curated PSOs | None | None | Medium estimate - removes dynamic trace control from the roughly 1.2 ms trace; likely tens to low hundreds of microseconds, non-additive. | DXIL indexing/instructions/registers/timing | Candidate implemented in the current build; controlled Intel target timing is not yet measured. | Exact | Implemented and retained | Fixed schedules/counts create direct-index opportunities. Implementation evidence: compile-time fixed keys. It is CPU-selected; Reference retains canonical work and no target-GPU timing is claimed. |
| O-039 | Register-pressure and spill budget as a retention gate | Intel Arc and AMD guides | Verification | Exact evidence | Reproducible DXIL comparison exists; no physical-profile gate | Record temporary/register/spill data with every PSO result | CPU report only | None | Negligible estimated production GPU impact - validation/reporting is inactive outside debug or capture. | DXIL/ISA/tool counters | DXIL report complete for 18 core variants; physical registers, spills, and occupancy pending target Intel tooling | No image effect | Blocked by architecture | [`visibility-dxil-evidence.md`](visibility-dxil-evidence.md) records static IR, operations, branches, texture calls, and code size and shows fixed DXIL grows from 29,104 bytes at 8T to 55,260 at 20T. DXIL uses virtual SSA values; physical Xe-LPG GRF allocation, spills, SIMD width, and occupancy require the target Intel driver/GPA/IGA or `ocloc`, none of which was available. The build therefore lacks the physical retention gate; this is explicit target-evidence debt, not a measured pass. |
| O-040 | Debug-assertion validation permutation | Supplied prompt, Fast Math drawer | Validation | Exact debug instrumentation | Runtime safety checks only | Debug PSO/CPU assertions for trusted-unit, finite, range, and profile invariants | None beyond debug pipeline | None in Release/reference | Negligible estimated production GPU impact - validation/reporting is inactive outside debug or capture. | Invalid-input and assertion tests | Pending | No valid-input image change | Blocked by architecture | Completes the requested Full Safety, Trusted Release, Debug Assertions, and Selective Finite control family. Current-build inspection found no CPU-selectable implementation in the cited shader/configuration paths. The row's stated prerequisite or invariant must be resolved before an experiment can be valid. Where a separate PSO or conditional resource can preserve zero Reference cost, the omission remains an explicit deferred/unimplemented limitation; no attempt, generated-code comparison, or target timing establishes rejection. The architecture disposition records missing implementation, not impossibility. |

## Source Integration Boundaries

### Activision Approximation Boundary

The eight-tap workload and coupled PS4 implementation order came from the 2016
slide deck. The expanded 2019 `ATVI-TR-19-01` memo supplied a fuller analytical
derivation, described one, 16, and 96 effective sample directions after spatial
and temporal reuse, and reported a 0.5 ms PS4 baseline for GTAO plus GI. Neither
source published replacement shipping shader code, all reconstruction
constants, or a portable engine integration, so UVSR correctly labeled its
experiment an approximation.

That experiment is now removed. The planner no longer exposes the 4x4-by-6
scheduler or the scalar/packed profiles; the host no longer allocates prepared
depth, PS4 spatial output, or coupled history; and the source shaders and test
fixtures are deleted. Historical source and timing evidence remains in this
ledger. The independent analytic-horizon attribution profile and permutation
are also removed because they changed the estimator without proposing a
production UVSR result.

### Removed XeGTAO Source-Port Boundary

The three removed profiles were pinned to Intel XeGTAO 1.30 commit
`a5b1686c7ea37788eeb3576b5be47f7c03db532c`. For finite perspective inputs they
retained High's five smart-average depth mips, three slices by three steps by two
sides, golden-ratio step sequence, Hilbert/R2 noise, cosine-space horizon math,
falloffs, R8 packed edges, final power, and one sharp denoise pass. The profiles
isolate LUT versus inline Hilbert and mixed precision versus FP32. They use
separate depth, main, denoise, and composition passes and are not aliases for
the older analytic-horizon control.

The unavoidable UVSR adapters are explicit: world-space float normals are
transformed to view space instead of reading packed view-space normals; Intel's
96-byte constants prefix sits in a 176-byte row-major buffer; 16x16-padded
physical depth extents are clamped while logical view reconstruction stays
unpadded; depth remains R16F; AO is R16F rather than R8_UINT but emulates the
source's UNORM8 rounding; the LUT uses UVSR's binding; the phase is static
because there is no matching TAA history; bent normals, debug views, and
generated normals are omitted; and safety guards define invalid/bounds/
degenerate cases. Orthographic projection, nonzero viewport origin, or viewport
dimensions that did not exactly match the depth-texture extent fell back to
Reference. The complete family was later removed because every controlled Intel
variant was slower than canonical Reference. The current build creates no
XeGTAO PSO, resource, binding, history, or dispatch.

### Plain-English Production Disposition

Keep **Exact Fixed 8 + Fused Resolve & Apply** first and **Exact Fused Resolve &
Apply** second for manual image validation and a real performance profile. On
the Intel adapter they saved 20.6-22.4% and 17.7-18.8% respectively across two
controlled runs. Keep **Fixed 8** available as a supporting specialization, but
do not present its smaller 1.9-4.7% standalone result as equally conclusive.

Keep 16-bit storage as the default. The strongest clean paired format results
were final GI `RGBA16_FLOAT` at 8.39% faster than `RGBA32_FLOAT` and the
`R16_FLOAT` depth hierarchy at 3.88% faster than `R32_FLOAT`. Raw/cumulative GI
and final AO had smaller directional wins. Do not draw a production conclusion
from the inconsistent adaptive-feedback, temporal-GI, or temporal-depth pairs.

Do not promote any approximate per-function math option: almost every exact,
standard, and approximation trio was within +/-1%, and several approximations
were directionally slower. The per-function selectors, runtime constants,
benchmark matrix, and shader paths have been removed. Retained Reference and
curated shaders use static validated math without a user-selectable math mode.

Remove the Activision PS4 approximation: both variants were slower than
Reference and scalar/packed ordering changed between repeats. Remove the UVSR
horizon control as well: its historical 15.4% result exposed an approximately
18.2% complete/77.6% trace cost for bitmask work, but it changed the visibility
estimator and was not a production optimization. Remove XeGTAO because every
variant was slower than Reference on the controlled Intel driver.

Packed-edge 4x4 has been removed: optimized DXIL still contains 209 static
texture loads and no gather, and the measured reconstruction run was roughly
73% slower than its compact Reference. Compact-filter conservative algebra,
neutral group shapes, regressing radius clamps, diagnostic floors, feature-off
controls, and horizon controls have all been removed from the current build.

## Profiling and Timing Evidence

The target adapter is Intel Core Ultra 9 185H integrated Arc/Xe-LPG. Intel's
[product specification](https://www.intel.com/content/www/us/en/products/sku/236849/intel-core-ultra-9-processor-185h-24m-cache-up-to-5-10-ghz/specifications.html)
lists eight Xe cores, shared-memory integrated graphics, and DirectX 12.2;
Intel identifies Core Ultra Series 1 graphics as
[Xe-LPG](https://www.intel.com/content/www/us/en/support/articles/000098346/graphics.html).
No public Xe-LPG-specific optimization guide was found, so Xe-HPG guidance is
architecturally adjacent evidence only and every performance claim remains
target-run dependent.

[PIX hardware counters](https://devblogs.microsoft.com/pix/hardware-counters-in-gpu-captures/)
can support instruction, utilization, cache, barrier, and traffic evidence.
[PIX GPU-capture guidance](https://devblogs.microsoft.com/pix/gpu-captures/)
warns that replay does not preserve cross-queue overlap, so async candidates
must use a Timing Capture and complete-frame measurement. Intel's
[GPA overview](https://www.intel.com/content/www/us/en/developer/tools/graphics-performance-analyzers/overview.html)
states that 2025.1 was the final release and the product was discontinued in
2026; therefore maintained GPA availability cannot block completion. Available
Intel or PIX counters should include EU activity/stalls, sampler busy/stalls,
cache behavior, bandwidth, occupancy/registers, divergence, barriers, and UAV
traffic where the driver exposes them.

External timing scope must stay explicit:

- Activision slide 89 reports a 1080p half-resolution PS4 trace at 0.35 ms and
  spatial/temporal denoiser at 0.15 ms; it does not separately label pyramid,
  full-resolution application, or lighting composition.
- The expanded 2019 `ATVI-TR-19-01` memo reports baseline GTAO plus GI at
  0.5 ms on PS4 at 1080p with a standard half-resolution occlusion buffer. That
  combined result and the 2016 slide's trace/filter breakdown are publication
  provenance, not independent additive timings or UVSR measurements.
- The visibility paper's Table 1 is a sampling-pass comparison on RTX 2080;
  its roughly 0.3 ms denoiser is excluded. The reported 8, 12, and 16 counts are
  per side, so physical depth reads are twice those labels.
- XeGTAO README timings cover its stated prefilter/main/denoise scope and
  settings, not UVSR's full composition path. At the pinned revision it reports
  High at about 0.56 ms for 1080p RTX 2060, 1.4 ms for 4K RTX 3070, and 2.39 ms
  for 1080p i7-1195G7 integrated graphics; its same-system RTX 2060 comparison
  lists ASSAO Medium at about 0.72 ms. It also reports about 2.2 ms High and
  1.5 ms Medium at 1080p GTX 1050, with Low roughly two-thirds of Medium. The
  README reports optional FP16 gains of 5-20% on tested hardware and warns that
  some GPU/driver combinations regress. All are upstream provenance, not UVSR
  results or forecasts.
- SAO timings include their stated guard band and depth hierarchy. A hierarchy
  is retained in UVSR only when construction plus trace is a net win or a
  semantically compatible producer is genuinely shared.

### Controlled Intel 600-Frame Results

The target Intel Arc integrated GPU run used PBR Sponza Decorated at 1920x1080,
Benchmark Position 1, driver `32.0.101.8724`, 120 warm-up frames, and 600
measured frames per entry. Every reported profile completed 600/600 frames with
zero incomplete frames and `controlled_protocol_valid=true`.

| Matrix | Coverage | Main Result |
| --- | ---: | --- |
| Compatibility | 11 profiles | Fused AO saved 15.17% versus combined AO; all composed AO/GI, temporal, adaptive, packed-edge, and packed-FAST paths completed |
| New Candidates | 9 profiles, repeated | Fused saved 17.7-18.8%, Fixed 8 plus fusion saved 20.6-22.4%, and Fixed 8 alone saved 1.9-4.7% versus Reference |
| Fixed Sample And Horizon | 6 profiles | Horizon control 8 saved 15.4% complete and 0.455 ms trace versus Reference; Fixed 12/16/20 increased work as expected |
| Math Functions | 24 profiles | All eight standard/exact/approximation trios stayed near parity; no approximation earned production promotion |
| Paired Buffers | 20 profiles | Final GI RGBA16 saved 8.39% and R16 depth hierarchy saved 3.88%; inconsistent near-two-percent temporal/adaptive pairs were treated as noise |

In the repeated current-source new-candidate run, Reference measured
3.0240/3.6807 ms median/p95, fused measured 2.4889/3.2230 ms, and Fixed 8 plus
fusion measured 2.3999/3.1210 ms. The canonical Reference's large remaining
cost is full-resolution resolve plus composition, roughly 1.9 ms combined,
versus about 1.0 ms for trace. That stage evidence explains why fusion is the
most reliable same-estimator optimization.

The Intel XeGTAO ordering differs from the historical NVIDIA ordering: LUT/mixed
beat LUT/FP32, LUT beat inline Hilbert by roughly 1.0-1.7%, but every tested Xe
profile remained slower than canonical Reference. PS4 scalar versus packed
ordering changed between repeats, so neither is an Intel performance-profile
winner. These are still useful algorithm and quality controls.

### Historical Source-Port Smoke

The current local `new-candidates` run completed 9/9 profiles and 18/18 measured
frames with zero incomplete frames on the NVIDIA GeForce RTX 4090 Laptop GPU.
It used one warm-up and two measured frames per profile, embedded build identity
`e3f1908`, and exported `controlled_protocol_valid=false`; it is runtime coverage,
not a controlled result.

The same-estimator Fixed 8 plus fusion profile had the lowest short smoke median
at 0.220160 ms. Activision packed gather measured 0.320000 ms complete versus
0.323584 ms scalar and 0.024576 ms spatial versus 0.029696 ms scalar. XeGTAO LUT
mixed, inline mixed, and LUT FP32 measured 0.439808, 0.445952, and 0.367104 ms
respectively. The directionally faster FP32 result is consistent with Intel's
warning that optional FP16 can regress on some hardware/driver combinations, but
two NVIDIA frames cannot establish Xe-LPG behavior or change the forecast rank.

### Controlled Local 600-Frame Results

The final local run used PBR Sponza Decorated at 1920x1080 on the NVIDIA GeForce
RTX 4090 Laptop GPU, 120 warm-up frames, and 600 measured frames per profile.
Every profile completed 600/600 frames with zero incomplete frames and
`controlled_protocol_valid=true`. The independent precision sequence repeated
the same protocol for both entries. Final captures were clean, including the
scene that previously showed black PS4 AO artifacts.

| Profile | Complete Median / p95 | Local Disposition |
| --- | ---: | --- |
| Reference | 0.221184 / 0.263168 ms | Product baseline |
| Exact Fixed 8 | 0.223232 / 0.230400 ms | 0.926% median regression and identical trace median; drop standalone production consideration |
| Exact Fused Resolve and Apply | 0.207360 / 0.217088 ms | 6.250% median and 17.510% p95 saving; keep for product validation |
| Exact Fixed 8 + Fused Resolve and Apply | 0.203776 / 0.212992 ms | 7.870% median and 19.066% p95 saving; keep for product validation |
| Activision Scalar | 0.306176 / 0.340019 ms | 38.4% slower than Reference; retain as an algorithm comparison |
| Activision Packed Gather | 0.302080 / 0.313344 ms | 1.338% median and 7.845% p95 faster than scalar, but 36.6% slower than Reference; prefer for the comparison |
| XeGTAO LUT + Mixed | 0.413696 / 0.454656 ms | Removed; slower than canonical Reference |
| XeGTAO Inline Hilbert + Mixed | 0.419840 / 0.454656 ms | Removed; LUT was 1.463% faster internally but the family missed the replacement goal |
| XeGTAO LUT + FP32 | 0.349184 / 0.384000 ms | Removed; fastest local XeGTAO variant still did not justify a UVSR performance profile |

The independent precision repeat measured XeGTAO LUT/mixed at
0.394240/0.410726 ms and LUT/FP32 at 0.350208/0.371712 ms. FP32 was 11.169%
faster by median there and 15.594% faster in the main sequence, an observed
11.2-15.6% local range. None of the NVIDIA rankings predicts Xe-LPG behavior.

## Remaining Validation

The inventory, expected-impact ranking, final dispositions, and explicit
Activision plus removed-XeGTAO boundaries are reconciled to the current build.
Implemented rows identify their shader/profile or CPU resource-plan evidence;
blocked rows identify the missing correctness proof or complete selectable
pipeline contract; and all optional paths preserve the Reference zero-cost
contract by design.

Release compilation, all CTest targets, the Intel controlled matrices, and the
retained compatibility smoke cover the current feature surface. The remaining
product evidence is
exact same-phase GPU image equivalence, dynamic motion/disocclusion stress, and
Intel native ISA/register/occupancy counters. Target timing is now recorded, but
manual image acceptance remains required before promoting the measured
same-estimator finalists. A later measured rejection may replace a current
experimental or blocked disposition, but an external product timing or
unmatched scope cannot do so.

Local DXC compilation and reflection can establish that an entry point exists,
that compile-time macros remove code, and that advanced layouts match reflected
resource subsets. It does not establish Intel ISA instruction count, register
allocation, occupancy, EU activity, sampler pressure, cache hit rate, or target
bandwidth. Those counter fields and reference-versus-candidate exact same-phase
image acceptance remain deliberately unfilled until native tooling and the
remaining manual motion/image validation exist.

# unsafe Rust policy and audit

**read this before adding, changing, or reviewing unsafe code.** this is the authoritative project policy and visible registry, linked from the root README, agent instructions, specification, and checkpoint prompt.

## current audit status

| area | observed state |
| --- | --- |
| first-party Theta Rust | the compiler diagnostic module uses `forbid(unsafe_code)`. actual-NGAPI shaders use deny lints with U-002/U-005/U-007/U-008/U-012/U-013/U-014's narrow exceptions. U-003 registers physical access, and U-006 registers two native heap constructors and their source callers. U-001/U-004 register compile-only shader entries. the agfx Cargo owner denies unsafe except U-010's narrow native scopes. its copy runner forbids unsafe; U-009 registers the ordinary AGFX shader |
| native and shader boundaries | U-001/U-004 are compile-only. U-002 covers the scalar runtime shader and actual NGAPI caller, reviewed and executed with U-003's library in Debug/Release. U-005/U-007/U-008 cover uniform/divergent compute and vertex/fragment cube consumers through U-006, reviewed and executed in Debug/Release. U-009/U-011 add ordinary Rust compute, U-010 adds direct Rust copy owners, U-012 adds aggregate/alias and Function-memory execution, U-013 adds native storage-image read/write execution, and U-014 adds task payload/mesh execution |
| dependencies, generated code, macros, external hosts | separately patched NGAPI C++ and Vulkan SDK 1.4.357.0 run the diagnostic. these remain native dependencies, not safe Rust or an audited finished Rust port |
| RustGPU contribution | E-015 passed all 16 assembly-input probes in both tool configurations. E-018 adds aligned scalar accesses. E-019 adds qptr memory effects with a native optimizer prerequisite. E-020 adds address-only comparisons and wrapping offsets. U-001 has one compile-only entry and three raw accesses. E-022 adds U-003's two unsafe methods and two access fixtures. compilation alone does not establish runtime obligations |
| runtime and soundness evidence | E-021 and E-022 each pass six scalar Rust shader cases in actual NGAPI Debug/Release with explicit validation, first raw pointers and then the library. U-005, U-007 and U-008 each add eight native heap cases in each Debug/Release host. U-012 adds aggregate writes and loaded aliases under default/qptr in both host configurations. nonzero high-address runtime coverage remains open. this is bounded execution evidence, not a Rust boundary soundness proof |

source pins are in [research](docs/specs/001-theta-prototype/research.md), and current evidence is in [execution](docs/execution.md). **zero Rust implementation is not evidence of a safe completed system.** replace these rows with revision-specific Rust inventories as code is added.

the [RustGPU diagnostic module](tools/theta/rustgpu-instruction-probes.rs) uses upstream parser/linker/test APIs and standard owned vectors and strings. its only added lint is `#![forbid(unsafe_code)]`. it contains no unsafe operation, native call, or shader dispatch. the upstream compiler and SPIRV-Tools remain dependency boundaries with their own unsafe code. this probe does not audit those complete implementations or prove runtime soundness.

E-009 patch audit: the [rspirv loader patch](patches/rustgpu-prerequisites/rspirv-untyped-global.patch) changes safe enum matching and adds a safe serialization/placement regression. no unsafe operation, public memory-access API, native call, or lint exception is added. coordinator self-review checked the complete patch against its pinned source and the full rspirv test results. no independent review or complete dependency safety audit is claimed. the registry still needs no new U record.

E-010 patch audit: the [grammar/control patches](patches/rustgpu-prerequisites/README.md#modern-grammar-and-concrete-interfaces) use safe collections, checked name-index bounds, enum lookups and existing specialization ownership. they add no unsafe site, lint waiver, native ABI or runtime memory-access API. self-review covered complete diffs, source pins, regression outcomes and remaining failed gates. patched dependencies still contain unrelated unsafe code. no independent safety review or new U record is claimed.

E-011 patch audit: the [compiled-tools update](patches/rustgpu-prerequisites/README.md#compiled-descriptor-heap-tools) adds safe generator arguments and regressions with `forbid(unsafe_code)`. no FFI declaration, native wrapper, unsafe operation or lint exception changes. the pinned native library advances to SPIRV-Tools `9a49b0883b9b635689a85b5647dbfcb223268151`; existing wrapper/native boundaries remain dependencies, not a first-party soundness proof. coordinator self-review inspected source changes, generated-file identities, public C target-environment compatibility, build source selection and all eight integration results. no GPU dispatch, complete native safety audit, independent review or new U record is claimed.

E-012 patch audit: the [untyped-pointer changes](patches/rustgpu-prerequisites/README.md#untyped-pointers-and-static-type-operands) add safe IR fields, enum variants, traversal and inference logic. regression code forbids unsafe. no unsafe site, FFI, lint waiver or runtime memory-access API is added. coordinator self-review checked static type/value separation, dependency traversal, placement, interface collection and qptr behavior against the recorded tests. whole-buffer qptr usage remains diagnosed, and heap ID decorations remain failing. no independent review, GPU soundness proof or new U record is claimed.

E-014 patch audit: the [heap metadata changes](patches/rustgpu-prerequisites/README.md#heap-metadata-and-dependency-retention) use safe IR variants, dependency traversal and collection operations. no unsafe operation, FFI, lint exception or runtime access API is added. coordinator self-review checked annotation ordering, constant/type remapping, liveness, member-offset deduplication, diagnostic paths and the complete passing installed-tools matrix. SPIR-T structural tests retain `forbid(unsafe_code)`. no independent review, full dependency safety audit, GPU soundness result or new U record is claimed.

E-015 audit: the same compiler sources pass the compiled-tools configuration against the E-011 native dependency pin. no source boundary changed for this run. the unsafe inventory, self-review limits and absence of a GPU soundness claim remain as above. new AGFX A-006/A-007 notes are source observations and future contract requirements, not implemented native calls.

E-016 audit: the [explicit pointer-width foundation](patches/rustgpu-prerequisites/README.md#explicit-rust-pointer-width-foundation) changes target selection, JSON generation and test selection using safe Rust. source probes use `forbid(unsafe_code)` and no raw-pointer dereference. synthetic high bits test integer representation only. raw casts retain rejection. coordinator self-review checked the complete diff, strict target agreement, layout assertions and all emitted semantic instructions before approving expectations. no unsafe site, native call, FFI or lint waiver is added, and no U record is needed. independent review, physical access and GPU soundness remain unproved.

E-017 audit: the [physical conversion patches](patches/rustgpu-prerequisites/README.md#physical-address-conversions) use safe compiler transformations and non-executed test fixtures. source probes forbid unsafe and only transport addresses. coordinator self-review checked source-signed extension, narrowing after 64-bit conversion, storage-class conflicts, final alias placement, preservation of explicit restrictions, qptr's concrete-address boundary and exact test output. no unsafe operation, FFI, reference fabrication, safe memory-access API or lint waiver is added. pointer-slot/array annotation checks are structural evidence, not a complete aggregate-access guarantee. no new U record, independent review or GPU soundness claim is warranted.

E-018 audit: [U-001](#u-001-compile-only-physical-u32-access) registers the first compile-only shader access boundary. all newly changed compiler transformations and the other new source fixture remain safe Rust. self-review checked the narrow exception, all three operation contracts, exact emitted alignment and logical interfaces, successful deny lints, and the full compiler/source regression gates. native calls, FFI declarations and runtime resource owners did not change. the dependency/compiler/macro implementations are not thereby fully audited, and no GPU execution or independent safety review is claimed.

E-019 audit: the [qptr memory patches](patches/rustgpu-prerequisites/README.md#qptr-memory-operands-and-volatile-loads) use safe Rust IR fields, traversal and regressions. no unsafe site, lint exception or public access API is added. the native SPIRV-Tools change reads an existing instruction's memory mask only after checking its opcode and operand count. it changes optimizer classification inside an existing C++ dependency, with no FFI or wrapper change. self-review covered the complete diffs, scope-ID traversal, effect retention and ordinary-load elimination controls. no new U record, full native safety audit or independent review is claimed. U-001 remains compile-only.

E-020 audit: the [pointer-operation patch](patches/rustgpu-prerequisites/README.md#typed-pointer-operations) changes safe compiler arithmetic and adds six source fixtures with `forbid(unsafe_code)`. they compare or transport raw addresses without dereferencing or creating references to synthetic allocations. coordinator self-review checked pointer width, stride, modular arithmetic, logical-storage rejection and the explicit unsupported helper diagnostics. a same-toolchain native Rust oracle exercises wrapping/address comparisons without dereferences. no new unsafe site, FFI, lint exception, safe memory-access API or U record is added. in-bounds methods and GPU/runtime obligations remain unproved, and no independent review is claimed.

E-007 diagnostic audit: [capabilities.cpp](tools/theta/ngapi-probe/capabilities.cpp) checks device creation before querying borrowed caps, waits idle and destroys the same device. it exposes no Rust API. the other target compiles unchanged NGAPI `d60b10bdfe15c0f350d6d291d8e06afef3fe7d38` command-context tests, which own their allocations and completion. Vulkan SDK 1.4.357.0 supplies headers/import libraries and the explicit validation layer. these dependencies contain native code and raw memory operations, excluded from the first-party Rust count, not claimed safe through that exclusion. review: coordinator self-review, 2026-09-19, source/control-flow inspection plus Debug/Release execution. no independent review.

the [SPIR-V fixtures](tests/compiler-probes/README.md) are assembly-only tool inputs with raw-address and descriptor assumptions documented at their entry. none was dispatched. compiler validation does not discharge their allocation, bounds, alignment, aliasing, visibility or lifetime obligations for future execution. no safe shader API or Rust lint exception is introduced by these fixtures.

## safe default and exceptions

ordinary ownership, collections, state machines, shader math, formatting, fixtures, reporting, and compiler transformations use safe Rust. a close source port preserves behavior, not unsafe C/C++ pointer idioms.

an exception needs a specific required operation that a suitable safe operation cannot provide. convenience, silencing the borrow checker, copying upstream idioms, reducing line count, and unmeasured performance claims are insufficient. first try a simpler representation or ownership model. using a dependency merely to hide unsafe code does not remove the obligation to understand its exposed guarantees.

| candidate boundary | why unsafe may be necessary | what remains safe |
| --- | --- | --- |
| native Vulkan, OS, or compiler FFI | the foreign API cannot express all lifetime, ABI, threading, and synchronization obligations in Rust's type system | validated parameters, ordinary bookkeeping, state transitions, and ownership outside the call |
| mapped native memory | constructing or accessing Rust values over a foreign allocation requires validity, bounds, layout, and lifetime proof | bounds calculations and safe views whose complete obligations are enforced |
| physical GPU-pointer operations | the shader may access a raw device address whose validity, aliasing, and lifetime depend on the host and GPU work | address transport, checked arithmetic, and shader logic that do not themselves require unsafe operations |
| native escape hatches | a caller can bypass owned resource and synchronization rules | the ordinary API, when it actually prevents such bypasses from invalidating safe operations |

these are investigation candidates, not permission to mark whole modules unsafe. no exception is justified until it has a completed record and source-local explanation.

## enforcement and source documentation

use `#![forbid(unsafe_code)]` in first-party crates or modules needing no exception. in a crate with necessary boundary code, use `#![deny(unsafe_code)]` and the smallest documented `#[allow(unsafe_code)]` scopes. do not put `forbid` on an ancestor that must contain an exception. use `#![deny(unsafe_op_in_unsafe_fn)]` so an unsafe function still identifies its unsafe operations explicitly. adapt equivalent enforcement to the supported upstream RustGPU toolchain without unrelated policy churn.

place a concrete `SAFETY:` comment beside every unsafe operation. cite its registry ID and explain how this call establishes the applicable invariants, rather than saying only “the caller guarantees safety.” public unsafe functions and traits need a `# Safety` contract. unsafe trait implementations, foreign interfaces, and unsafe attributes need equivalent local justification. a shared record may cover several sites only when it enumerates them and each site explains its local proof.

a safe signature promises that safe callers cannot violate the implementation's memory-safety preconditions. use it only when the implementation enforces that promise. raw pointer access, arbitrary shader dispatch, foreign resource ownership, and synchronization bypasses must not acquire safe signatures merely to reduce the unsafe count. if caller obligations remain unenforceable, expose a narrow, explicit unsafe contract.

## boundary registry

current entries: **U-001** through **U-014** distinguish compile-only fixtures, contributed libraries, direct Rust Vulkan owners and actual NGAPI consumers. add one `U-nnn` entry here for each distinct implemented boundary. this file owns the overview and complete review record. large supporting proofs may be linked, but the necessity, obligations, status, and source sites must remain visible here.

each entry must contain all fields below. mark an inapplicable field with a reason.

| field | required content |
| --- | --- |
| identity and scope | stable U ID, concise operation, host/shader/compiler classification, source files and symbols, exact revision or dirty-diff identity, all relevant call sites |
| necessity | required behavior, why safe Rust cannot express this operation, alternatives considered, and why a simpler or existing safe operation is insufficient |
| interface and owners | safe or unsafe public boundary, who creates/validates/uses/retires the resource, and exactly which obligations belong to callers |
| pointer and value validity | allocation/provenance, byte range, overflow, alignment, initialization, valid value/bit patterns, layouts and ABI, allowed casts, and reference creation if any |
| lifetime and aliasing | allocation lifetime, outstanding CPU/GPU users, mutable/shared aliases, resource movement, mapping/unmapping, and descriptor/address invalidation |
| concurrency and completion | thread affinity, Send/Sync claims, host/device synchronization, queue completion and visibility, and descriptor/resource retirement |
| failure paths | partial initialization, cleanup, device loss, allocation failure, panic/unwind or foreign exceptions, and guarantees after errors |
| proof at each site | the checks, types, ownership rules, or external specification that establish each precondition. identify assumptions the program cannot validate |
| verification | meaningful positive and negative tests, compiler/lint results, native validation where relevant, tools/configuration, evidence links, and untested conditions |
| review | reviewer identity, date, revision, findings, resolution, and whether review was self-review or independent. no invented independent approval |
| remaining risk and status | proposed, implemented-unreviewed, reviewed, or retired. list open assumptions and blockers without implying tests prove soundness |
| change history | record contract changes, added/removed sites, changed callers or dependencies, and the reason to repeat review |

### U-001. compile-only physical u32 access

status: reviewed for compile-only use. execution requires a host that establishes the contract below. no runtime safety result is claimed.

| field | record |
| --- | --- |
| identity and scope | `tests/compiletests/ui/physical_storage/access_physical64.rs::main` in the [aligned-access patch](patches/rustgpu-prerequisites/rustgpu-physical-access.patch), based on `0983e4e07ca382804c9f0084b36e049f8613d211`. fixture SHA-256 `18722cce7665957041773f09eeb42b6e343d8dc3a8ebbe6fc86868c8e8e33243`. the patch manifest pins the verified compiler commit. sites are the unsafe entry declaration and its two reads and one write. |
| necessity | T011 needs actual Rust load/store lowering through a physical address. safe address transport cannot generate those memory operations, and an assembly-only fixture cannot prove Rust lowering. a conventional descriptor access would exercise a different pointer class. |
| interface and owners | `pub unsafe fn main` states the caller obligations. no safe wrapper is exposed. the compiletest harness compiles only and never invokes the entry. a future runtime host must allocate, initialize, validate, synchronize and retire the allocation before this fixture may execute. |
| pointer and value validity | caller supplies a real device address for a live four-byte allocation containing an initialized u32, with alignment at least four. all u32 representations are valid. address transport is u64 and does not perform offset arithmetic. no Rust reference is fabricated from the raw address. logical output is a separate two-element u32 array. |
| lifetime and aliasing | physical allocation remains live and exclusively accessible by the one invocation until queue completion. it is disjoint from push constants and output, with no concurrent CPU/GPU aliases. the fixture does not move, map, free or reuse resources. |
| concurrency and completion | dispatch exactly one invocation. host establishes prior initialization visibility and waits for completion plus visibility before reading or retiring resources. the fixture provides no Send/Sync implementation, queue API or completion primitive. |
| failure paths | fixture owns no allocation and creates no native object. compilation/validation failure prevents dispatch. it does not attempt to recover from an invalid pointer. a future host must handle allocation/pipeline/submit failure, device loss and retirement separately. |
| proof at each site | first read relies on the entry's initialization/alignment/exclusivity contract. write uses the same valid allocation and wrapping u32 arithmetic. final read observes this invocation's initialized value. local SAFETY comments enumerate these obligations. the compiler cannot establish host allocation validity or completion. |
| verification | the baseline failed SPIR-V validation for missing Aligned operands. the corrected Rust source compiles and validates with the pinned nightly and compiled tools. manually reviewed output has two physical loads and one store with Aligned 4, while logical accesses carry no alignment. the two-target source gate passes without blessing. no CPU call or GPU dispatch of this fixture occurred. synthetic addresses are never dereferenced. |
| review | coordinator self-review, 2026-09-19, fixture hash above and E-018 patch. source inventory found one unsafe function, three explicit unsafe blocks and one narrow `allow(unsafe_code)`, all covered here. crate-level `deny(unsafe_code)` and `deny(unsafe_op_in_unsafe_fn)` compile successfully. reviewed the spirv entry macro's emitted interfaces and complete instruction stream. no independent review or complete dependency/macro safety audit. |
| remaining risk | compile-only use avoids execution but does not prove GPU safety. the host obligations are currently unenforced because no runtime host for this fixture exists. aggregate access, unaligned access, reference creation, races, atomics and arbitrary dispatch remain outside this record. |
| change history | introduced for the aligned scalar-access probe after E-017. repeat review when adding sites, changing data types/layout, exporting a callable library API or introducing a runtime caller. |

template for another boundary:

```text
### U-nnn. operation name
status / revision / scope / source sites:
required behavior and insufficient safe alternatives:
public interface and owner/caller obligations:
validity, layout, bounds, alignment, initialization, provenance:
lifetime, aliasing, synchronization, threading, completion:
failure and cleanup behavior:
how each source site establishes the contract:
tests, validation, evidence, and limits:
reviewer, review type, date, findings:
remaining risks and next action:
change history:
```

### U-002. actual-NGAPI physical u32 readback

status: reviewed and executed for the library-adapted scalar dispatch. [E-022](docs/execution.md#e-022-2026-09-19-tested-the-physical-pointer-library) records the current evidence and limits. E-021 retains the earlier raw-pointer consumer result.

| field | record |
| --- | --- |
| identity and scope | [physical_readback.rs](tools/theta/ngapi-probe/physical_readback.rs)::`computeMain`, its one unsafe entry declaration, four explicit unsafe library-call blocks and one narrow lint exception. [physical_readback.cpp](tools/theta/ngapi-probe/physical_readback.cpp)::`run_variant` is the only runtime caller through actual NGAPI. the [shared host layout](tools/theta/ngapi-probe/physical_readback.hpp) contains two u64 addresses, matched by transparent PhysicalPtr<u32> shader fields. generated metadata records the exact shader source, module, compiler and library hashes. |
| necessity | this consumer must exercise Rust-generated loads and stores through actual device addresses. safe integer transport and assembly-only fixtures cannot prove that path. ordinary descriptors would change the tested interface. no existing safe first-party allocation-bound shader pointer API exists. |
| interface and owners | the shader entry is unsafe with a complete caller contract and no safe wrapper. the C++ diagnostic owns upload, source, destination and readback heaps, pipeline and timeline. it accepts only the two embedded modules generated from this fixture, not arbitrary user-supplied shader files. NGAPI and Vulkan are existing native dependency boundaries, not a finished safe Rust host. |
| pointer and value validity | the host obtains source/destination addresses from separate actual 16-byte NGAPI allocations, checks nonzero addresses, four-byte alignment, sufficient size, non-wrapping range ends and disjoint ranges. nonzero upper 32 bits are a separate coverage field, not an allocation validity condition. u64 root offsets are 0/8, total size 16, checked in both languages. the shader reads one initialized u32 and writes three u32 values at destination offsets 0/4/8. all u32 bit patterns are valid. no Rust reference is fabricated from a physical address. host copies use memcpy to/from owned fixed arrays. |
| lifetime and aliasing | all four heaps and the pipeline remain live until the timeline covering the dispatch/readback has completed. source and destination are disjoint. one invocation alone accesses them while submitted. upload/readback mapped bytes are accessed by the host only before submit or after completion. subsequent cases wait before reusing the allocations. root bytes are copied during command recording. |
| concurrency and completion | one host thread and one queue. initialization uses coherent upload memory and a transfer-write to compute-read/write barrier. dispatch is exactly one workgroup with one invocation. compute/initial transfer writes become transfer-readable before copying both allocations to readback. transfer-write to host-read visibility plus timeline wait precedes CPU inspection. shutdown waits idle before immediate destruction. no Send/Sync claim or concurrent host access is exposed. |
| failure paths | compile/validation/profile mismatch prevents generating the include. unsupported device creation returns 77 and is not a pass. range/oracle failures return nonzero and still drain/retire owned resources. no command buffer is begun before range checks. after begin, the straight-line path submits it exactly once and waits. NGAPI aborts for native allocation/pipeline/device failures rather than supporting recoverable errors, so process failure/timeouts must be classified separately. the shader owns no allocation and cannot recover from a violated caller contract. |
| proof at each site | first read uses the initialized source established by the copy/barrier. first write uses the first live destination word and wrapping arithmetic. subsequent writes use safe wrapping pointer transport to offsets four/eight, whose complete range is checked by the host before dispatch. local SAFETY comments cover each block. trusted dependency assumptions include allocation non-aliasing, coherent mappings, Vulkan root/interface layout, command execution and timeline guarantees. checks cannot independently validate device-address provenance. |
| verification | the library-adapted opt0/opt3 modules compile and validate with the same reviewed root and scalar access profile. the current Debug/Release runtime gates both passed all six cases. opt0/opt3 Rust compilation and SPIR-V validation passed. disassembly was manually inspected for root offsets, physical pointer class, one Aligned 4 load and three Aligned 4 stores, Vulkan memory model and absence of descriptor bindings. both deny lints remain active. MSVC Debug/Release builds pass /W4 /WX. all 16 CPU range/oracle controls pass, including overlap, null, misalignment, insufficient sizes, overflow and corrupted output words. each host configuration passes all six GPU cases with exact value/address/guard/source readback, explicit core/synchronization validation requested, confirmed instance/device layer insertion and no native validation diagnostics. eight unchanged result-gate unit tests passed at E-021. actual upper address words are zero, recorded as missing high-address runtime coverage. no invalid/synthetic address is dispatched. |
| review | coordinator self-review, 2026-09-19. shader source SHA-256 `cf8ed25ba9f0845cf6916e78530001eb4fe6b8a7566d3b5c835ec10a04cee8f9`, host SHA-256 `448f29eab22bbff9fc0715a2fdc047903a055720edd25d08fb4469e61b0fce4f`. reviewed every shader access and host source line, NGAPI entry/feature setup, allocation/mapping policy, root-data copy, copy/barrier/timeline sequence and immediate destruction contract. the entry macro is the same upstream attribute/lowering boundary used by U-001. the current adaptation delegates the same four operations to U-003, transports offsets as u64 and keeps the native allocation/barrier/retirement path unchanged. both current instruction streams were reviewed before dispatch. current Debug/Release controls and runtime gates passed, and both final records/streams were inspected. no independent review. |
| remaining risk and status | reviewed for the stated scalar diagnostic. this is not a soundness proof for all raw-pointer operations, a Rust host audit, native descriptor-heap shader proof, cube/parity result or Linux result. native failure recovery and dependency internals retain their existing limitations. |
| change history | E-026 revalidated the unchanged scalar source/payloads in both host configurations after reviewing NGAPI's added non-uniform sampled-image feature requirement. all six cases and 16 CPU controls still pass with zero validation diagnostics. E-022 adapts four accesses and the root to PhysicalPtr. reviewed its complete caller obligations against U-003 before dispatch. introduced for the first real-address NGAPI consumer after E-020. first native attempts stopped before dispatch on the initial high-address coverage check. the detailed allocation record established valid low addresses, so review separated that coverage condition from memory validity. the shader contract and all range/synchronization checks remain unchanged. this boundary is separate from compile-only U-001 and generic compiler patches. allocation, layout, dispatch count, synchronization, shader or host dependency changes require renewed review. |

### U-003. PhysicalPtr shader-library access

status: reviewed for the declared library and fixture contracts. the actual U-002 consumer has been adapted and reviewed. both Debug/Release scalar dispatch gates passed at E-022.

| field | record |
| --- | --- |
| identity and scope | owned RustGPU `crates/spirv-std/src/physical_ptr.rs`, at `ff7883bd22981f0a67ffddb19d159f09542a14bd`, source SHA-256 `bf1bd644cebffde7a20c738672fd6177f49a50682b928f9e8dd9c4517872881a`. two unsafe methods (`read`, `write`), one explicit operation block and one narrow lint exception each. compile-only `physical_storage/library_access_physical64.rs::main` adds one unsafe entry, three call blocks and one exception. `library_aggregate_physical64.rs::main` adds one unsafe entry, two call blocks and one exception. the verified export is rustgpu-physical-library.patch in the source manifest. |
| necessity | reusable physical memory access cannot be expressed by safe integer transport. caller-owned GPU allocation, visibility and aliasing obligations are not encoded by a copied address. raw reads/writes avoid fabricating Rust references or introducing a misleading safe ownership API. |
| interface and owners | PhysicalPtr stores a u64 plus a zero-sized non-owning marker. construction, cast, equality, null test and wrapping element arithmetic are safe and make no validity claim. read/write are unsafe, require Copy values and are absent on ordinary 32-bit SPIR-V targets. the existing gpu_only macro provides CPU panic stubs rather than host dereferences. host allocation/initialization/submission/retirement remain external. no Deref/reference/restriction API is exposed. |
| pointer and value validity | read/write require a nonzero, appropriately aligned device address, even for a zero-sized T. the complete size_of(T) range belongs to a live allocation with the shader's T layout. reads require initialized valid T bits visible to the invocation. writes take a valid typed value and do not read old bytes. embedded resources/lifetimes must remain valid as required by T. address arithmetic is explicit u64 modulo arithmetic and never establishes a valid allocation. no physical-address-to-reference cast is performed. |
| lifetime and aliasing | the marker owns no allocation or T value. copies retain only address bits. read requires no conflicting write or incompatible reference alias. write requires exclusive access and no conflicting CPU/GPU users. allocation and embedded-resource lifetimes extend through their last use and queue completion. no inferred RestrictPointer promise is added. |
| concurrency and completion | the marker permits ordinary address transport without imposing T's auto traits. this does not authorize memory access on the host or another invocation. callers establish initialization visibility, barriers, completion and retirement. compile-only fixtures declare exactly one invocation and disjoint interfaces/allocations. U-002 uses the scalar methods under its separately reviewed allocation, visibility and completion contract. |
| failure paths | unsupported ordinary shader-target access should fail method lookup, not truncate addresses. host access methods are panic stubs generated by gpu_only. no allocation or cleanup occurs in the library. invalid GPU addresses remain a violated unsafe contract, not a recoverable error. compiler type/capability limitations remain explicit rather than implying all Copy types compile. |
| proof at each site | library read delegates to raw read under the method's range/value/alignment/lifetime/alias contract. library write delegates to raw write under exclusive complete-range validity. the scalar fixture's first read uses initialized visible memory, its write uses the same exclusive u32, and its final read observes that write. the array fixture copies a complete initialized 12-byte source to a disjoint writable destination. all sites have local SAFETY comments. |
| verification | four native layout/arithmetic tests and two existing compile-doc tests passed, with one pre-existing ignored doc. all 19 required source/target pairs passed after manual expectation review. 46 compiler and three shared-type tests passed, with four pre-existing macOS ignores. both new aggregate-linker cases preserve Aligned 4 through ordinary/qptr linking and performance optimization. 31 adjacent cases passed. library Clippy with denied warnings, warning-free docs, full workspace and direct fixture formatting passed. unit tests use only safe address transport and wide-integer arithmetic references, never synthetic dereferences. aggregate compilation and default/qptr optimization pass. U-008 adds actual 24-byte six-float vertex-record reads at opt0/opt3 in Debug/Release. this does not execute the separate u32-array copy fixture or prove arbitrary aggregate access. actual scalar library access passes through U-002 in both host configurations. |
| review | coordinator self-review, 2026-09-19. inspected every new source line, both unsafe method bodies, five unsafe fixture call blocks, narrow lint scopes, target cfg, transparent layout and safe arithmetic. reconciled the inventory against the complete patch. inspected the existing gpu_only macro: it retains method attributes/signature, emits the original body only for SPIR-V and a CPU panic stub otherwise. no macro/FFI implementation changes were made. native access stubs have no dereference. compiler transformations and transport fixtures add no unsafe operation. no independent review. |
| remaining risk and status | reviewed bounded library contract. no safe memory-access, general Copy-type, aggregate-runtime, host portability or dependency soundness claim. ordinary pointers/usize inside T require separate ABI agreement. native heap lookups, volatile/atomic/copy/reference APIs remain outside this library. |
| change history | introduced after E-021's direct raw-address consumer. U-002 records the reviewed actual NGAPI scalar adoption. future caller/layout/feature changes require renewed review before dispatch. |

### U-004. compile-only native-heap source probe

status: reviewed for the compile-only fixture contract. no runtime dispatch or safe heap API.

| field | record |
| --- | --- |
| identity and scope | owned RustGPU `tests/compiletests/ui/descriptor_heap/auxiliary/sample_body.rs::main`, shared by seven target-feature/optimization wrappers, at e2858253836b9236263c20c6e372e3b7913e822f. shared source SHA-256 `6ff848b7a523663b38cf1b960def40e7f05aaa9bdf3873db409d36cd28db3fa3`. one unsafe entry, two explicit blocks and one narrow lint exception. one block is inline SPIR-V assembly, the other calls U-003's PhysicalPtr<Vec4>::write. |
| necessity and owners | a source-level probe must cross the existing RustGPU assembly boundary to test native heap instructions before choosing a larger public API. no current safe native-heap API enforces validity. the fixture owns only initialized local coordinates/color. a hypothetical host owns heaps, descriptors, image, sampler, output allocation and submission. the compiletest never invokes it. |
| validity and layout | resource slot 1 and sampler slot 2 require live initialized compatible sampled 2D f32 image/sampler descriptors, with strides matching each native descriptor type. image layout and contents must be sampleable/visible. output must be a nonzero 16-byte-aligned complete 16-byte device range. SPIR-V assembly writes one initialized local Vec4 before fallthrough, with aligned live local Rust operands. no descriptor reference or allocation is fabricated. |
| aliasing, concurrency and lifetime | one invocation, no descriptor/image updates during use, and a disjoint exclusive output allocation. heap, descriptor and resource lifetimes extend through queue completion. host barriers and completion establish initialization/output visibility and retirement. no ownership, Send/Sync or restriction guarantee is provided by the entry. |
| failure paths and site proof | compilation is the only operation currently run. failed parsing/linking/validation is a failed gate, never permission to dispatch. assembly reads only the contract's uniform heap slots and stores through a live local mutable reference. the second block delegates complete output-range validity to U-003. ordinary vector arithmetic remains safe. no cleanup or host FFI is added. |
| verification and review | coordinator reviewed both local SAFETY comments, deny lints, initialized assembly operands, descriptor/native-size assumptions and the absence of any runtime caller for this compiletest. the baseline exposed descriptor-size constants emitted after their decorated array types. the candidate compiles and validates default/optimized/qptr variants. missing capability and extension produce expected validator errors. all 26 required source/target pairs and 46 compiler plus three shared-type tests pass, with four existing macOS unit ignores. adjacent checks pass 120 cases but fail the existing subpass-coordinate case, which also fails with the exact prior compiler sources. that gate remains failed. this is a boundary review for a non-dispatched fixture, not a soundness or native-heap execution result. |
| history and limits | introduced after E-022 to isolate T014's Rust assembly/type/global-placement path. no independent review. a runtime caller or public heap utility requires a separate complete review and updated registry before execution. |

### U-005. actual NGAPI native heap sample

status: reviewed and executed through the U-006 library constructors in Debug/Release, after the E-024 assembly consumer.

| field | record |
| --- | --- |
| identity and scope | `tools/theta/ngapi-probe/native_heap_sample.rs::computeMain`, one unsafe entry, two explicit blocks and one narrow lint exception. the first block calls both U-006 constructors, existing sample_by_lod performs sampling, and the second calls U-003's PhysicalPtr<Vec4>::write. the matching `.hpp` owns the native root layout. source SHA-256 `6535b899ad04c7c7fd136b6ee5e6a5f3b8b242f836dc0627b4e9b5ff0b47d75c`. reviewed opt0/opt3 payload hashes are `d6647313e4f38c5bd4e69c7c90bca4ec5ec0def104437e53058e75f7d0273b3f` and `b43bc4cbf20933d51482986c3ccfd54fb76822bf3ce289b9e5b67251ebc19cf4`. the matching `.cpp` is shared with U-007 through compile-time fixture selection. E-026 renewed review verifies the one-lane ranges and oracle are preserved. |
| necessity and owners | U-006's constructors cannot enforce external native allocation and descriptor lifetimes, so this actual consumer remains an unsafe entry. Rust owns local coordinates, opaque image/sampler values and the sampled Vec4. the host owns every native allocation, texture, descriptor, pipeline and completion point. the shader acquires no ownership or Rust descriptor reference. |
| validity and layout | root u64 output, u32 resource, u32 sampler at offsets 0/8/12, size 16. uniform indices are within four initialized native slots per heap, with queried image/sampler descriptor sizes. two sampled 2D RGBA8 UNORM images have initialized visible contents in GENERAL layout. nearest repeat/clamp samplers have comparison disabled. output is a nonzero 16-byte-aligned complete exclusive 16-byte range. |
| aliasing, concurrency and lifetime | exactly one invocation writes output. its allocation is disjoint from all descriptors, textures and other storage. no conflicting updates while submitted. all native objects and storage survive queue completion. transfer/compute/host barriers and completion precede readback, reuse and retirement. U-006 initializes the two opaque handles, which stay within this entry and its host-controlled lifetime. |
| caller obligations and failure paths | native caller checks format support, descriptor sizes, texture size/alignment, complete mapped ranges, aligned nonzero output and required creation results. five independent live allocations hold the two descriptor heaps, upload, output and readback, with a separate non-overlapping texture heap. all four descriptor slots are initialized. selected indices 1/3 and 2/3 stay in range. one submission records GENERAL initialization, copies, transfer-to-compute and compute-to-transfer dependencies, then transfer-to-host visibility. every begun command buffer is submitted once and its timeline is waited before readback, reuse or destruction. wait_idle also precedes cleanup after an oracle failure. native device loss remains a failed run. no synthetic address or deliberate invalid GPU access is permitted. |
| verification and review | coordinator reviewed the complete caller, both local unsafe blocks, native descriptor writers, GENERAL initialization, completion/cleanup paths and root assertions before dispatch. opt0/opt3 compile, validate and pass the profile gate. both complete final instruction streams retain two native heaps, queried-size constants/ID strides, dynamic uniform indices, explicit LOD zero and an Aligned 16 physical store. no ordinary descriptor binding is present. all 34 current compiler source pairs pass. the E-024 compiler/shared unit results remain prior evidence because this checkpoint changes only the library. all 74 native CPU controls pass with each current host. the 20 compute record tests still pass in the 31-test E-027 image/record gate. MSVC Debug/Release builds with /W4 /WX. each current host passes all eight library-based actual GPU cases, with exact color bits, destination guard and unchanged texture bytes. explicit core/synchronization validation is requested, layer insertion is confirmed and both runs contain zero native validation diagnostics. native image/sampler descriptor sizes are both 32 bytes on this device, so unequal-stride runtime coverage is absent. output addresses remain below 4 GiB. no safe public API or independent review is claimed. |
| history | follows E-023's non-dispatched U-004 fixture. changing resource types, stage, invocation count, aliasing or lifetime requires renewed review before execution. |

### U-006. native heap image and sampler constructors

| field | record |
| --- | --- |
| identity and scope | RustGPU `101153ea8fd0275468c271f03038302bbe7ded6f`, exported in rustgpu-native-heap-library.patch. `spirv-std/src/native_heap.rs` SHA-256 `32f904f04c139e295101f2c2d1b5597de5bbe96d43d14af6ab752bb95a5f7356` contains `Image::from_resource_heap` and `Sampler::from_sampler_heap`, two unsafe functions, two explicit blocks and two narrow lint exceptions. compile-only `auxiliary/library_body.rs` has one unsafe entry, two blocks and one exception shared by five wrappers. `auxiliary/library_mixed_body.rs` has one entry, one block and one exception shared by two target wrappers. library_requires_unsafe has no unsafe site and rejects both calls. the actual compute and fragment consumers call both constructors under U-005/U-007/U-008. |
| necessity | existing RustGPU image/sampler operations need valid opaque values. safe Rust cannot validate an externally bound descriptor heap or keep its native resources alive. use the existing image/sampler types and sampling methods, with two unsafe constructors instead of a new resource abstraction or unchecked safe indexing. |
| interface and owners | both constructors return opaque values, own nothing and expose no Rust reference to a heap or image. callers validate the bound native heap, native-size index offset, descriptor kind, image/sampler compatibility, lifetime, access and synchronization. the host owns allocation, descriptors, submission and retirement. CPU stubs panic without touching memory. |
| pointer and value validity | each index addresses a complete initialized descriptor at `index * OpConstantSizeOfEXT(resource type)`. this is a native byte size, independent of Rust's opaque type layout. offset/range must not overflow and must obey native alignment and heap bounds. image parameters and valid accesses must agree with the descriptor. the assembly receives only an aligned local MaybeUninit slot, stores one complete opaque value, then assume_init returns it. no reference or typed dereference of descriptor bytes is fabricated. |
| lifetime and aliasing | copied handles do not extend lifetime. heaps, descriptors and resource state remain valid and without conflicting updates through every use and completion. the compile-only entry requires two live sampled images and compatible samplers, plus a distinct 64-byte output allocation. four lanes have disjoint 16-byte output ranges. no unique/restrict promise is attached to image descriptors. |
| concurrency and completion | constructors establish neither synchronization nor uniformity. divergent indices are permitted by the heap extension, subject to applicable resource capability rules. callers establish visibility before reading descriptors/images, exclude conflicting writes and wait before reuse/destruction. no Send/Sync or safe native dispatch API is added. |
| failure paths | missing capabilities/extensions must fail compilation/validation. invalid runtime indices and stale descriptors violate the unsafe contract, with no recovery guarantee. no native allocation or dispatch occurs in these source fixtures. upstream CPU gpu_only stubs retain a panic. |
| proof at each site | each constructor's block loads the caller-specified valid descriptor and writes its complete local result before assume_init. the shared entry supplies fixed valid candidate slots 1/3 and 2/3 for its four lanes. its physical write uses U-003 and a caller-owned complete aligned output allocation. source-local SAFETY comments document those obligations. |
| verification | all 34 required source/target pairs pass, including eight new cases. five final positive modules separately validate and retain native size/stride dependencies and one declaration per heap. repeated float/integer image construction compiles in fragment shaders under both ABIs. divergent four-lane compute compiles at opt0/opt3 and through qptr. missing heap capability/extension reject, as do safe constructor calls. four existing native address tests and two doctests pass, with one existing documentation ignore. Clippy and docs pass with warnings denied. the actual U-005 uniform and U-007 divergent consumers each pass eight GPU cases in each Debug/Release host with zero native validation diagnostics. U-008 adds actual sampled float fragment execution. storage images, arbitrary image parameters and task/mesh execution remain unproved. |
| review | coordinator self-review, 2026-09-19. inspected complete library/caller source, deny lints, local-slot initialization, descriptor-kind/native-size assumptions, no reference/restriction fabrication, all normalized expectations and final positive modules. U-005's current final streams preserve its one-invocation root and caller contract. eight-case uniform runtime is approved for this reviewed source only. no independent review or complete dependency/macro safety audit. the upstream divergent source fixtures remain compile-only and are never dispatched. U-007 separately reviews the actual divergent consumer. |

### U-007. actual NGAPI divergent heap sample

| field | record |
| --- | --- |
| identity and scope | `tools/theta/ngapi-probe/native_heap_divergent.rs::computeMain`, one unsafe entry, two explicit blocks, one narrow lint exception and both deny lints. matching .hpp asserts the root ABI. the native_heap_sample.cpp caller now serves two compiled fixture targets, with THETA_HEAP_DIVERGENT selecting the new header/artifact and four lanes. U-005 retains its one-lane mode. no new library unsafe site. |
| necessity and owners | lane-dependent heap access requires actual descriptors and physical output. no safe API enforces external resource validity or submission. the shader calls U-006's image/sampler constructors and U-003's physical store, while Rust arithmetic and existing sampling remain outside the unsafe blocks. the native host owns all allocations, descriptors, pipelines and completion. |
| pointer and value validity | root is output u64/resource_xor u32/sampler_xor u32 at 0/8/12, size16/align8 in both languages. output has 64 valid, 16-byte-aligned writable bytes followed by a 16-byte guard within an 80-byte native allocation. four lanes address offsets0/16/32/48. two complete sampled RGBA8 images and sampler heaps have four initialized native-size slots. masked indices select only images1/3 and samplers2/3. each descriptor/resource satisfies U-006. no synthetic pointer or resource reference is fabricated. |
| lifetime and aliasing | each lane writes one disjoint Vec4 and all lanes only read descriptors/textures. output is separate from both descriptor heaps, two textures, upload and readback. native objects and descriptor state stay live and unchanged until completion, including between the four phase submissions. shader values acquire no ownership. |
| concurrency and completion | dispatch exactly one workgroup with LocalSize4x1x1. the fixed experimental NGAPI profile queries, requires and enables shaderSampledImageArrayNonUniformIndexing before device creation. the module explicitly declares the corresponding capability. coherent upload, GENERAL initialization, transfer-to-compute, compute/transfer-to-copy and copy-to-host barriers precede timeline completion and CPU readback. wait_idle precedes immediate destruction, including failed-oracle cleanup. |
| failure paths | compiler/validator/profile mismatch prevents generated headers. device feature rejection is unsupported, never a pass. native range, size, alignment and creation checks precede recording. every begun buffer is submitted once, then awaited before inspecting output or cleanup. native fatal errors, device loss, validation diagnostics and timeouts fail the runner. no invalid GPU dereference is a negative test. |
| proof at each site | masked lane/root arithmetic yields only the two initialized compatible image/sampler slots. U-006 constructs complete local opaque values. local lane0..3, fixed workgroup and one dispatch group make U-003's 16-byte ranges disjoint and within the complete output. host source review confirms its 112-byte upload/readback ranges cover the 80-byte output/guard and 32-byte texture readback. |
| verification | opt0/opt3 shaders compile, validate and pass the final profile checks. Debug/Release native targets build under /W4 /WX, and all 122 divergent CPU controls pass in both. U-005 retains its 74 controls. all 20 Python record tests pass, including corruption of every output/guard/source word and uniform-broadcast/lane-permutation rejection. both Debug/Release hosts pass all eight opt/phase GPU cases with 28 exact words each, explicit core/synchronization validation, confirmed layer insertion and zero diagnostics. scalar six-case and uniform eight-case regressions also pass in both hosts. source recompilation preserves their prior payload hashes. native image/sampler strides are both 32 bytes and output addresses remain below 4 GiB, leaving unequal-stride and high-address coverage open. |
| review | coordinator self-review before dispatch, 2026-09-19. source SHA-256 `1d2e8bf643cdf1ef9d01e9bd034c10e4d31297efaaea8eb39d70b3fc27446e7a`; shared native caller `b027e3a7372094cef1165013d83181cb7d95eaa21c5bf934326391717a356b56`. reviewed complete source, both final instruction streams, root assertions, descriptor writers, two native feature checks, disjoint lane ranges, transfer/readback bounds, visibility, completion and cleanup. opt0/opt3 payloads are `e96e31502f5a7502f77bc39032febb171a2d36c39d59018aa7159c2ce684fb34` and `9e7b2aff896f29f09d1cb6493ded1b20d830e734c764eb883f739da73dcc4538`. renewed U-005 review confirms its one-lane ranges/oracle remain equivalent. actual runtime passed with NGAPI a9b7e3fb00704385f186ba804483f5e7b0be5a9e after review of the two-line feature prerequisite. inspected all final records and native streams. no independent review or full dependency soundness claim. |

| status and history | reviewed and executed at E-026. this adds a separate four-lane caller after E-025, preserving the one-lane fixture. host/feature/layout/lane/resource changes require renewed review. no general resource-type, stage or safe-host guarantee is made. |

### U-008. actual NGAPI textured cube

status: reviewed and executed in Debug/Release at E-027, with exact color/depth and native input/guard checks.

| field | record |
| --- | --- |
| identity and scope | native_heap_cube.rs has two unsafe entry declarations, two explicit blocks and two narrow exceptions, with both deny lints. vertexMain calls U-003's PhysicalPtr<Vertex>::read. fragmentMain calls U-006's image/sampler constructors. all math, interpolation and sampling use existing safe operations. the matching .hpp asserts the native layout. |
| necessity and owners | an actual physical vertex fetch and native heap sample cannot validate external allocation/descriptor lifetime in shader code. the native caller owns all vertices, indices, descriptors, sampled images, attachments, views, pipelines and completion. there is no safe unchecked shader dispatch API or new generic library boundary. |
| pointer and value validity | a complete initialized 576-byte vertex range contains 24 elements of size24/align4, with position at 0 and UV at 16. 36 u16 indices are in0..23, and base vertex is zero. finite float values and a row-major transform are supplied. root size80/align8, offsets0/8/72/76 preserve the original cube prefix and append two selectors. uniform resource1/3 and sampler2/3 indices select initialized compatible native-size slots from four-slot heaps. every image is RGBA8 UNORM and GENERAL. attachments use RGBA8 UNORM and D32 float. no descriptor or physical allocation becomes a Rust reference. |
| lifetime and aliasing | vertices, indices, descriptors and sampled images are read-only during each draw. color/depth attachments and readback are disjoint from them. all views, images, allocations, heaps and pipelines remain live until submission completion. local copies acquire no resource ownership. |
| concurrency and completion | one queue/thread, one indexed draw per case. coherent host uploads precede submission, transfer-to-vertex/index/fragment dependencies precede draw, attachment-to-transfer dependencies precede readback, and transfer-to-host visibility plus timeline wait precedes CPU access/reuse. later attachment writes are ordered after preceding reads. wait_idle precedes immediate destruction on every nonfatal cleanup path. |
| failure paths | compile/validation/profile/layout failure prevents header generation. host checks device formats, allocations, complete nonwrapping ranges, descriptor sizes, indices, creation results and file writes. unsupported hardware, device loss, process failure, timeout, missing output and validation diagnostics fail the required gate. no invalid GPU access is a negative test. every begun command buffer must submit once and complete before cleanup. |
| proof at each site | the fixed checked indices and zero base vertex bound U-003's aggregate reads. the host initializes the complete six-float vertex before use. U-006 receives only initialized in-range compatible descriptors, with native size/alignment and immutable resource state. source-local contracts expose the external obligations. shader compilation cannot establish provenance or native completion. |
| verification | opt0/opt3 source modules compile and validate, with the correct root/vertex layout and both stage interfaces. opt0 has one aggregate Aligned4 physical read, opt3 has six scalar Aligned4 reads, both use stride24. both final streams retain native heaps and implicit fragment sampling. Debug builds under /W4 /WX and all ten native range/index controls pass without creating a device. the independent ray/box oracle uses exact RGBA, depth tolerance 3e-6 and predeclared edge/texel tolerances, masking at most 256 of 16384 pixels. all 31 Python image/record tests pass, including known analytic rays, malformed color/depth/blank/sampler/view/resource/truncation controls, stale identity/layout/range errors and missing/duplicate cases. each Debug/Release host passes all eight GPU cases and ten native CPU controls, with confirmed core/synchronization validation insertion and zero diagnostics. each image checks 16,216 to 16,257 pixel colors/depths, with exact color agreement and maximum depth error below 7.95e-8. the native caller checks all source bytes and both guards. output images were also inspected, as secondary evidence only. |
| review | coordinator self-review before first draw, 2026-09-19. shader source SHA-256 `8e1c75c296ac3fbc65f65bc9cbd2c1e49feb14c5d09034446e0b8a6972e6176b`, native caller SHA-256 `11a3035677c986cb48bee32f69b923df60229d774d0699292921e559943a09e5`, opt0 SHA-256 `cbd3d78e37bb6a889eaa85140968e8c0c52dea5952c3824dd6bc6ca267bdfff8`, opt3 SHA-256 `aa625982916d3f8f0f3097ae5945cb173af3fe38289711af950d261c355a548b`. inspected both complete final modules, source and native call chain, 696-byte input layout, disjoint image/source/guard readback ranges, native coherent mappings, all descriptor writes, attachment views, fixed valid draw indices, copy/graphics/host dependencies, timeline waits and cleanup. separate native allocations own every live resource, and GENERAL transitions are consumed by the first submitted buffer. changed a prospective overlapping readback copy to disjoint ranges before build or GPU work. rustfmt expanded only the vertex access expression after the first passing Debug/Release runs. renewed source review and recompilation confirm byte-identical opt0/opt3 payloads. both final hosts were rebuilt with the updated embedded source identity and passed all eight cases again. no independent review or full dependency soundness claim. |
| remaining risk and history | new graphics caller after the compute-only E-026 checkpoint. the unlit fixed-view diagnostic is a documented adaptation of the NGAPI cube, not original spinning/lit visual parity. high addresses, unequal strides, presentation, Linux and a safe Rust host remain separate evidence. runtime records and both native streams were inspected after passing the complete image/record gate. any shader, caller, layout, resource or synchronization change requires renewed review before dispatch. |

### U-009. ordinary AGFX multi-dispatch shader

status: reviewed and executed through the direct Rust host at E-029, opt0/opt3 in Debug/Release.

| field | record |
| --- | --- |
| identity and scope | shaders/rust/compute_multi_dispatch.rs::main_cs, translated from the frozen AGFX multi_dispatch.hlsl. one unsafe entry, one explicit RuntimeArray::index_mut call and one narrow lint exception, with both deny lints. ordinary math and bounds-checked word access remain safe Rust. this uses upstream RuntimeArray/TypedBuffer, without new generic library code. |
| necessity and owners | a runtime descriptor array carries no Rust length. its existing index_mut operation is unsafe and cannot prove host binding validity. the host owns every descriptor and allocation, while each invocation accesses one disjoint u32. no new safe unchecked descriptor wrapper is exposed. |
| pointer and value validity | the root is four u32 fields at 0/4/8/12, size16/align4. set0/binding0 is an ordinary storage-buffer descriptor array with a valid uniform resource index. the selected complete buffer contains 64 initialized u32 words, with four-byte alignment and a valid range of at least256 bytes. all u32 bit patterns are valid. this is a logical32 shader, not a native heap or physical-address substitution. |
| lifetime and aliasing | all 64 lanes use one uniform descriptor and write distinct words. initialization and four ordered dispatches precede readback. no concurrent host/GPU users or aliased writable descriptor selects conflict with the active buffer. descriptors, buffers and pipeline remain live until completion. the host never forms a Rust slice while GPU work accesses that memory. |
| concurrency and completion | one 64-thread workgroup per pass, pass indices0..3. host shader-write to shader-read/write barriers order each dependent pass, then transfer/host visibility and completion precede readback or retirement. no Send/Sync or ownership promise is added by the shader. |
| failure paths | source, validator or profile errors prevent valid artifact metadata. an out-of-range descriptor violates the unsafe contract and is never used as a GPU negative test. the existing typed slice bounds check does not establish descriptor validity. U-010 owns device/buffer/copy behavior and U-011 owns reviewed pipeline/descriptor creation, dispatch and cleanup. |
| proof at each site | the U-011 host validates the uniform descriptor index against the initialized set and retain its selected complete buffer. the global invocation ID and single workgroup give disjoint indices0..63. the explicit element-count guard and typed slice bounds check limit word access. U-011's distinct initialized buffers, complete set update, borrowed lifetime and synchronous completion establish the external caller conditions for this fixture. |
| verification and review | opt0/opt3 source compilation and Vulkan 1.3 validation pass. coordinator inspected both complete final instruction streams, root offsets, uniform descriptor access and bounded disjoint u32 load/store. source SHA-256 `0bd17bcd69b1f3c59331fa86da689a4d1f0bbd081e7d859a8e4ee954b4487143`. payload hashes `e191ccd221de55e383fce98ad60f79ed1dbe8ec4b5fa1a8389c8a665e0daf655` / `8bb34555fa9101ad52ecee2a01991f90868c7d0d26c188410e224ee674202d3a`. explicit StorageBufferArrayDynamicIndexing and its matching host feature were added from the normative interface rule, even though the validator accepted the earlier missing capability. the frozen golden remains 64 words 15*i+11. E-029 passes all four opt/index cases in each Debug/Release host, plus exact unchanged sentinel buffers and zero Khronos core/synchronization diagnostics. no independent review or general soundness claim. |
| remaining risk and history | introduced for T006-T009's direct Rust Vulkan slice after E-027's actual NGAPI cube. U-010/U-011 register the actual host and its narrow execution boundary. wider descriptor/resource behavior remains unproved. |

### U-010. Rust Vulkan device, buffer and synchronous submission

status: coordinator reviewed before execution, then passed the bounded copy fixture in Debug/Release on Windows Vulkan at E-028.

| field | record |
| --- | --- |
| identity and scope | `crates/agfx/src/vulkan.rs` SHA-256 `082748d2e73178600599516f8313a98d0006baf2b656486d0404da4f23f833f2` owns ash loader/instance/device calls, buffer allocation/mapping, command recording, synchronous timeline completion and destruction. 41 explicit unsafe blocks, one private unsafe callback function and 13 function-level allow scopes. no unsafe traits/impls, foreign declarations or unsafe attributes. crate deny lints and binary forbid lints pass. the callback ABI is separately part of this record. |
| necessity | Vulkan FFI and mapped native bytes require external validity/lifetime facts. safe Rust handles bookkeeping, checked ranges, initialization state, metadata and oracles. ash is a binding with caller obligations, not a safe resource owner. no C++ wrapper or new backend abstraction is added. |
| interface and owners | one non-Send/non-Sync Device owns loader, instance, logical device, one graphics/compute queue, command pool and timeline. each unique Buffer borrows its Device, owns one allocation and hides native handles. mapped bytes never escape a synchronous write/read method. copy borrows source and destination until completion. a Completion borrows its originating Device. no mandatory shared ownership or unsafe trait impl is needed. |
| pointer and value validity | allocate nonzero four-byte-sized buffers, honor native memory requirements/type bits, require coherent mapped upload/readback and prefer cached readback. exact full writes initialize uploads. copies check aligned nonoverflowing source/destination ranges and reject overlapping destination regions. an uninitialized destination must be covered completely. mapped u8 copies use complete live allocation ranges and no fabricated typed references. |
| lifetime and aliasing | unique allocations do not alias. &mut Buffer prevents concurrent host mapping/readback while copied. native buffers outlive all recorded/submitted uses because operations are synchronous and borrowed. buffers cannot outlive Device. command buffers are freed only before submission or after known completion. source references and Vulkan create-info arrays remain live for each FFI call. |
| concurrency and completion | native host access is single-threaded, enforced by a non-Send/non-Sync marker. coherent host writes precede submit. explicit transfer and host dependencies precede mapped reads. timeline values increase without wrapping and each operation waits for its submitted value. no completion token exists before that wait succeeds. shader/pipeline work requires a separate later contract. |
| failure paths | unsupported version/features/layers, invalid operations and initialization/allocation failures return errors before submission with partial owners cleaned up. native submission or wait errors that leave completion uncertain must fail the process rather than retire possibly pending resources. device-loss recovery is not claimed. unit tests never load a GPU. external runner timeouts/crashes/validation errors remain failures, not skips or passes. |
| proof at each site | native owners and borrow lifetimes establish device/resource ordering. validated ranges and complete initialization establish byte-copy validity. private handles and no escaping maps prevent safe external callers from bypassing these conditions. the Vulkan implementation/driver and ash-generated functions remain dependency boundaries. debug callback message pointers are borrowed only for the callback duration. |
| verification and review | coordinator reviewed every source site, callers and cleanup path before execution. 3 pure range/golden tests, 3 compile-fail lifetime/thread tests, Debug/Release compilation, denied-warning Clippy and 8 pure record controls pass. reviewed create-info pointer lifetimes, reset of the debug messenger pNext after instance creation, partial owner cleanup, coherent memory selection, full initialization, barriers, pending-command abort guard and native handle privacy. Debug/Release compiled identities and dependency lock are recorded in the ignored pre-GPU review. all 17 native controls and the exact 256-byte readback pass in both final hosts, with confirmed layer insertion and zero Khronos core/synchronization diagnostics. ten exact intentional loader notices per run are retained separately. the first runner attempt failed by classifying those loader notices as validation errors, then the narrowed classification control and fresh runs passed. no independent review or soundness proof. |
| renewal for U-011 | the Device now retains queried physical limits, records the already queried loader API version, exposes the private compute child module and preserves AGFX's discrete/integrated/virtual/CPU preference order. the final selection loop was reviewed against the frozen source before rerunning both fixtures. no U-010 native operation or scope changed. renewed source/caller review and matching compiled identities preceded the shader run. copy regression passes in both renewed hosts with all 17 native controls and zero Khronos correctness diagnostics. |
| dependency boundary | Cargo.lock pins ash 0.38.0+1.3.281, libloading 0.8.9 and all host dependencies with registry checksums. used ash generated entry/instance/device calls and debug-utils dispatch require the registered native validity rules. Vulkan 1.4 is queried/requested using existing core buffer APIs covered by these bindings. this does not audit all ash, platform loader, SDK or driver unsafe code. serde_json and sha2 serve only safe runner records/identity, with their transitive native code outside this first-party count. |
| remaining risk and history | new T006/T007 host after E-027. synchronous buffer transfers intentionally retain source fixture completion behavior. asynchronous recording, arbitrary shader dispatch, native handle escape, full AGFX parity, Linux execution and device-loss recovery remain outside this initial boundary. |

### U-011. bounded ordinary storage-buffer compute pipeline

status: coordinator reviewed before dispatch, then executed all four opt/index cases in each Debug/Release host at E-029.

| field | record |
| --- | --- |
| identity and necessity | `crates/agfx/src/vulkan/compute.rs` owns shader module, four-slot storage descriptor set/pool/layout, 16-byte compute pipeline layout, pipeline creation, descriptor updates and four ordered dispatches. Vulkan FFI needs unsafe, while root packing, bounds, identities, oracles and ownership remain safe. each FFI operation requires a local U-011 comment and narrow lint scope. |
| public contract | shader bytes, stage and entry are separate explicit inputs. arbitrary shader creation is an unsafe constructor with the complete bounded profile obligation. its module must be valid for the enabled Vulkan features, use only the declared root and set0/binding0 array, and for one 64-thread workgroup access only its selected initialized 64-word storage buffer with disjoint lane writes. no physical addresses or other native resources are permitted. safe dispatch validates root values, all four same-device buffers, exact byte ranges and full initialization. |
| owners and lifetime | the unique compute owner borrows Device and retains its layouts, pool/set and pipeline until destruction. dispatch borrows four distinct mutable buffers through synchronous completion. descriptor updates happen only with no pending uses, and every slot is rewritten before a later dispatch. destroyed buffers may leave stale descriptors only between completed dispatches, when the set is never accessed. the shader module is retired after pipeline creation, including failure cleanup. |
| values and concurrency | descriptor count4, complete 256-byte ranges and four initialized distinct allocations, one chosen nonzero index1 or3 in the fixture. root is four u32 values at offsets0/4/8/12, exactly16 bytes, count64, passes0..3 and padding0. native limits and required features are queried. transfer-write to compute-read/write, dependent compute-write to read/write and final transfer/host barriers establish visibility. one thread, family and synchronous timeline as U-010. |
| failure and cleanup | reject bad stage, module header, root, buffer size/role/device or unsupported limits before dispatch. a failed multi-pipeline creation may return partial handles and must destroy them. descriptor/pipeline owners unwind safely before submission. uncertain submission/wait completion follows U-010's abort rule. never use undefined descriptor/shader access as a negative test. |
| caller proof | the fixture requires a reviewed payload hash, explicit matching metadata and fresh source/compiler identity before the unsafe constructor. final SPIR-V inspection and U-009 establish the entry's bounded access contract. distinct sentinel buffers in unused slots and exact source goldens check descriptor selection and ordered recurrence. shader validation is evidence, not a substitute for the constructor's full contract. |
| review and limits | complete implementation and caller review before GPU. compute.rs SHA-256 `f2bbbb700061e0ad22080d2aad9ad6b7bc388448d1edd722304c80980646c347`, 15 unsafe blocks, one public unsafe constructor and 4 narrow allow scopes. multi_dispatch.rs SHA-256 `9af7d0a7deb485c424f0540db8ed48562cb0a65f8d580c1db032ce475b8736fb` has one unsafe constructor call and one narrow allow, in ReviewedShader::pipeline. its private loader admits only the two reviewed U-009 payload hashes and matching metadata. reviewed native limits, partial creation cleanup, no retained shader module, full descriptor rewrite between completed runs, complete initialized buffers, root packing, bounds and all barriers. Debug/Release builds, root CPU controls, denied-warning Clippy and record controls pass. exact source/binary identities are saved in the ignored pre-GPU review. all four opt0/opt3 and slot1/3 cases pass in each Debug/Release host, checking all 1024 bytes per case, plus ten isolated native controls. eight invalid artifact controls reject before Vulkan. 19 Python record/phase controls, five Rust CPU tests and three compile-fail docs pass. renewed review covers the recorded loader-version field and final exact compiled identities. no native operation changed in that renewal. no safe arbitrary shader loader, asynchronous reuse, independent audit, full descriptor API or cross-platform GPU claim. |

### U-012. aggregate physical access and loaded alias probe

status: coordinator reviewed before dispatch, then executed all 12 cases in each Debug/Release host at E-030.

| field | record |
| --- | --- |
| identity and scope | the owned RustGPU `tests/compiletests/ui/physical_storage/auxiliary/operations_body.rs` is the single generic shader source for upstream compile wrappers and the separate NGAPI diagnostic. it uses deny lints, one unsafe entry, seven explicit unsafe blocks and one narrow allow scope. export its exact patch and source hash at the verified checkpoint. no new public library unsafe method is added. |
| necessity | U-003 value reads/writes exercise a 24-byte aggregate containing a device address and a three-word array. explicit assembly exercises Function storage with one volatile store and two volatile loads, including an unused load. these operations require shader allocation and instruction-validity obligations. address transport, root packing, expected values and record handling remain safe. |
| caller contract | exactly one invocation receives a 16-byte root with source/destination u64 addresses at offsets0/8. source is a live nonzero eight-byte-aligned allocation containing Source: alias at0, three u32 values at8 and tag at20, size24/alignment8. the alias must equal destination. destination is a disjoint live allocation of at least32 bytes aligned to eight, exclusively writable by this invocation without concurrent readers/writers or incompatible Rust references. initialize source and guards visibly before dispatch, retain all allocations through completion and establish output visibility before readback/reuse. |
| alias and value validity | two non-owning address values intentionally name the same destination. all accesses occur sequentially within one invocation, with no physical Rust references. destination is initialized by the first array write before either alias reads it. all u32/u64 representations are valid, and the loaded alias is checked by the host before dispatch. source remains unchanged. seven output words are written and the eighth remains a guard. |
| assembly boundary | Function storage belongs only to this invocation and is initialized before either volatile load. the temporary Rust output reference is live, aligned, exclusively borrowed for the block and receives a valid u32. no reference or temporary pointer escapes. inspect final memory operands and the unused load after both ordinary/qptr lowering and optimization. numeric output alone cannot establish volatile preservation. |
| native owner and failure paths | the C++ diagnostic owns four heaps, the pipeline and a timeline through completion. coherent uploads initialize every source/destination byte. transfer/compute/transfer/host barriers and a timeline wait precede inspection, reuse and destruction. null-safe native destruction handles precondition rejection. NGAPI allocation/pipeline/submit failures retain its fatal native boundary, with no recovery guarantee or safe Rust wrapper. the separate runner rejects stale embedded identities before dispatch and preserves streams plus unknown execution on a timeout. unsupported device creation is not a pass. |
| review | coordinator self-review, 2026-09-19. complete shader, compiler/native optimizer changes, C++ caller, record oracle and final four instruction streams inspected. source SHA-256 `3b93694b1cfb92311ac20fb75ff3eba83a03762e2648678743638d2ecdb823b8`, host `7c60a9e20569df7ac6c4ae79b8baa5888d18dcc6b7d41e1afd187552f46d747d`. the inventory is one unsafe entry, seven blocks and one allow. RustGPU `e59c924a5287c9b8cf7d8c49cc37aa8d6bb0bc08` passes 48 compiler and three shared-type tests, 38 source pairs, denied-warning Clippy and formatting. both native builds pass 25 CPU controls and embed exactly the reviewed source/root/host/four payload identities. the ignored pre-GPU record retains exact binary/compiler/library hashes. six Python controls include all 768 readback-byte mutations and timeout preservation. no independent review or complete dependency soundness proof. |
| validation and limits | Debug and Release each pass 12 required shader cases and 25 native controls with explicit Khronos core/synchronization insertion and zero diagnostics on the RTX 4090 Laptop GPU. every case checks all 64 readback bytes. real addresses remain below 4 GiB. the final compiler preserves all ten older consumer payloads exactly, and the four newly reviewed payloads are also unchanged after lint cleanup. the broader existing subpass failure and full upstream/native CI remain open. this fixture does not implement a volatile library API, concurrent aliasing, effectful copies, atomic/reference methods or general raw-pointer parity. |

E-031 audit: the five new raw-pointer method fixtures use `forbid(unsafe_code)` and only transport addresses. they neither dereference a device address nor construct physical Rust references. the positive case changes pointer constness without changing the pointee. four negative families retain complete unsupported-operation diagnostics. no compiler/library implementation, unsafe site, FFI, native caller, layout or synchronization rule changed. the existing U-001 through U-012 reviews remain scoped to their recorded inputs and execution. no new U record, GPU rerun or broader soundness claim is needed.

### U-013. native storage-image fixture

status: coordinator reviewed the complete source, final modules and both native binaries before dispatch, then executed all 16 cases per Debug/Release host at E-032.

| field | record |
| --- | --- |
| identity and scope | owned RustGPU `tests/compiletests/ui/descriptor_heap/auxiliary/storage_body.rs`, shared by default/qptr and opt0/opt3 compile wrappers. one unsafe entry, three explicit blocks and one narrow allow, with deny lints on every wrapper. no new library unsafe method. |
| necessity | H05 needs actual native storage-image descriptor loads, ImageRead/ImageWrite and a separate physical output. safe address transport or sampled-only fixtures cannot exercise writable storage descriptors. |
| interface and caller contract | Root contains a PhysicalPtr<[u32;4]> at0 and source/destination u32 indices at8/12, total16/alignment8. caller binds initialized live descriptors for distinct rgba32ui 2D storage images at least2x2, in GENERAL layout with visible initial contents. exactly one invocation reads source(1,0), writes destination(0,1), and writes a separate aligned16-byte physical range. |
| validity and ownership | every u32 bit pattern is valid. descriptors use their native size/alignment and stay unchanged, and complete image/output ranges stay live through queue completion. source is read-only for this invocation, destination texel and output are exclusively writable, with no incompatible aliases or concurrent access. no physical Rust reference is created. |
| synchronization and cleanup | [native_heap_storage.cpp](tools/theta/ngapi-probe/native_heap_storage.cpp) owns two disjoint placed textures, a four-slot native descriptor heap, upload/output/readback heaps, pipeline and timeline. every image texel and descriptor is initialized. transfer-to-compute, compute/transfer-to-transfer and transfer-to-host barriers plus timeline completion precede readback, reuse and destruction. NGAPI native allocation/submit failure retains its fatal boundary. unsupported feature/format checks prevent dispatch. null-safe retirement is used only after idle/completion. no safe Rust native wrapper or recovery guarantee is introduced. |
| proof at source sites | constructor block relies on exact descriptor types, valid indices and retained lifetimes. write block uses an in-bounds destination texel and valid typed data. PhysicalPtr write relies on the separate complete aligned exclusive output range. local SAFETY comments mirror the entry contract. Image read/write internals and U-003/U-006 remain dependency/library boundaries with their prior scope. |
| review and pending evidence | coordinator self-review, 2026-09-19. source SHA-256 64b0dfbf21a4244cfd7bee76841d169a776b7a68ba1629b75c3ce0e63cab54df, host 3dab0fbbee69d337bde34bd1f85d2c891cdc4155eac24b2efe170422782e1beb. reviewed complete source/caller, all four final instruction streams, descriptor stride/types, root 16 bytes at offsets 0/8/12, image read/write coordinates, aligned 4-byte output, wrapping arithmetic, barriers and cleanup. all 47 required source pairs and five Python controls pass, including every one of 2560 readback-byte mutations and mocked timeout retention. Debug/Release build with /W4 /WX, pass 169 native CPU controls each, and embed exact current source/host/payload identities. the ignored pre-GPU review records every binary/module hash and NGAPI b52ffa02/RustGPU2cf7d9a. no production RustGPU code changed. no independent audit. |
| execution and limits | each Debug/Release host passes 16 required cases and 169 CPU controls on NVIDIA GeForce RTX 4090 Laptop GPU, with confirmed Khronos core/synchronization insertion and zero diagnostics. every case compares all 160 bytes, including unchanged source/other texels and physical output guards. observed native image descriptors are 32 bytes and output addresses are below 4 GiB. default/qptr and opt0/opt3 execute with nonzero descriptor slots. no other storage format, image atomic, divergent storage index, concurrent access or cross-platform GPU claim. |

### U-014. task payload and mesh physical vertices

status: coordinator reviewed the six-group fixture, final modules and both compiled native hosts before dispatch. all sixteen cases passed in each Debug/Release host at E-033.

| field | record |
| --- | --- |
| identity and scope | owned RustGPU `tests/compiletests/ui/descriptor_heap/auxiliary/mesh_body.rs`, four `mesh_*physical64.rs` wrappers, taskMain/meshMain/fragmentMain. three unsafe entries, four source unsafe blocks and three narrow allows. the local write_vertex macro expands its physical-read block four times, making seven expanded blocks. no compiler or library implementation change. [native_heap_cube.cpp](tools/theta/ngapi-probe/native_heap_cube.cpp) with THETA_CUBE_TASK_MESH uses create_mesh_pso/draw_meshlets and the existing numeric oracle. source SHA-256 `d02b1333d03e401d2856a3a0c4b87952b6a9a7b9ecb972059e1a43f0d7102bf0`, host `daba9a7a1239f4c77bfe4b1e264f74a0c179723da5876ef0f8f74dc0d26a7a41`. exact module/binary identities are recorded in the ignored pre-GPU review. |
| necessity | task emission and mesh output counts use existing unsafe stage intrinsics. physical vertex reads and native heap construction retain U-003/U-006's obligations. safe payload copying, transform arithmetic and constant output writes need no exception. |
| interface and owners | all three shader entries are unsafe with complete local contracts. the compiletest harness never dispatches them. the C++ host owns initialization, queried features, descriptors, attachments, root data, submission and retirement. no safe arbitrary-dispatch API. |
| pointer and value validity | root/payload size80, alignment8 and offsets0/8/72/76. Vertex size24, alignment4 and UV offset16. a live 696-byte input allocation includes 576 bytes containing 24 initialized finite vertices without address overflow. task copies the complete payload then emits six mesh groups along X. each group's x in0..6 selects four physical vertices using full u64 arithmetic and constant local slots0..3, yielding indices0..23. each group outputs four vertices and two triangles using only constant output indices. no physical Rust references or synthetic address dereferences. |
| lifetime and aliasing | vertex, descriptors, sampled images, root and payload remain valid through their users. physical data and sampled resources are immutable, disjoint from writable color/depth attachments and output interfaces. task-payload visibility follows TaskPayloadWorkgroupEXT emission semantics. mesh groups share only immutable data, with private mesh outputs. |
| concurrency and completion | one task group with one invocation emits six child groups, each with one invocation. emission is uniformly once as the terminating operation. each mesh group sets output counts uniformly once before writing outputs. transfer-to-task/mesh/fragment visibility and synchronous timeline completion precede readback, reuse and destruction. no concurrent host access or Send/Sync claim. |
| failure paths | invalid shader input cannot be recovered from. stale identities, compile or validation failure prevent dispatch. NGAPI retains its fatal native allocation/pipeline/submit boundary and requires completion before ordinary destruction. the external runner preserves streams and unknown execution after timeout or abnormal exit, with any completed case records separate. no device-loss recovery promise. |
| proof at each site | task copies the complete typed root before emission. mesh sets counts, then four macro expansions select complete aligned immutable Vertex values using the bounded group ID and literal slots. the fragment's native descriptor constructors rely on initialized native-size descriptor ranges, compatible image/sampler lifetimes and visibility. each source block has a local SAFETY explanation. |
| verification and review | coordinator source/caller self-review, 2026-09-19. no independent audit. the host's root, 24-vertex input, twelve triangles, transforms and numeric color/depth oracle are unchanged. the fixed NGAPI profile queries its already enabled taskShader feature. new shader group/output counts remain below the required EXT minima. all four final modules validate with matching stage/group/ABI checks. full default opt0 and qptr opt3 streams were reviewed. both hosts compile at /W4 /WX, embed matching source/host/ABI/payload identities and pass ten CPU controls each. all 46 Python controls pass, including abnormal-exit/timeout preservation. all 51 source pairs pass. Debug/Release each pass sixteen actual image/depth cases with zero Khronos core/synchronization diagnostics. all input bytes/guards and completion checks pass. both indexed-cube regressions also pass. committed recompilation retains exactly the executed payload bytes. high-address execution remains absent. |
| investigation and limits | original RustGPU8fa5966 compiled and validated in four variants but the NVIDIA pipeline compiler faulted before a shader case. split-stage controls isolate the original mesh body. a diagnostic-only module with four bounded Output indices converted from u64 to u32 creates a pipeline while preserving physical addressing. that edited module is not a delivered shader. the revised fixture uses fixed output slots and six groups, preserving the image contract without altering RustGPU's usize ABI or pointer width. original source/modules/binaries and diagnostic evidence remain ignored and retained. dynamic 64-bit mesh output indexing, broader task/mesh parity and execution on other drivers remain unproved. |

## checkpoint audit

inventory all first-party unsafe blocks, unsafe functions/traits/impls, foreign declarations/interfaces, unsafe attributes, and lint exceptions across every owned target and feature configuration. inspect relevant macro expansions and generated bindings separately. text search is a starting aid, not proof of complete coverage. record the method, tool versions, source scope, and exclusions.

map every site to its U record and reconcile the actual source inventory with this page. list dependency-provided unsafe boundaries separately with exact dependency identity, used APIs, trusted guarantees, and known limitations. distinguish first-party sites from dependency/generated totals instead of hiding one in the other. mirror the relevant safety contracts into the RustGPU patch so an upstream reviewer does not need this repository to understand it.

a checkpoint cannot close with an unlisted site, stale contract, unexplained lint waiver, or unreviewed boundary it relies on. a changed caller, layout, synchronization rule, feature configuration, or dependency can invalidate a prior review even when the unsafe line is unchanged. retire removed records with a pointer to the removal instead of leaving stale active claims.

run checks appropriate to the actual risk. CPU tools such as Miri or sanitizers can help supported host logic, but cannot establish native GPU correctness. use deterministic readback and Vulkan validation where applicable. passing tests and validation do not prove soundness. do not run undefined GPU accesses as negative tests expecting a recoverable error.

append the audit result, unresolved issues, and evidence scope to [execution](docs/execution.md). promote any reusable finding to [lessons](docs/lessons.md). reducing unsafe sites is useful only when the remaining contracts become simpler and more credible.

## references

the [Rust book](https://doc.rust-lang.org/book/ch20-01-unsafe-rust.html), [Rustonomicon](https://doc.rust-lang.org/nomicon/safe-unsafe-meaning.html), and [unsafe-operation lint guide](https://doc.rust-lang.org/edition-guide/rust-2024/unsafe-op-in-unsafe-fn.html) explain Rust's obligations. [Vulkan queue submission in ash](https://docs.rs/ash/latest/ash/struct.Device.html#method.queue_submit) is an example of a binding with caller obligations. use exact toolchain/dependency versions for implementation reviews.
